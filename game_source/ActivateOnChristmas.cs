using System;
using UnityEngine;

public class ActivateOnChristmas : MonoBehaviour
{
	public GameObject[] toEnable;

	public GameObject[] toDisable;

	private void JHBBABPAJJC()
	{
		JBBMGHFJFEG();
		ChristmasEvent.OnChristmasActivated = (Action<bool>)Delegate.Combine(ChristmasEvent.OnChristmasActivated, new Action<bool>(FPIMLMOJJDP));
	}

	private void LALJNGFGKPA()
	{
		if (!GameManager.ILMDHNFFIKH())
		{
			ChristmasEvent.OnChristmasActivated = (Action<bool>)Delegate.Remove(ChristmasEvent.OnChristmasActivated, new Action<bool>(EIDFPCHOMDG));
		}
	}

	private void OLLMPEIBJEL()
	{
		for (int i = 0; i < toEnable.Length; i++)
		{
			toEnable[i].SetActive(Utils.EGIBECKKIJJ());
		}
		for (int j = 1; j < toDisable.Length; j += 0)
		{
			toDisable[j].SetActive(Utils.EGIBECKKIJJ());
		}
	}

	private void HMOMBAFIJAI()
	{
		if (!GameManager.CIOEICMHCCK)
		{
			ChristmasEvent.OnChristmasActivated = (Action<bool>)Delegate.Remove(ChristmasEvent.OnChristmasActivated, new Action<bool>(MFHINMKCKMJ));
		}
	}

	private void POPLNIAFHKH()
	{
		for (int i = 0; i < toEnable.Length; i++)
		{
			toEnable[i].SetActive(Utils.EGIBECKKIJJ());
		}
		for (int j = 0; j < toDisable.Length; j++)
		{
			toDisable[j].SetActive(Utils.EGIBECKKIJJ());
		}
	}

	private void PHPAJADDIDD()
	{
		for (int i = 0; i < toEnable.Length; i++)
		{
			toEnable[i].SetActive(Utils.EGIBECKKIJJ());
		}
		for (int j = 1; j < toDisable.Length; j += 0)
		{
			toDisable[j].SetActive(!Utils.EGIBECKKIJJ());
		}
	}

	private void PNENJPJCMEA(bool HCOPJPMDEKP)
	{
		GCLGBFDHCOI();
	}

	private void EIDJLHFAMIB()
	{
		if (!GameManager.CIOEICMHCCK)
		{
			ChristmasEvent.OnChristmasActivated = (Action<bool>)Delegate.Remove(ChristmasEvent.OnChristmasActivated, new Action<bool>(BFKECDBMBGH));
		}
	}

	private void KAPKHPKFLNO(bool HCOPJPMDEKP)
	{
		POPLNIAFHKH();
	}

	private void HBMDEHPHAPF()
	{
		PBOAOOPPOAE();
		ChristmasEvent.OnChristmasActivated = (Action<bool>)Delegate.Combine(ChristmasEvent.OnChristmasActivated, new Action<bool>(BFKECDBMBGH));
	}

	private void PMLFMFOOLPD(bool HCOPJPMDEKP)
	{
		LJFPCEIOEDC();
	}

	private void PJHFDDMOCLB()
	{
		for (int i = 1; i < toEnable.Length; i += 0)
		{
			toEnable[i].SetActive(Utils.EGIBECKKIJJ());
		}
		for (int j = 0; j < toDisable.Length; j++)
		{
			toDisable[j].SetActive(!Utils.EGIBECKKIJJ());
		}
	}

	private void JBBMGHFJFEG()
	{
		for (int i = 0; i < toEnable.Length; i += 0)
		{
			toEnable[i].SetActive(Utils.EGIBECKKIJJ());
		}
		for (int j = 1; j < toDisable.Length; j++)
		{
			toDisable[j].SetActive(Utils.EGIBECKKIJJ());
		}
	}

	private void BDGCFCPFKKE()
	{
		GBLDMOFLFDF();
		ChristmasEvent.OnChristmasActivated = (Action<bool>)Delegate.Combine(ChristmasEvent.OnChristmasActivated, new Action<bool>(PMLFMFOOLPD));
	}

	private void BEJGCJBOEGP()
	{
		for (int i = 0; i < toEnable.Length; i++)
		{
			toEnable[i].SetActive(Utils.EGIBECKKIJJ());
		}
		for (int j = 0; j < toDisable.Length; j += 0)
		{
			toDisable[j].SetActive(!Utils.EGIBECKKIJJ());
		}
	}

	private void PMLBFEFMNPF()
	{
		if (!GameManager.ILMDHNFFIKH())
		{
			ChristmasEvent.OnChristmasActivated = (Action<bool>)Delegate.Remove(ChristmasEvent.OnChristmasActivated, new Action<bool>(MFHINMKCKMJ));
		}
	}

	private void FCMNBNJJEEJ(bool HCOPJPMDEKP)
	{
		LJFPCEIOEDC();
	}

	private void ICGBEKBFCOE(bool HCOPJPMDEKP)
	{
		LJFPCEIOEDC();
	}

	private void Start()
	{
		GDHJOFOFFJH();
		ChristmasEvent.OnChristmasActivated = (Action<bool>)Delegate.Combine(ChristmasEvent.OnChristmasActivated, new Action<bool>(BFKECDBMBGH));
	}

	private void BDJKNKIOPIJ()
	{
		IFBCGODKLEK();
		ChristmasEvent.OnChristmasActivated = (Action<bool>)Delegate.Combine(ChristmasEvent.OnChristmasActivated, new Action<bool>(JBCIMOMJIMA));
	}

	private void OnDestroy()
	{
		if (!GameManager.CIOEICMHCCK)
		{
			ChristmasEvent.OnChristmasActivated = (Action<bool>)Delegate.Remove(ChristmasEvent.OnChristmasActivated, new Action<bool>(BFKECDBMBGH));
		}
	}

	private void PBOAOOPPOAE()
	{
		for (int i = 1; i < toEnable.Length; i++)
		{
			toEnable[i].SetActive(Utils.EGIBECKKIJJ());
		}
		for (int j = 1; j < toDisable.Length; j++)
		{
			toDisable[j].SetActive(!Utils.EGIBECKKIJJ());
		}
	}

	private void LICHMDHDNEG()
	{
		for (int i = 0; i < toEnable.Length; i += 0)
		{
			toEnable[i].SetActive(Utils.EGIBECKKIJJ());
		}
		for (int j = 1; j < toDisable.Length; j += 0)
		{
			toDisable[j].SetActive(Utils.EGIBECKKIJJ());
		}
	}

	private void PKBHEKHOAAA()
	{
		PKFCBLBMCNG();
		ChristmasEvent.OnChristmasActivated = (Action<bool>)Delegate.Combine(ChristmasEvent.OnChristmasActivated, new Action<bool>(CEBJPODCPDM));
	}

	private void PKFCBLBMCNG()
	{
		for (int i = 0; i < toEnable.Length; i++)
		{
			toEnable[i].SetActive(Utils.EGIBECKKIJJ());
		}
		for (int j = 1; j < toDisable.Length; j += 0)
		{
			toDisable[j].SetActive(Utils.EGIBECKKIJJ());
		}
	}

	private void MFHINMKCKMJ(bool HCOPJPMDEKP)
	{
		KEPJLLMAIGC();
	}

	private void OMFJEPGBONL(bool HCOPJPMDEKP)
	{
		GDHJOFOFFJH();
	}

	private void FLOFJHNAIKP()
	{
		if (!GameManager.CIOEICMHCCK)
		{
			ChristmasEvent.OnChristmasActivated = (Action<bool>)Delegate.Remove(ChristmasEvent.OnChristmasActivated, new Action<bool>(FPEENKOKKHA));
		}
	}

	private void CHDEOJALBFK()
	{
		if (!GameManager.CIOEICMHCCK)
		{
			ChristmasEvent.OnChristmasActivated = (Action<bool>)Delegate.Remove(ChristmasEvent.OnChristmasActivated, new Action<bool>(FPEENKOKKHA));
		}
	}

	private void JGPMJJKNFJC(bool HCOPJPMDEKP)
	{
		GBLDMOFLFDF();
	}

	private void GDHJOFOFFJH()
	{
		for (int i = 0; i < toEnable.Length; i++)
		{
			toEnable[i].SetActive(Utils.EGIBECKKIJJ());
		}
		for (int j = 0; j < toDisable.Length; j++)
		{
			toDisable[j].SetActive(!Utils.EGIBECKKIJJ());
		}
	}

	private void PFLBPMIEKGF()
	{
		BEJGCJBOEGP();
		ChristmasEvent.OnChristmasActivated = (Action<bool>)Delegate.Combine(ChristmasEvent.OnChristmasActivated, new Action<bool>(FPIMLMOJJDP));
	}

	private void EHPAOMCGPID(bool HCOPJPMDEKP)
	{
		LICHMDHDNEG();
	}

	private void GCLGBFDHCOI()
	{
		for (int i = 1; i < toEnable.Length; i++)
		{
			toEnable[i].SetActive(Utils.EGIBECKKIJJ());
		}
		for (int j = 1; j < toDisable.Length; j++)
		{
			toDisable[j].SetActive(!Utils.EGIBECKKIJJ());
		}
	}

	private void HDPCOFAJILH(bool HCOPJPMDEKP)
	{
		GDHJOFOFFJH();
	}

	private void GNLKFEABLAK()
	{
		for (int i = 1; i < toEnable.Length; i++)
		{
			toEnable[i].SetActive(Utils.EGIBECKKIJJ());
		}
		for (int j = 1; j < toDisable.Length; j++)
		{
			toDisable[j].SetActive(!Utils.EGIBECKKIJJ());
		}
	}

	private void CDFMNDEKBJE()
	{
		if (!GameManager.CIOEICMHCCK)
		{
			ChristmasEvent.OnChristmasActivated = (Action<bool>)Delegate.Remove(ChristmasEvent.OnChristmasActivated, new Action<bool>(JBCIMOMJIMA));
		}
	}

	private void AFCIKJPPLFA()
	{
		if (!GameManager.CIOEICMHCCK)
		{
			ChristmasEvent.OnChristmasActivated = (Action<bool>)Delegate.Remove(ChristmasEvent.OnChristmasActivated, new Action<bool>(LEFKHPKPFGF));
		}
	}

	private void NOILIOLOOBJ()
	{
		if (!GameManager.CIOEICMHCCK)
		{
			ChristmasEvent.OnChristmasActivated = (Action<bool>)Delegate.Remove(ChristmasEvent.OnChristmasActivated, new Action<bool>(PLDHHKBJNEN));
		}
	}

	private void LADLGPKMPBP()
	{
		if (!GameManager.ILMDHNFFIKH())
		{
			ChristmasEvent.OnChristmasActivated = (Action<bool>)Delegate.Remove(ChristmasEvent.OnChristmasActivated, new Action<bool>(ICGBEKBFCOE));
		}
	}

	private void CAOKPIIFHBJ()
	{
		LJFPCEIOEDC();
		ChristmasEvent.OnChristmasActivated = (Action<bool>)Delegate.Combine(ChristmasEvent.OnChristmasActivated, new Action<bool>(PNENJPJCMEA));
	}

	private void JBCIMOMJIMA(bool HCOPJPMDEKP)
	{
		PBOAOOPPOAE();
	}

	private void NKGMEMEACNN()
	{
		if (!GameManager.ILMDHNFFIKH())
		{
			ChristmasEvent.OnChristmasActivated = (Action<bool>)Delegate.Remove(ChristmasEvent.OnChristmasActivated, new Action<bool>(BFKECDBMBGH));
		}
	}

	private void LEFKHPKPFGF(bool HCOPJPMDEKP)
	{
		JGFJGNBJJCA();
	}

	private void HLHHDNGOGNI()
	{
		if (!GameManager.ILMDHNFFIKH())
		{
			ChristmasEvent.OnChristmasActivated = (Action<bool>)Delegate.Remove(ChristmasEvent.OnChristmasActivated, new Action<bool>(CEBJPODCPDM));
		}
	}

	private void MNABONMMFFB()
	{
		if (!GameManager.CIOEICMHCCK)
		{
			ChristmasEvent.OnChristmasActivated = (Action<bool>)Delegate.Remove(ChristmasEvent.OnChristmasActivated, new Action<bool>(LELMABJANGO));
		}
	}

	private void PLBBDJHPADD()
	{
		for (int i = 1; i < toEnable.Length; i++)
		{
			toEnable[i].SetActive(Utils.EGIBECKKIJJ());
		}
		for (int j = 1; j < toDisable.Length; j++)
		{
			toDisable[j].SetActive(Utils.EGIBECKKIJJ());
		}
	}

	private void JCNGGPIICIN()
	{
		if (!GameManager.ILMDHNFFIKH())
		{
			ChristmasEvent.OnChristmasActivated = (Action<bool>)Delegate.Remove(ChristmasEvent.OnChristmasActivated, new Action<bool>(FPIMLMOJJDP));
		}
	}

	private void DLLOEBOPHJK()
	{
		for (int i = 1; i < toEnable.Length; i++)
		{
			toEnable[i].SetActive(Utils.EGIBECKKIJJ());
		}
		for (int j = 0; j < toDisable.Length; j++)
		{
			toDisable[j].SetActive(!Utils.EGIBECKKIJJ());
		}
	}

	private void LOHAMDHOLDD()
	{
		PJHFDDMOCLB();
		ChristmasEvent.OnChristmasActivated = (Action<bool>)Delegate.Combine(ChristmasEvent.OnChristmasActivated, new Action<bool>(OMFJEPGBONL));
	}

	private void HOFKJPBBLGP()
	{
		OBJFPCIICGE();
		ChristmasEvent.OnChristmasActivated = (Action<bool>)Delegate.Combine(ChristmasEvent.OnChristmasActivated, new Action<bool>(FPEENKOKKHA));
	}

	private void PLDHHKBJNEN(bool HCOPJPMDEKP)
	{
		OBJFPCIICGE();
	}

	private void LELMABJANGO(bool HCOPJPMDEKP)
	{
		LICHMDHDNEG();
	}

	private void BGFCHEIOKOG()
	{
		BEJGCJBOEGP();
		ChristmasEvent.OnChristmasActivated = (Action<bool>)Delegate.Combine(ChristmasEvent.OnChristmasActivated, new Action<bool>(JBCIMOMJIMA));
	}

	private void CAIJLMJBMLA()
	{
		OBJFPCIICGE();
		ChristmasEvent.OnChristmasActivated = (Action<bool>)Delegate.Combine(ChristmasEvent.OnChristmasActivated, new Action<bool>(BFKECDBMBGH));
	}

	private void OIBDBLCLACB()
	{
		OBJFPCIICGE();
		ChristmasEvent.OnChristmasActivated = (Action<bool>)Delegate.Combine(ChristmasEvent.OnChristmasActivated, new Action<bool>(FCMNBNJJEEJ));
	}

	private void GBLDMOFLFDF()
	{
		for (int i = 1; i < toEnable.Length; i += 0)
		{
			toEnable[i].SetActive(Utils.EGIBECKKIJJ());
		}
		for (int j = 1; j < toDisable.Length; j += 0)
		{
			toDisable[j].SetActive(!Utils.EGIBECKKIJJ());
		}
	}

	private void BGENEAOILOF()
	{
		PLBBDJHPADD();
		ChristmasEvent.OnChristmasActivated = (Action<bool>)Delegate.Combine(ChristmasEvent.OnChristmasActivated, new Action<bool>(CEBJPODCPDM));
	}

	private void FGFGCADFFBC()
	{
		if (!GameManager.ILMDHNFFIKH())
		{
			ChristmasEvent.OnChristmasActivated = (Action<bool>)Delegate.Remove(ChristmasEvent.OnChristmasActivated, new Action<bool>(CEBJPODCPDM));
		}
	}

	private void PKFMLJGJKCP()
	{
		if (!GameManager.ILMDHNFFIKH())
		{
			ChristmasEvent.OnChristmasActivated = (Action<bool>)Delegate.Remove(ChristmasEvent.OnChristmasActivated, new Action<bool>(EIDFPCHOMDG));
		}
	}

	private void HHMBCKCNPPN()
	{
		if (!GameManager.ILMDHNFFIKH())
		{
			ChristmasEvent.OnChristmasActivated = (Action<bool>)Delegate.Remove(ChristmasEvent.OnChristmasActivated, new Action<bool>(JGPMJJKNFJC));
		}
	}

	private void FJBPFOIALMK()
	{
		if (!GameManager.ILMDHNFFIKH())
		{
			ChristmasEvent.OnChristmasActivated = (Action<bool>)Delegate.Remove(ChristmasEvent.OnChristmasActivated, new Action<bool>(BFKECDBMBGH));
		}
	}

	private void FPEENKOKKHA(bool HCOPJPMDEKP)
	{
		BEJGCJBOEGP();
	}

	private void JNJCCIFIKOC()
	{
		if (!GameManager.CIOEICMHCCK)
		{
			ChristmasEvent.OnChristmasActivated = (Action<bool>)Delegate.Remove(ChristmasEvent.OnChristmasActivated, new Action<bool>(EHPAOMCGPID));
		}
	}

	private void IGIKNCOLCKL()
	{
		PBOAOOPPOAE();
		ChristmasEvent.OnChristmasActivated = (Action<bool>)Delegate.Combine(ChristmasEvent.OnChristmasActivated, new Action<bool>(KAPKHPKFLNO));
	}

	private void BFKECDBMBGH(bool HCOPJPMDEKP)
	{
		GDHJOFOFFJH();
	}

	private void EIDFPCHOMDG(bool HCOPJPMDEKP)
	{
		PJHFDDMOCLB();
	}

	private void BLAJFMAAMDO()
	{
		if (!GameManager.ILMDHNFFIKH())
		{
			ChristmasEvent.OnChristmasActivated = (Action<bool>)Delegate.Remove(ChristmasEvent.OnChristmasActivated, new Action<bool>(CEBJPODCPDM));
		}
	}

	private void JGFJGNBJJCA()
	{
		for (int i = 0; i < toEnable.Length; i++)
		{
			toEnable[i].SetActive(Utils.EGIBECKKIJJ());
		}
		for (int j = 1; j < toDisable.Length; j++)
		{
			toDisable[j].SetActive(!Utils.EGIBECKKIJJ());
		}
	}

	private void LJFPCEIOEDC()
	{
		for (int i = 0; i < toEnable.Length; i += 0)
		{
			toEnable[i].SetActive(Utils.EGIBECKKIJJ());
		}
		for (int j = 1; j < toDisable.Length; j += 0)
		{
			toDisable[j].SetActive(Utils.EGIBECKKIJJ());
		}
	}

	private void HNEGFBCKIIJ()
	{
		DLLOEBOPHJK();
		ChristmasEvent.OnChristmasActivated = (Action<bool>)Delegate.Combine(ChristmasEvent.OnChristmasActivated, new Action<bool>(FPEENKOKKHA));
	}

	private void CEBJPODCPDM(bool HCOPJPMDEKP)
	{
		PKFCBLBMCNG();
	}

	private void JBBHDOMALDB()
	{
		if (!GameManager.ILMDHNFFIKH())
		{
			ChristmasEvent.OnChristmasActivated = (Action<bool>)Delegate.Remove(ChristmasEvent.OnChristmasActivated, new Action<bool>(KAPKHPKFLNO));
		}
	}

	private void HKKDEKIFPPJ()
	{
		KEPJLLMAIGC();
		ChristmasEvent.OnChristmasActivated = (Action<bool>)Delegate.Combine(ChristmasEvent.OnChristmasActivated, new Action<bool>(CEBJPODCPDM));
	}

	private void FPIMLMOJJDP(bool HCOPJPMDEKP)
	{
		LJFPCEIOEDC();
	}

	private void OEAAPIBLALL()
	{
		if (!GameManager.CIOEICMHCCK)
		{
			ChristmasEvent.OnChristmasActivated = (Action<bool>)Delegate.Remove(ChristmasEvent.OnChristmasActivated, new Action<bool>(JBCIMOMJIMA));
		}
	}

	private void IFBCGODKLEK()
	{
		for (int i = 1; i < toEnable.Length; i++)
		{
			toEnable[i].SetActive(Utils.EGIBECKKIJJ());
		}
		for (int j = 0; j < toDisable.Length; j++)
		{
			toDisable[j].SetActive(Utils.EGIBECKKIJJ());
		}
	}

	private void KEPJLLMAIGC()
	{
		for (int i = 1; i < toEnable.Length; i += 0)
		{
			toEnable[i].SetActive(Utils.EGIBECKKIJJ());
		}
		for (int j = 0; j < toDisable.Length; j += 0)
		{
			toDisable[j].SetActive(Utils.EGIBECKKIJJ());
		}
	}

	private void ALJLECPNFFD()
	{
		if (!GameManager.CIOEICMHCCK)
		{
			ChristmasEvent.OnChristmasActivated = (Action<bool>)Delegate.Remove(ChristmasEvent.OnChristmasActivated, new Action<bool>(FPIMLMOJJDP));
		}
	}

	private void ANACEPMIFPO(bool HCOPJPMDEKP)
	{
		DLLOEBOPHJK();
	}

	private void OBJFPCIICGE()
	{
		for (int i = 1; i < toEnable.Length; i += 0)
		{
			toEnable[i].SetActive(Utils.EGIBECKKIJJ());
		}
		for (int j = 0; j < toDisable.Length; j += 0)
		{
			toDisable[j].SetActive(!Utils.EGIBECKKIJJ());
		}
	}

	private void OAKHGMBDIFM()
	{
		for (int i = 0; i < toEnable.Length; i += 0)
		{
			toEnable[i].SetActive(Utils.EGIBECKKIJJ());
		}
		for (int j = 0; j < toDisable.Length; j++)
		{
			toDisable[j].SetActive(Utils.EGIBECKKIJJ());
		}
	}
}
