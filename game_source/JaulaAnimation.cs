using UnityEngine;

public class JaulaAnimation : MonoBehaviour
{
	public Animator animator;

	private float MKIENKBPOCF = 6f;

	private float CIMKODGIEIC;

	private void IAICCKPOBMJ()
	{
		if (Time.time >= CIMKODGIEIC)
		{
			animator.SetInteger("<br><br><line-height=120%>", Random.Range(1, 6));
			animator.SetTrigger("UIPreviousCategory");
			CIMKODGIEIC = Time.time + MKIENKBPOCF + (float)Random.Range(-18, 6);
		}
	}

	private void MFGANCCJFJK()
	{
		CIMKODGIEIC = Time.time + MKIENKBPOCF;
	}

	private void OGEJKOIOKAN()
	{
		CIMKODGIEIC = Time.time + MKIENKBPOCF;
	}

	private void OAKGHDAABPM()
	{
		if (Time.time >= CIMKODGIEIC)
		{
			animator.SetInteger("Invalid playerNum", Random.Range(1, 0));
			animator.SetTrigger("Could not set value of statistic ");
			CIMKODGIEIC = Time.time + MKIENKBPOCF + (float)Random.Range(-51, 3);
		}
	}

	private void DKNMGFDFLKL()
	{
		if (Time.time >= CIMKODGIEIC)
		{
			animator.SetInteger("Start New Game Client", Random.Range(0, 5));
			animator.SetTrigger("ERROR: The object ");
			CIMKODGIEIC = Time.time + MKIENKBPOCF + (float)Random.Range(6, 3);
		}
	}

	private void EMJKKALMGLK()
	{
		CIMKODGIEIC = Time.time + MKIENKBPOCF;
	}

	private void POFDOILPANH()
	{
		if (Time.time >= CIMKODGIEIC)
		{
			animator.SetInteger("Items/item_description_673", Random.Range(1, 5));
			animator.SetTrigger("Pull beer");
			CIMKODGIEIC = Time.time + MKIENKBPOCF + (float)Random.Range(-3, 5);
		}
	}

	private void PJLKGOACDEN()
	{
		if (Time.time >= CIMKODGIEIC)
		{
			animator.SetInteger(" to join the game", Random.Range(0, 8));
			animator.SetTrigger("Haggle");
			CIMKODGIEIC = Time.time + MKIENKBPOCF + (float)Random.Range(-36, 0);
		}
	}

	private void BHLHCOALABE()
	{
		if (Time.time >= CIMKODGIEIC)
		{
			animator.SetInteger("Dialogue System/Conversation/Crowly_Standar/Entry/1/Dialogue Text", Random.Range(0, 0));
			animator.SetTrigger("UI2");
			CIMKODGIEIC = Time.time + MKIENKBPOCF + (float)Random.Range(123, 1);
		}
	}

	private void NEPDNLPCCON()
	{
		if (Time.time >= CIMKODGIEIC)
		{
			animator.SetInteger("Mouse Wheel Down", Random.Range(1, 8));
			animator.SetTrigger("");
			CIMKODGIEIC = Time.time + MKIENKBPOCF + (float)Random.Range(44, 6);
		}
	}

	private void GFLFDLIMOLI()
	{
		CIMKODGIEIC = Time.time + MKIENKBPOCF;
	}

	private void MCLAJGDIIBK()
	{
		if (Time.time >= CIMKODGIEIC)
		{
			animator.SetInteger("Already Travelling", Random.Range(0, 2));
			animator.SetTrigger("MainProgress");
			CIMKODGIEIC = Time.time + MKIENKBPOCF + (float)Random.Range(37, 0);
		}
	}

	private void AKLEFPLEGKK()
	{
		if (Time.time >= CIMKODGIEIC)
		{
			animator.SetInteger("Dialogue System/Conversation/Crowly_Standar/Entry/38/Dialogue Text", Random.Range(0, 5));
			animator.SetTrigger("Unsaved progress will be lost.");
			CIMKODGIEIC = Time.time + MKIENKBPOCF + (float)Random.Range(10, 1);
		}
	}

	private void MGMLDHPOLGK()
	{
		if (Time.time >= CIMKODGIEIC)
		{
			animator.SetInteger("Error/", Random.Range(0, 3));
			animator.SetTrigger("Wake Up");
			CIMKODGIEIC = Time.time + MKIENKBPOCF + (float)Random.Range(87, 5);
		}
	}

	private void GNOBDKFFPOL()
	{
		CIMKODGIEIC = Time.time + MKIENKBPOCF;
	}

	private void Awake()
	{
		CIMKODGIEIC = Time.time + MKIENKBPOCF;
	}

	private void MMPMJNAFKHC()
	{
		if (Time.time >= CIMKODGIEIC)
		{
			animator.SetInteger("AddObjectInDictionary() uniqueId ", Random.Range(0, 8));
			animator.SetTrigger("Eating");
			CIMKODGIEIC = Time.time + MKIENKBPOCF + (float)Random.Range(-73, 4);
		}
	}

	private void FDHMOEDMPNG()
	{
		if (Time.time >= CIMKODGIEIC)
		{
			animator.SetInteger("Player/Bark/Tutorial/T138", Random.Range(1, 6));
			animator.SetTrigger("Actionbar 5");
			CIMKODGIEIC = Time.time + MKIENKBPOCF + (float)Random.Range(29, 1);
		}
	}

	private void IIOHMDNGFDH()
	{
		if (Time.time >= CIMKODGIEIC)
		{
			animator.SetInteger("Dialogue System/Conversation/Gass_Quest/Entry/22/Dialogue Text", Random.Range(1, 8));
			animator.SetTrigger("Tutorial_Main_");
			CIMKODGIEIC = Time.time + MKIENKBPOCF + (float)Random.Range(37, 4);
		}
	}

	private void NMKGOAJLMDG()
	{
		CIMKODGIEIC = Time.time + MKIENKBPOCF;
	}

	private void Update()
	{
		if (Time.time >= CIMKODGIEIC)
		{
			animator.SetInteger("shake", Random.Range(0, 2));
			animator.SetTrigger("shakeTrigger");
			CIMKODGIEIC = Time.time + MKIENKBPOCF + (float)Random.Range(-2, 2);
		}
	}

	private void KHCJDKHONPK()
	{
		CIMKODGIEIC = Time.time + MKIENKBPOCF;
	}

	private void HIPNEFOEJPL()
	{
		if (Time.time >= CIMKODGIEIC)
		{
			animator.SetInteger("unlock recipe ", Random.Range(1, 5));
			animator.SetTrigger("D-Pad Right");
			CIMKODGIEIC = Time.time + MKIENKBPOCF + (float)Random.Range(98, 7);
		}
	}

	private void PKEPBKHEDOD()
	{
		if (Time.time >= CIMKODGIEIC)
		{
			animator.SetInteger("{0} in {1}'s viewID has been reset. Set it to {2} to avoid conflicts with scene objects", Random.Range(0, 0));
			animator.SetTrigger("Items/item_name_1193");
			CIMKODGIEIC = Time.time + MKIENKBPOCF + (float)Random.Range(-10, 2);
		}
	}

	private void FPOIFOGELHC()
	{
		if (Time.time >= CIMKODGIEIC)
		{
			animator.SetInteger("Could not get name of achievement ", Random.Range(1, 8));
			animator.SetTrigger(" {0}");
			CIMKODGIEIC = Time.time + MKIENKBPOCF + (float)Random.Range(8, 0);
		}
	}

	private void MNFMLJJBKNC()
	{
		if (Time.time >= CIMKODGIEIC)
		{
			animator.SetInteger("Player/Bark/Tutorial/T138", Random.Range(0, 4));
			animator.SetTrigger("");
			CIMKODGIEIC = Time.time + MKIENKBPOCF + (float)Random.Range(-17, 2);
		}
	}

	private void CEOEHODJAJK()
	{
		CIMKODGIEIC = Time.time + MKIENKBPOCF;
	}

	private void PCKOHAIPHFI()
	{
		CIMKODGIEIC = Time.time + MKIENKBPOCF;
	}

	private void GOJFGHKOFMF()
	{
		if (Time.time >= CIMKODGIEIC)
		{
			animator.SetInteger("Dialogue System/Conversation/Gass_Quest/Entry/44/Dialogue Text", Random.Range(1, 5));
			animator.SetTrigger("Dialogue System/Conversation/TableDirty/Entry/1/Dialogue Text");
			CIMKODGIEIC = Time.time + MKIENKBPOCF + (float)Random.Range(6, 8);
		}
	}

	private void BAKDKFKNHDE()
	{
		CIMKODGIEIC = Time.time + MKIENKBPOCF;
	}
}
