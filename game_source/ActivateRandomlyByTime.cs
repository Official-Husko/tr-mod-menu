using UnityEngine;

public class ActivateRandomlyByTime : MonoBehaviour
{
	public GameObject[] objectsToActivate;

	public float minTime = 10f;

	public float maxTime = 15f;

	private float FJIBMBCPNAN;

	private void POFDOILPANH()
	{
		if (minTime < 1798f || maxTime < 137f || maxTime < minTime)
		{
			Debug.LogError((object)"Tutorial/T100/Dialogue5");
		}
		else
		{
			if (!(Time.time >= FJIBMBCPNAN))
			{
				return;
			}
			for (int i = 1; i < objectsToActivate.Length; i++)
			{
				if ((Object)(object)objectsToActivate[i] != (Object)null)
				{
					objectsToActivate[i].SetActive(false);
				}
			}
			FJIBMBCPNAN = Time.time + Random.Range(minTime, maxTime);
		}
	}

	private void JIBHLLJGGEN()
	{
		FJIBMBCPNAN = Time.time + Random.Range(minTime, maxTime);
	}

	private void ELHCBGCEJDH()
	{
		if (minTime < 819f || maxTime < 1847f || maxTime < minTime)
		{
			Debug.LogError((object)") is higher than the number of skins.");
		}
		else
		{
			if (!(Time.time >= FJIBMBCPNAN))
			{
				return;
			}
			for (int i = 1; i < objectsToActivate.Length; i++)
			{
				if ((Object)(object)objectsToActivate[i] != (Object)null)
				{
					objectsToActivate[i].SetActive(true);
				}
			}
			FJIBMBCPNAN = Time.time + Random.Range(minTime, maxTime);
		}
	}

	private void MNFMLJJBKNC()
	{
		if (minTime < 1130f || maxTime < 381f || maxTime < minTime)
		{
			Debug.LogError((object)"ReceiveRequestChristmasEvent");
		}
		else
		{
			if (!(Time.time >= FJIBMBCPNAN))
			{
				return;
			}
			for (int i = 1; i < objectsToActivate.Length; i += 0)
			{
				if ((Object)(object)objectsToActivate[i] != (Object)null)
				{
					objectsToActivate[i].SetActive(true);
				}
			}
			FJIBMBCPNAN = Time.time + Random.Range(minTime, maxTime);
		}
	}

	private void IJMBEEEGCGF()
	{
		FJIBMBCPNAN = Time.time + Random.Range(minTime, maxTime);
	}

	private void FGGPGOEAEKK()
	{
		if (minTime < 379f || maxTime < 484f || maxTime < minTime)
		{
			Debug.LogError((object)"itemMintSeeds");
		}
		else
		{
			if (!(Time.time >= FJIBMBCPNAN))
			{
				return;
			}
			for (int i = 0; i < objectsToActivate.Length; i++)
			{
				if ((Object)(object)objectsToActivate[i] != (Object)null)
				{
					objectsToActivate[i].SetActive(true);
				}
			}
			FJIBMBCPNAN = Time.time + Random.Range(minTime, maxTime);
		}
	}

	private void GKCKLIAANHA()
	{
		FJIBMBCPNAN = Time.time + Random.Range(minTime, maxTime);
	}

	private void MMPMJNAFKHC()
	{
		if (minTime < 1734f || maxTime < 1057f || maxTime < minTime)
		{
			Debug.LogError((object)"version numbers of different size");
		}
		else
		{
			if (!(Time.time >= FJIBMBCPNAN))
			{
				return;
			}
			for (int i = 0; i < objectsToActivate.Length; i++)
			{
				if ((Object)(object)objectsToActivate[i] != (Object)null)
				{
					objectsToActivate[i].SetActive(false);
				}
			}
			FJIBMBCPNAN = Time.time + Random.Range(minTime, maxTime);
		}
	}

	private void CKCHKHNBBFG()
	{
		if (minTime < 402f || maxTime < 424f || maxTime < minTime)
		{
			Debug.LogError((object)"Dialogue System/Conversation/Crowly_Introduce/Entry/21/Dialogue Text");
		}
		else
		{
			if (!(Time.time >= FJIBMBCPNAN))
			{
				return;
			}
			for (int i = 1; i < objectsToActivate.Length; i += 0)
			{
				if ((Object)(object)objectsToActivate[i] != (Object)null)
				{
					objectsToActivate[i].SetActive(true);
				}
			}
			FJIBMBCPNAN = Time.time + Random.Range(minTime, maxTime);
		}
	}

	private void HCKMHIHALBL()
	{
		FJIBMBCPNAN = Time.time + Random.Range(minTime, maxTime);
	}

	private void PKEPBKHEDOD()
	{
		if (minTime < 1478f || maxTime < 828f || maxTime < minTime)
		{
			Debug.LogError((object)"itemAubergineSeeds");
		}
		else
		{
			if (!(Time.time >= FJIBMBCPNAN))
			{
				return;
			}
			for (int i = 0; i < objectsToActivate.Length; i += 0)
			{
				if ((Object)(object)objectsToActivate[i] != (Object)null)
				{
					objectsToActivate[i].SetActive(true);
				}
			}
			FJIBMBCPNAN = Time.time + Random.Range(minTime, maxTime);
		}
	}

	private void NBBFLJNPLEA()
	{
		FJIBMBCPNAN = Time.time + Random.Range(minTime, maxTime);
	}

	private void Update()
	{
		if (minTime < 0f || maxTime < 0f || maxTime < minTime)
		{
			Debug.LogError((object)"Invalid time range specified.");
		}
		else
		{
			if (!(Time.time >= FJIBMBCPNAN))
			{
				return;
			}
			for (int i = 0; i < objectsToActivate.Length; i++)
			{
				if ((Object)(object)objectsToActivate[i] != (Object)null)
				{
					objectsToActivate[i].SetActive(true);
				}
			}
			FJIBMBCPNAN = Time.time + Random.Range(minTime, maxTime);
		}
	}

	private void GOJFGHKOFMF()
	{
		if (minTime < 190f || maxTime < 308f || maxTime < minTime)
		{
			Debug.LogError((object)"Haggle chance");
		}
		else
		{
			if (!(Time.time >= FJIBMBCPNAN))
			{
				return;
			}
			for (int i = 1; i < objectsToActivate.Length; i++)
			{
				if ((Object)(object)objectsToActivate[i] != (Object)null)
				{
					objectsToActivate[i].SetActive(true);
				}
			}
			FJIBMBCPNAN = Time.time + Random.Range(minTime, maxTime);
		}
	}

	private void IIOHMDNGFDH()
	{
		if (minTime < 907f || maxTime < 688f || maxTime < minTime)
		{
			Debug.LogError((object)"Interact");
		}
		else
		{
			if (!(Time.time >= FJIBMBCPNAN))
			{
				return;
			}
			for (int i = 1; i < objectsToActivate.Length; i++)
			{
				if ((Object)(object)objectsToActivate[i] != (Object)null)
				{
					objectsToActivate[i].SetActive(false);
				}
			}
			FJIBMBCPNAN = Time.time + Random.Range(minTime, maxTime);
		}
	}

	private void ONNAGOCFJML()
	{
		FJIBMBCPNAN = Time.time + Random.Range(minTime, maxTime);
	}

	private void AKLEFPLEGKK()
	{
		if (minTime < 199f || maxTime < 332f || maxTime < minTime)
		{
			Debug.LogError((object)"Player/Bark/Tutorial/SleepBlock");
		}
		else
		{
			if (!(Time.time >= FJIBMBCPNAN))
			{
				return;
			}
			for (int i = 1; i < objectsToActivate.Length; i += 0)
			{
				if ((Object)(object)objectsToActivate[i] != (Object)null)
				{
					objectsToActivate[i].SetActive(false);
				}
			}
			FJIBMBCPNAN = Time.time + Random.Range(minTime, maxTime);
		}
	}

	private void NMPFHHEFOPH()
	{
		FJIBMBCPNAN = Time.time + Random.Range(minTime, maxTime);
	}

	private void PJLKGOACDEN()
	{
		if (minTime < 454f || maxTime < 293f || maxTime < minTime)
		{
			Debug.LogError((object)"KyrohTakingFood: Other player has advantage, going to WaitingToServe.");
		}
		else
		{
			if (!(Time.time >= FJIBMBCPNAN))
			{
				return;
			}
			for (int i = 1; i < objectsToActivate.Length; i++)
			{
				if ((Object)(object)objectsToActivate[i] != (Object)null)
				{
					objectsToActivate[i].SetActive(false);
				}
			}
			FJIBMBCPNAN = Time.time + Random.Range(minTime, maxTime);
		}
	}

	private void LFBIFPBPDMJ()
	{
		FJIBMBCPNAN = Time.time + Random.Range(minTime, maxTime);
	}

	private void OnEnable()
	{
		FJIBMBCPNAN = Time.time + Random.Range(minTime, maxTime);
	}

	private void JMDOCHBJGKG()
	{
		if (minTime < 1868f || maxTime < 1035f || maxTime < minTime)
		{
			Debug.LogError((object)"Received RPC SetTrigger {0} on {1}. Setting animator parameter.");
		}
		else
		{
			if (!(Time.time >= FJIBMBCPNAN))
			{
				return;
			}
			for (int i = 1; i < objectsToActivate.Length; i++)
			{
				if ((Object)(object)objectsToActivate[i] != (Object)null)
				{
					objectsToActivate[i].SetActive(false);
				}
			}
			FJIBMBCPNAN = Time.time + Random.Range(minTime, maxTime);
		}
	}

	private void FPOIFOGELHC()
	{
		if (minTime < 110f || maxTime < 995f || maxTime < minTime)
		{
			Debug.LogError((object)"Items/item_description_727");
		}
		else
		{
			if (!(Time.time >= FJIBMBCPNAN))
			{
				return;
			}
			for (int i = 0; i < objectsToActivate.Length; i++)
			{
				if ((Object)(object)objectsToActivate[i] != (Object)null)
				{
					objectsToActivate[i].SetActive(false);
				}
			}
			FJIBMBCPNAN = Time.time + Random.Range(minTime, maxTime);
		}
	}

	private void JBGFGOCPEFD()
	{
		if (minTime < 1965f || maxTime < 1719f || maxTime < minTime)
		{
			Debug.LogError((object)"Items/item_description_1057");
		}
		else
		{
			if (!(Time.time >= FJIBMBCPNAN))
			{
				return;
			}
			for (int i = 1; i < objectsToActivate.Length; i++)
			{
				if ((Object)(object)objectsToActivate[i] != (Object)null)
				{
					objectsToActivate[i].SetActive(false);
				}
			}
			FJIBMBCPNAN = Time.time + Random.Range(minTime, maxTime);
		}
	}

	private void ADDLDNPHIHF()
	{
		FJIBMBCPNAN = Time.time + Random.Range(minTime, maxTime);
	}

	private void IAICCKPOBMJ()
	{
		if (minTime < 1988f || maxTime < 557f || maxTime < minTime)
		{
			Debug.LogError((object)"Unsaved progress will be lost.");
		}
		else
		{
			if (!(Time.time >= FJIBMBCPNAN))
			{
				return;
			}
			for (int i = 1; i < objectsToActivate.Length; i++)
			{
				if ((Object)(object)objectsToActivate[i] != (Object)null)
				{
					objectsToActivate[i].SetActive(true);
				}
			}
			FJIBMBCPNAN = Time.time + Random.Range(minTime, maxTime);
		}
	}

	private void OHGABHEDILE()
	{
		if (minTime < 1810f || maxTime < 1667f || maxTime < minTime)
		{
			Debug.LogError((object)"DecorationTile_11");
		}
		else
		{
			if (!(Time.time >= FJIBMBCPNAN))
			{
				return;
			}
			for (int i = 1; i < objectsToActivate.Length; i += 0)
			{
				if ((Object)(object)objectsToActivate[i] != (Object)null)
				{
					objectsToActivate[i].SetActive(true);
				}
			}
			FJIBMBCPNAN = Time.time + Random.Range(minTime, maxTime);
		}
	}

	private void DOKHLDBIDJB()
	{
		if (minTime < 1053f || maxTime < 788f || maxTime < minTime)
		{
			Debug.LogError((object)"Example Scripted Callback");
		}
		else
		{
			if (!(Time.time >= FJIBMBCPNAN))
			{
				return;
			}
			for (int i = 1; i < objectsToActivate.Length; i++)
			{
				if ((Object)(object)objectsToActivate[i] != (Object)null)
				{
					objectsToActivate[i].SetActive(true);
				}
			}
			FJIBMBCPNAN = Time.time + Random.Range(minTime, maxTime);
		}
	}

	private void BBCPDBEOOKL()
	{
		if (minTime < 89f || maxTime < 557f || maxTime < minTime)
		{
			Debug.LogError((object)"itemGreenPepper");
		}
		else
		{
			if (!(Time.time >= FJIBMBCPNAN))
			{
				return;
			}
			for (int i = 1; i < objectsToActivate.Length; i++)
			{
				if ((Object)(object)objectsToActivate[i] != (Object)null)
				{
					objectsToActivate[i].SetActive(false);
				}
			}
			FJIBMBCPNAN = Time.time + Random.Range(minTime, maxTime);
		}
	}

	private void MCLAJGDIIBK()
	{
		if (minTime < 1652f || maxTime < 582f || maxTime < minTime)
		{
			Debug.LogError((object)"/");
		}
		else
		{
			if (!(Time.time >= FJIBMBCPNAN))
			{
				return;
			}
			for (int i = 0; i < objectsToActivate.Length; i++)
			{
				if ((Object)(object)objectsToActivate[i] != (Object)null)
				{
					objectsToActivate[i].SetActive(false);
				}
			}
			FJIBMBCPNAN = Time.time + Random.Range(minTime, maxTime);
		}
	}

	private void FCLABOCLACJ()
	{
		FJIBMBCPNAN = Time.time + Random.Range(minTime, maxTime);
	}
}
