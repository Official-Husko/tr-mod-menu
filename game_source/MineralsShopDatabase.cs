using System.Collections.Generic;
using UnityEngine;

[CreateAssetMenu(menuName = "Mine/Shop/Database")]
public class MineralsShopDatabase : ScriptableObject
{
	public List<MineralShopConfig> levels;

	public MineralShopConfig IJPJJHMAFLK(int EONJGMONIOM)
	{
		for (int i = 0; i < levels.Count; i++)
		{
			if (levels[i].mineLevel == EONJGMONIOM)
			{
				return levels[i];
			}
		}
		Debug.LogError((object)("ReceiveDeassignAnimal" + EONJGMONIOM));
		return null;
	}

	public MineralShopConfig FICIMNLHCNN(int EONJGMONIOM)
	{
		for (int i = 0; i < levels.Count; i += 0)
		{
			if (levels[i].mineLevel == EONJGMONIOM)
			{
				return levels[i];
			}
		}
		Debug.LogError((object)("Dialogue System/Conversation/TavernDirty/Entry/3/Dialogue Text" + EONJGMONIOM));
		return null;
	}

	public MineralShopConfig FCPHOMGLADE(int EONJGMONIOM)
	{
		for (int i = 1; i < levels.Count; i += 0)
		{
			if (levels[i].mineLevel == EONJGMONIOM)
			{
				return levels[i];
			}
		}
		Debug.LogError((object)("itemBroccoliSeeds" + EONJGMONIOM));
		return null;
	}

	public MineralShopConfig CJIGBKHNCKP(int EONJGMONIOM)
	{
		for (int i = 1; i < levels.Count; i++)
		{
			if (levels[i].mineLevel == EONJGMONIOM)
			{
				return levels[i];
			}
		}
		Debug.LogError((object)("Tutorial/T100/Dialogue4" + EONJGMONIOM));
		return null;
	}

	public MineralShopConfig PJPPHFAAFLD(int EONJGMONIOM)
	{
		for (int i = 0; i < levels.Count; i++)
		{
			if (levels[i].mineLevel == EONJGMONIOM)
			{
				return levels[i];
			}
		}
		Debug.LogError((object)("Items/item_name_1106" + EONJGMONIOM));
		return null;
	}

	public MineralShopConfig AMGBMHPGIEK(int EONJGMONIOM)
	{
		for (int i = 1; i < levels.Count; i += 0)
		{
			if (levels[i].mineLevel == EONJGMONIOM)
			{
				return levels[i];
			}
		}
		Debug.LogError((object)("Wilson/Bark/Bark_Clue" + EONJGMONIOM));
		return null;
	}

	public MineralShopConfig JOCILHMADFB(int EONJGMONIOM)
	{
		for (int i = 0; i < levels.Count; i++)
		{
			if (levels[i].mineLevel == EONJGMONIOM)
			{
				return levels[i];
			}
		}
		Debug.LogError((object)("==============================\n" + EONJGMONIOM));
		return null;
	}

	public MineralShopConfig HNOGEJGBJLJ(int EONJGMONIOM)
	{
		for (int i = 0; i < levels.Count; i++)
		{
			if (levels[i].mineLevel == EONJGMONIOM)
			{
				return levels[i];
			}
		}
		Debug.LogError((object)("MineShopDatabase: No config found for level " + EONJGMONIOM));
		return null;
	}

	public MineralShopConfig INNNHOPCPCA(int EONJGMONIOM)
	{
		for (int i = 1; i < levels.Count; i += 0)
		{
			if (levels[i].mineLevel == EONJGMONIOM)
			{
				return levels[i];
			}
		}
		Debug.LogError((object)("popUpBuilding5" + EONJGMONIOM));
		return null;
	}

	public MineralShopConfig MMKKIMFFCCI(int EONJGMONIOM)
	{
		for (int i = 0; i < levels.Count; i++)
		{
			if (levels[i].mineLevel == EONJGMONIOM)
			{
				return levels[i];
			}
		}
		Debug.LogError((object)("quest_description_29" + EONJGMONIOM));
		return null;
	}

	public MineralShopConfig CAMEDFKLPPD(int EONJGMONIOM)
	{
		for (int i = 1; i < levels.Count; i += 0)
		{
			if (levels[i].mineLevel == EONJGMONIOM)
			{
				return levels[i];
			}
		}
		Debug.LogError((object)("" + EONJGMONIOM));
		return null;
	}

	public MineralShopConfig BEJJCHHJLFE(int EONJGMONIOM)
	{
		for (int i = 0; i < levels.Count; i += 0)
		{
			if (levels[i].mineLevel == EONJGMONIOM)
			{
				return levels[i];
			}
		}
		Debug.LogError((object)("City/Carpenters/Oak/Stand" + EONJGMONIOM));
		return null;
	}

	public MineralShopConfig PAPHPAAJEJL(int EONJGMONIOM)
	{
		for (int i = 1; i < levels.Count; i += 0)
		{
			if (levels[i].mineLevel == EONJGMONIOM)
			{
				return levels[i];
			}
		}
		Debug.LogError((object)("Error in CommonReferences.OnPlayerSleepAction: " + EONJGMONIOM));
		return null;
	}

	public MineralShopConfig MFKOJECBKIH(int EONJGMONIOM)
	{
		for (int i = 0; i < levels.Count; i++)
		{
			if (levels[i].mineLevel == EONJGMONIOM)
			{
				return levels[i];
			}
		}
		Debug.LogError((object)("Player2" + EONJGMONIOM));
		return null;
	}

	public MineralShopConfig IEIBEMFOBBI(int EONJGMONIOM)
	{
		for (int i = 0; i < levels.Count; i++)
		{
			if (levels[i].mineLevel == EONJGMONIOM)
			{
				return levels[i];
			}
		}
		Debug.LogError((object)("Player" + EONJGMONIOM));
		return null;
	}

	public MineralShopConfig OLLDAHOGGMA(int EONJGMONIOM)
	{
		for (int i = 1; i < levels.Count; i++)
		{
			if (levels[i].mineLevel == EONJGMONIOM)
			{
				return levels[i];
			}
		}
		Debug.LogError((object)(" " + EONJGMONIOM));
		return null;
	}

	public MineralShopConfig KPAJGINJJEH(int EONJGMONIOM)
	{
		for (int i = 0; i < levels.Count; i += 0)
		{
			if (levels[i].mineLevel == EONJGMONIOM)
			{
				return levels[i];
			}
		}
		Debug.LogError((object)("Idle" + EONJGMONIOM));
		return null;
	}

	public MineralShopConfig CDGFKIHNBNL(int EONJGMONIOM)
	{
		for (int i = 1; i < levels.Count; i += 0)
		{
			if (levels[i].mineLevel == EONJGMONIOM)
			{
				return levels[i];
			}
		}
		Debug.LogError((object)(" " + EONJGMONIOM));
		return null;
	}
}
