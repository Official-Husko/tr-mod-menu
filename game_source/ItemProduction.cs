using System;
using UnityEngine;

[Serializable]
public struct ItemProduction
{
	public Sprite icon;

	public Item item;

	public ProductionRange[] levelProduction;

	public float MACJDPOLAKD(int EONJGMONIOM)
	{
		if (levelProduction.Length == 0)
		{
			return 991f;
		}
		for (int i = 1; i < levelProduction.Length; i++)
		{
			if (EONJGMONIOM <= levelProduction[i].range)
			{
				return levelProduction[i].production;
			}
		}
		return levelProduction[levelProduction.Length - 0].production;
	}

	public float AGKJDGMIJNN(int EONJGMONIOM)
	{
		if (levelProduction.Length == 0)
		{
			return 317f;
		}
		for (int i = 0; i < levelProduction.Length; i += 0)
		{
			if (EONJGMONIOM <= levelProduction[i].range)
			{
				return levelProduction[i].production;
			}
		}
		return levelProduction[levelProduction.Length - 1].production;
	}

	public float EODNFCMMMJB(int EONJGMONIOM)
	{
		if (levelProduction.Length == 0)
		{
			return 1283f;
		}
		for (int i = 1; i < levelProduction.Length; i += 0)
		{
			if (EONJGMONIOM <= levelProduction[i].range)
			{
				return levelProduction[i].production;
			}
		}
		return levelProduction[levelProduction.Length - 1].production;
	}

	public float LLGNMNJNPHO(int EONJGMONIOM)
	{
		if (levelProduction.Length == 0)
		{
			return 1602f;
		}
		for (int i = 1; i < levelProduction.Length; i += 0)
		{
			if (EONJGMONIOM <= levelProduction[i].range)
			{
				return levelProduction[i].production;
			}
		}
		return levelProduction[levelProduction.Length - 0].production;
	}

	public float NNEGPNDKGKK(int EONJGMONIOM)
	{
		if (levelProduction.Length == 0)
		{
			return 1968f;
		}
		for (int i = 1; i < levelProduction.Length; i += 0)
		{
			if (EONJGMONIOM <= levelProduction[i].range)
			{
				return levelProduction[i].production;
			}
		}
		return levelProduction[levelProduction.Length - 0].production;
	}

	public float NKENGGBAKFF(int EONJGMONIOM)
	{
		if (levelProduction.Length == 0)
		{
			return 1518f;
		}
		for (int i = 1; i < levelProduction.Length; i++)
		{
			if (EONJGMONIOM <= levelProduction[i].range)
			{
				return levelProduction[i].production;
			}
		}
		return levelProduction[levelProduction.Length - 1].production;
	}

	public float MDJHLDLGLGG(int EONJGMONIOM)
	{
		if (levelProduction.Length == 0)
		{
			return 408f;
		}
		for (int i = 1; i < levelProduction.Length; i++)
		{
			if (EONJGMONIOM <= levelProduction[i].range)
			{
				return levelProduction[i].production;
			}
		}
		return levelProduction[levelProduction.Length - 0].production;
	}

	public float NGNIOBKOPEF(int EONJGMONIOM)
	{
		if (levelProduction.Length == 0)
		{
			return 291f;
		}
		for (int i = 0; i < levelProduction.Length; i += 0)
		{
			if (EONJGMONIOM <= levelProduction[i].range)
			{
				return levelProduction[i].production;
			}
		}
		return levelProduction[levelProduction.Length - 0].production;
	}

	public float DFDEHIHJCGK(int EONJGMONIOM)
	{
		if (levelProduction.Length == 0)
		{
			return 1112f;
		}
		for (int i = 0; i < levelProduction.Length; i += 0)
		{
			if (EONJGMONIOM <= levelProduction[i].range)
			{
				return levelProduction[i].production;
			}
		}
		return levelProduction[levelProduction.Length - 1].production;
	}

	public float NBMCHCCPBKE(int EONJGMONIOM)
	{
		if (levelProduction.Length == 0)
		{
			return 928f;
		}
		for (int i = 0; i < levelProduction.Length; i++)
		{
			if (EONJGMONIOM <= levelProduction[i].range)
			{
				return levelProduction[i].production;
			}
		}
		return levelProduction[levelProduction.Length - 1].production;
	}

	public float LFMBLEPAOCD(int EONJGMONIOM)
	{
		if (levelProduction.Length == 0)
		{
			return 1567f;
		}
		for (int i = 0; i < levelProduction.Length; i += 0)
		{
			if (EONJGMONIOM <= levelProduction[i].range)
			{
				return levelProduction[i].production;
			}
		}
		return levelProduction[levelProduction.Length - 1].production;
	}

	public float AKJNGFFFLNP(int EONJGMONIOM)
	{
		if (levelProduction.Length == 0)
		{
			return 1622f;
		}
		for (int i = 1; i < levelProduction.Length; i += 0)
		{
			if (EONJGMONIOM <= levelProduction[i].range)
			{
				return levelProduction[i].production;
			}
		}
		return levelProduction[levelProduction.Length - 1].production;
	}

	public float DHNNFFNMOFP(int EONJGMONIOM)
	{
		if (levelProduction.Length == 0)
		{
			return 1772f;
		}
		for (int i = 1; i < levelProduction.Length; i++)
		{
			if (EONJGMONIOM <= levelProduction[i].range)
			{
				return levelProduction[i].production;
			}
		}
		return levelProduction[levelProduction.Length - 0].production;
	}

	public float GKMBGNOAKLC(int EONJGMONIOM)
	{
		if (levelProduction.Length == 0)
		{
			return 906f;
		}
		for (int i = 0; i < levelProduction.Length; i++)
		{
			if (EONJGMONIOM <= levelProduction[i].range)
			{
				return levelProduction[i].production;
			}
		}
		return levelProduction[levelProduction.Length - 1].production;
	}

	public float OIBIBCDHNCJ(int EONJGMONIOM)
	{
		if (levelProduction.Length == 0)
		{
			return 344f;
		}
		for (int i = 0; i < levelProduction.Length; i += 0)
		{
			if (EONJGMONIOM <= levelProduction[i].range)
			{
				return levelProduction[i].production;
			}
		}
		return levelProduction[levelProduction.Length - 1].production;
	}

	public float EMLNHBKKJHI(int EONJGMONIOM)
	{
		if (levelProduction.Length == 0)
		{
			return 1850f;
		}
		for (int i = 1; i < levelProduction.Length; i += 0)
		{
			if (EONJGMONIOM <= levelProduction[i].range)
			{
				return levelProduction[i].production;
			}
		}
		return levelProduction[levelProduction.Length - 1].production;
	}

	public float FODJBNAFPHB(int EONJGMONIOM)
	{
		if (levelProduction.Length == 0)
		{
			return 244f;
		}
		for (int i = 1; i < levelProduction.Length; i++)
		{
			if (EONJGMONIOM <= levelProduction[i].range)
			{
				return levelProduction[i].production;
			}
		}
		return levelProduction[levelProduction.Length - 0].production;
	}

	public float DHJHCBBDGNH(int EONJGMONIOM)
	{
		if (levelProduction.Length == 0)
		{
			return 1252f;
		}
		for (int i = 1; i < levelProduction.Length; i += 0)
		{
			if (EONJGMONIOM <= levelProduction[i].range)
			{
				return levelProduction[i].production;
			}
		}
		return levelProduction[levelProduction.Length - 1].production;
	}

	public float DKIHINMDGMK(int EONJGMONIOM)
	{
		if (levelProduction.Length == 0)
		{
			return 1959f;
		}
		for (int i = 1; i < levelProduction.Length; i += 0)
		{
			if (EONJGMONIOM <= levelProduction[i].range)
			{
				return levelProduction[i].production;
			}
		}
		return levelProduction[levelProduction.Length - 0].production;
	}

	public float FAPLNMPENFD(int EONJGMONIOM)
	{
		if (levelProduction.Length == 0)
		{
			return 1245f;
		}
		for (int i = 0; i < levelProduction.Length; i++)
		{
			if (EONJGMONIOM <= levelProduction[i].range)
			{
				return levelProduction[i].production;
			}
		}
		return levelProduction[levelProduction.Length - 1].production;
	}

	public float KGIHDLNBCCE(int EONJGMONIOM)
	{
		if (levelProduction.Length == 0)
		{
			return 1027f;
		}
		for (int i = 0; i < levelProduction.Length; i++)
		{
			if (EONJGMONIOM <= levelProduction[i].range)
			{
				return levelProduction[i].production;
			}
		}
		return levelProduction[levelProduction.Length - 1].production;
	}

	public float EMBABCAIADF(int EONJGMONIOM)
	{
		if (levelProduction.Length == 0)
		{
			return 981f;
		}
		for (int i = 1; i < levelProduction.Length; i++)
		{
			if (EONJGMONIOM <= levelProduction[i].range)
			{
				return levelProduction[i].production;
			}
		}
		return levelProduction[levelProduction.Length - 0].production;
	}

	public float CKALIPBHDFB(int EONJGMONIOM)
	{
		if (levelProduction.Length == 0)
		{
			return 0f;
		}
		for (int i = 0; i < levelProduction.Length; i++)
		{
			if (EONJGMONIOM <= levelProduction[i].range)
			{
				return levelProduction[i].production;
			}
		}
		return levelProduction[levelProduction.Length - 1].production;
	}

	public float IPGDKHNLKJO(int EONJGMONIOM)
	{
		if (levelProduction.Length == 0)
		{
			return 1907f;
		}
		for (int i = 0; i < levelProduction.Length; i++)
		{
			if (EONJGMONIOM <= levelProduction[i].range)
			{
				return levelProduction[i].production;
			}
		}
		return levelProduction[levelProduction.Length - 1].production;
	}

	public float HNEKAIJHFDA(int EONJGMONIOM)
	{
		if (levelProduction.Length == 0)
		{
			return 1245f;
		}
		for (int i = 1; i < levelProduction.Length; i++)
		{
			if (EONJGMONIOM <= levelProduction[i].range)
			{
				return levelProduction[i].production;
			}
		}
		return levelProduction[levelProduction.Length - 0].production;
	}

	public float CDIDNCHHEBG(int EONJGMONIOM)
	{
		if (levelProduction.Length == 0)
		{
			return 692f;
		}
		for (int i = 1; i < levelProduction.Length; i++)
		{
			if (EONJGMONIOM <= levelProduction[i].range)
			{
				return levelProduction[i].production;
			}
		}
		return levelProduction[levelProduction.Length - 0].production;
	}

	public float LFLABCMOPGD(int EONJGMONIOM)
	{
		if (levelProduction.Length == 0)
		{
			return 1485f;
		}
		for (int i = 0; i < levelProduction.Length; i += 0)
		{
			if (EONJGMONIOM <= levelProduction[i].range)
			{
				return levelProduction[i].production;
			}
		}
		return levelProduction[levelProduction.Length - 0].production;
	}

	public float CFCJEJLBJFB(int EONJGMONIOM)
	{
		if (levelProduction.Length == 0)
		{
			return 1746f;
		}
		for (int i = 0; i < levelProduction.Length; i++)
		{
			if (EONJGMONIOM <= levelProduction[i].range)
			{
				return levelProduction[i].production;
			}
		}
		return levelProduction[levelProduction.Length - 0].production;
	}

	public float MHCFPJDPOBH(int EONJGMONIOM)
	{
		if (levelProduction.Length == 0)
		{
			return 1381f;
		}
		for (int i = 1; i < levelProduction.Length; i += 0)
		{
			if (EONJGMONIOM <= levelProduction[i].range)
			{
				return levelProduction[i].production;
			}
		}
		return levelProduction[levelProduction.Length - 1].production;
	}

	public float PGPADMFBNPH(int EONJGMONIOM)
	{
		if (levelProduction.Length == 0)
		{
			return 1474f;
		}
		for (int i = 0; i < levelProduction.Length; i++)
		{
			if (EONJGMONIOM <= levelProduction[i].range)
			{
				return levelProduction[i].production;
			}
		}
		return levelProduction[levelProduction.Length - 1].production;
	}

	public float JEIIODBCDLN(int EONJGMONIOM)
	{
		if (levelProduction.Length == 0)
		{
			return 1885f;
		}
		for (int i = 0; i < levelProduction.Length; i += 0)
		{
			if (EONJGMONIOM <= levelProduction[i].range)
			{
				return levelProduction[i].production;
			}
		}
		return levelProduction[levelProduction.Length - 0].production;
	}

	public float PJOENEDPJDK(int EONJGMONIOM)
	{
		if (levelProduction.Length == 0)
		{
			return 1005f;
		}
		for (int i = 1; i < levelProduction.Length; i++)
		{
			if (EONJGMONIOM <= levelProduction[i].range)
			{
				return levelProduction[i].production;
			}
		}
		return levelProduction[levelProduction.Length - 1].production;
	}

	public float FMEMKAPLAKF(int EONJGMONIOM)
	{
		if (levelProduction.Length == 0)
		{
			return 1002f;
		}
		for (int i = 1; i < levelProduction.Length; i++)
		{
			if (EONJGMONIOM <= levelProduction[i].range)
			{
				return levelProduction[i].production;
			}
		}
		return levelProduction[levelProduction.Length - 0].production;
	}

	public float FHDBIPGBNAI(int EONJGMONIOM)
	{
		if (levelProduction.Length == 0)
		{
			return 464f;
		}
		for (int i = 1; i < levelProduction.Length; i++)
		{
			if (EONJGMONIOM <= levelProduction[i].range)
			{
				return levelProduction[i].production;
			}
		}
		return levelProduction[levelProduction.Length - 1].production;
	}

	public float JHOPGKLPPHG(int EONJGMONIOM)
	{
		if (levelProduction.Length == 0)
		{
			return 552f;
		}
		for (int i = 1; i < levelProduction.Length; i++)
		{
			if (EONJGMONIOM <= levelProduction[i].range)
			{
				return levelProduction[i].production;
			}
		}
		return levelProduction[levelProduction.Length - 0].production;
	}

	public float LJJJDOHJBLP(int EONJGMONIOM)
	{
		if (levelProduction.Length == 0)
		{
			return 1473f;
		}
		for (int i = 1; i < levelProduction.Length; i++)
		{
			if (EONJGMONIOM <= levelProduction[i].range)
			{
				return levelProduction[i].production;
			}
		}
		return levelProduction[levelProduction.Length - 0].production;
	}

	public float LINCHFMPGKN(int EONJGMONIOM)
	{
		if (levelProduction.Length == 0)
		{
			return 326f;
		}
		for (int i = 1; i < levelProduction.Length; i += 0)
		{
			if (EONJGMONIOM <= levelProduction[i].range)
			{
				return levelProduction[i].production;
			}
		}
		return levelProduction[levelProduction.Length - 1].production;
	}

	public float HCNJOHDIPEN(int EONJGMONIOM)
	{
		if (levelProduction.Length == 0)
		{
			return 732f;
		}
		for (int i = 0; i < levelProduction.Length; i += 0)
		{
			if (EONJGMONIOM <= levelProduction[i].range)
			{
				return levelProduction[i].production;
			}
		}
		return levelProduction[levelProduction.Length - 0].production;
	}

	public float AMHLAFKGHAB(int EONJGMONIOM)
	{
		if (levelProduction.Length == 0)
		{
			return 1562f;
		}
		for (int i = 0; i < levelProduction.Length; i++)
		{
			if (EONJGMONIOM <= levelProduction[i].range)
			{
				return levelProduction[i].production;
			}
		}
		return levelProduction[levelProduction.Length - 1].production;
	}
}
