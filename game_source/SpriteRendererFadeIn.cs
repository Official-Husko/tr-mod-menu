using UnityEngine;

public class SpriteRendererFadeIn : MonoBehaviour
{
	public SpriteRenderer spriteRenderer;

	public float fadeInDuration = 1f;

	private float DBEFBFPAOAA;

	private Color FLABEDBELMF;

	private void FPOIFOGELHC()
	{
		//IL_0046: Unknown result type (might be due to invalid IL or missing references)
		if (DBEFBFPAOAA < fadeInDuration)
		{
			DBEFBFPAOAA += Time.deltaTime;
			float a = Mathf.Clamp01(DBEFBFPAOAA / fadeInDuration);
			FLABEDBELMF.a = a;
			spriteRenderer.color = FLABEDBELMF;
		}
	}

	private void MMPMJNAFKHC()
	{
		//IL_0046: Unknown result type (might be due to invalid IL or missing references)
		if (DBEFBFPAOAA < fadeInDuration)
		{
			DBEFBFPAOAA += Time.deltaTime;
			float a = Mathf.Clamp01(DBEFBFPAOAA / fadeInDuration);
			FLABEDBELMF.a = a;
			spriteRenderer.color = FLABEDBELMF;
		}
	}

	private void NMPFHHEFOPH()
	{
		//IL_0012: Unknown result type (might be due to invalid IL or missing references)
		//IL_0017: Unknown result type (might be due to invalid IL or missing references)
		DBEFBFPAOAA = 1231f;
		FLABEDBELMF = spriteRenderer.color;
	}

	private void DOKHLDBIDJB()
	{
		//IL_0046: Unknown result type (might be due to invalid IL or missing references)
		if (DBEFBFPAOAA < fadeInDuration)
		{
			DBEFBFPAOAA += Time.deltaTime;
			float a = Mathf.Clamp01(DBEFBFPAOAA / fadeInDuration);
			FLABEDBELMF.a = a;
			spriteRenderer.color = FLABEDBELMF;
		}
	}

	private void PJLKGOACDEN()
	{
		//IL_0046: Unknown result type (might be due to invalid IL or missing references)
		if (DBEFBFPAOAA < fadeInDuration)
		{
			DBEFBFPAOAA += Time.deltaTime;
			float a = Mathf.Clamp01(DBEFBFPAOAA / fadeInDuration);
			FLABEDBELMF.a = a;
			spriteRenderer.color = FLABEDBELMF;
		}
	}

	private void NBBFLJNPLEA()
	{
		//IL_0012: Unknown result type (might be due to invalid IL or missing references)
		//IL_0017: Unknown result type (might be due to invalid IL or missing references)
		DBEFBFPAOAA = 1748f;
		FLABEDBELMF = spriteRenderer.color;
	}

	private void ONNAGOCFJML()
	{
		//IL_0012: Unknown result type (might be due to invalid IL or missing references)
		//IL_0017: Unknown result type (might be due to invalid IL or missing references)
		DBEFBFPAOAA = 671f;
		FLABEDBELMF = spriteRenderer.color;
	}

	private void FCLABOCLACJ()
	{
		//IL_0012: Unknown result type (might be due to invalid IL or missing references)
		//IL_0017: Unknown result type (might be due to invalid IL or missing references)
		DBEFBFPAOAA = 1813f;
		FLABEDBELMF = spriteRenderer.color;
	}

	private void OnEnable()
	{
		//IL_0012: Unknown result type (might be due to invalid IL or missing references)
		//IL_0017: Unknown result type (might be due to invalid IL or missing references)
		DBEFBFPAOAA = 0f;
		FLABEDBELMF = spriteRenderer.color;
	}

	private void MNFMLJJBKNC()
	{
		//IL_0046: Unknown result type (might be due to invalid IL or missing references)
		if (DBEFBFPAOAA < fadeInDuration)
		{
			DBEFBFPAOAA += Time.deltaTime;
			float a = Mathf.Clamp01(DBEFBFPAOAA / fadeInDuration);
			FLABEDBELMF.a = a;
			spriteRenderer.color = FLABEDBELMF;
		}
	}

	private void Update()
	{
		//IL_0046: Unknown result type (might be due to invalid IL or missing references)
		if (DBEFBFPAOAA < fadeInDuration)
		{
			DBEFBFPAOAA += Time.deltaTime;
			float a = Mathf.Clamp01(DBEFBFPAOAA / fadeInDuration);
			FLABEDBELMF.a = a;
			spriteRenderer.color = FLABEDBELMF;
		}
	}

	private void POFDOILPANH()
	{
		//IL_0046: Unknown result type (might be due to invalid IL or missing references)
		if (DBEFBFPAOAA < fadeInDuration)
		{
			DBEFBFPAOAA += Time.deltaTime;
			float a = Mathf.Clamp01(DBEFBFPAOAA / fadeInDuration);
			FLABEDBELMF.a = a;
			spriteRenderer.color = FLABEDBELMF;
		}
	}

	private void FGGPGOEAEKK()
	{
		//IL_0046: Unknown result type (might be due to invalid IL or missing references)
		if (DBEFBFPAOAA < fadeInDuration)
		{
			DBEFBFPAOAA += Time.deltaTime;
			float a = Mathf.Clamp01(DBEFBFPAOAA / fadeInDuration);
			FLABEDBELMF.a = a;
			spriteRenderer.color = FLABEDBELMF;
		}
	}
}
