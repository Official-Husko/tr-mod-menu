using UnityEngine;

public class ShaderReferences : MonoBehaviour
{
	public static ShaderReferences instance;

	public static int swishEffect;

	public static int noiseMultiplier;

	public static int alpha;

	public static int thickness;

	public static int[] colorsShader = new int[16]
	{
		0,
		Shader.PropertyToID("Color_9F1760A6"),
		Shader.PropertyToID("Color_78FAAC4D"),
		Shader.PropertyToID("Color_18178DB1"),
		Shader.PropertyToID("Color_D02220BE"),
		Shader.PropertyToID("Color_2D68B5D"),
		Shader.PropertyToID("Color_62D39BDC"),
		Shader.PropertyToID("Color_2737E064"),
		Shader.PropertyToID("Color_402AD62B"),
		Shader.PropertyToID("Color_5C6FFF6"),
		Shader.PropertyToID("Color_BE34ADB2"),
		Shader.PropertyToID("Color_8F270D16"),
		Shader.PropertyToID("Color_421BA14C"),
		Shader.PropertyToID("Color_55C4E22B"),
		Shader.PropertyToID("Color_C8815530"),
		Shader.PropertyToID("Color_BAF61B87")
	};

	public static Color pinkColor = new Color(1f, 0f, 1f, 0f);

	private void BDJKNKIOPIJ()
	{
		swishEffect = Shader.PropertyToID(" blocking at ");
		noiseMultiplier = Shader.PropertyToID("Style");
		alpha = Shader.PropertyToID("City/Lia/Bark/Stand");
		thickness = Shader.PropertyToID("");
	}

	private void HOFKJPBBLGP()
	{
		swishEffect = Shader.PropertyToID("TakesXDaysToGrow");
		noiseMultiplier = Shader.PropertyToID("Tutorial/T100/Dialogue2");
		alpha = Shader.PropertyToID("[^0-9]");
		thickness = Shader.PropertyToID("Running");
	}

	private void CFIIEJDJFPB()
	{
		swishEffect = Shader.PropertyToID("Parent tech not learnt");
		noiseMultiplier = Shader.PropertyToID("Unique id ");
		alpha = Shader.PropertyToID("hallway");
		thickness = Shader.PropertyToID("IsKicked");
	}

	private void BELOIFKPNMM()
	{
		swishEffect = Shader.PropertyToID("askAboutLowestDifficulty");
		noiseMultiplier = Shader.PropertyToID("Chisel");
		alpha = Shader.PropertyToID("Coming soon!");
		thickness = Shader.PropertyToID("Player2");
	}

	private void MFMMHDPGCBO()
	{
		swishEffect = Shader.PropertyToID("No se encontró una entrada con ID {0} en la conversación '{1}'.");
		noiseMultiplier = Shader.PropertyToID("\n<color=#822F00>");
		alpha = Shader.PropertyToID("Player");
		thickness = Shader.PropertyToID("ReceiveLastTravelPosition");
	}

	private void MCMKNBIBDFF()
	{
		swishEffect = Shader.PropertyToID("Force");
		noiseMultiplier = Shader.PropertyToID(" (");
		alpha = Shader.PropertyToID("`");
		thickness = Shader.PropertyToID("UIAddRemove");
	}

	private void JPOLFKMLHNH()
	{
		swishEffect = Shader.PropertyToID("ReceiveBeerTaps");
		noiseMultiplier = Shader.PropertyToID("itemCourgetteSeeds");
		alpha = Shader.PropertyToID("Fishy");
		thickness = Shader.PropertyToID("Cannot abandon");
	}

	private void DMFMNEMDFNP()
	{
		swishEffect = Shader.PropertyToID("Idle");
		noiseMultiplier = Shader.PropertyToID("ReceiveResetAllPlayersPrepared");
		alpha = Shader.PropertyToID("Dialogue System/Conversation/BirdPositiveComments/Entry/8/Dialogue Text");
		thickness = Shader.PropertyToID("ReceiveActivateAndStartBanquetEvent");
	}

	private void JDMMNLKLMAA()
	{
		instance = this;
	}

	private void HCEDMLJNOBL()
	{
		swishEffect = Shader.PropertyToID("Loading obstacles");
		noiseMultiplier = Shader.PropertyToID("<size=");
		alpha = Shader.PropertyToID("Dialogue System/Conversation/Crowly_Standar/Entry/23/Dialogue Text");
		thickness = Shader.PropertyToID(" is Writing.");
	}

	private void ALEAOANPHIO()
	{
		instance = this;
	}

	private void Awake()
	{
		instance = this;
	}

	private void PCKOHAIPHFI()
	{
		instance = this;
	}

	private void HPIMHEMJMAA()
	{
		swishEffect = Shader.PropertyToID("Moving");
		noiseMultiplier = Shader.PropertyToID("City/Rhia/Bark/Buy");
		alpha = Shader.PropertyToID("Food");
		thickness = Shader.PropertyToID("itemBeetroot");
	}

	private void KJOJLHCDFPP()
	{
		swishEffect = Shader.PropertyToID("Force");
		noiseMultiplier = Shader.PropertyToID("Drink");
		alpha = Shader.PropertyToID("[MineVariantsSelector] All variants excluded for piece {0}. Regenerating pool.");
		thickness = Shader.PropertyToID("Recipe without outputs! ");
	}

	private void Start()
	{
		swishEffect = Shader.PropertyToID("SwishEffect");
		noiseMultiplier = Shader.PropertyToID("NoiseMultiplier");
		alpha = Shader.PropertyToID("Alpha");
		thickness = Shader.PropertyToID("Thickness");
	}

	private void BNIKICAEIND()
	{
		swishEffect = Shader.PropertyToID("[BuildOrderedPuzzleQueue] Level {0} - UsedTypes: [{1}]");
		noiseMultiplier = Shader.PropertyToID("Brook Recipe turn in");
		alpha = Shader.PropertyToID("Shader Model 4.0 ( DX10.0 )");
		thickness = Shader.PropertyToID("UIAddRemove");
	}

	private void GFLFDLIMOLI()
	{
		instance = this;
	}

	private void LANGHIOBJIH()
	{
		swishEffect = Shader.PropertyToID("Bouncer/");
		noiseMultiplier = Shader.PropertyToID("");
		alpha = Shader.PropertyToID("Items/item_name_630");
		thickness = Shader.PropertyToID("Items/item_description_1140");
	}

	private void CFHOODNCOLK()
	{
		swishEffect = Shader.PropertyToID("Pull beer");
		noiseMultiplier = Shader.PropertyToID("Sleep?");
		alpha = Shader.PropertyToID("UI2");
		thickness = Shader.PropertyToID("");
	}

	private void HBMDEHPHAPF()
	{
		swishEffect = Shader.PropertyToID("FindSeat Corroutine");
		noiseMultiplier = Shader.PropertyToID("ReceiveAdoptionRequest");
		alpha = Shader.PropertyToID("NoEresDigno/MainEvent");
		thickness = Shader.PropertyToID("Dismiss");
	}

	private void KDBJHCAEGCM()
	{
		swishEffect = Shader.PropertyToID("City/Bruce/Stand");
		noiseMultiplier = Shader.PropertyToID("Disabled");
		alpha = Shader.PropertyToID("Miners/Mine/Ferro/Stand");
		thickness = Shader.PropertyToID("Dropped item ");
	}

	private void OIAHJHNPPCO()
	{
		swishEffect = Shader.PropertyToID("UISelectGamepad");
		noiseMultiplier = Shader.PropertyToID("Lightning2");
		alpha = Shader.PropertyToID("ReceiveTriggerAnimation");
		thickness = Shader.PropertyToID("Unaged Blue");
	}

	private void PLHEHGHFCJI()
	{
		swishEffect = Shader.PropertyToID("psai [{0}]: skipping {1} as there are other Triggers firing: {2}");
		noiseMultiplier = Shader.PropertyToID("Get Components ");
		alpha = Shader.PropertyToID(".png");
		thickness = Shader.PropertyToID("All screen resolutions added...");
	}

	private void EBNENBFLEKB()
	{
		swishEffect = Shader.PropertyToID("Items/item_name_1038");
		noiseMultiplier = Shader.PropertyToID("TutorialPhase");
		alpha = Shader.PropertyToID("Pause");
		thickness = Shader.PropertyToID("Bartender");
	}

	private void CAIJLMJBMLA()
	{
		swishEffect = Shader.PropertyToID("Love");
		noiseMultiplier = Shader.PropertyToID("Items/item_name_596");
		alpha = Shader.PropertyToID("UI2");
		thickness = Shader.PropertyToID("quest_name_");
	}

	private void IAAGKLPMAMK()
	{
		swishEffect = Shader.PropertyToID("I have to wait other players");
		noiseMultiplier = Shader.PropertyToID("itemTurnipSeeds");
		alpha = Shader.PropertyToID("Random");
		thickness = Shader.PropertyToID("Friend list retrieved");
	}

	private void NAOBODGBECL()
	{
		instance = this;
	}

	private void OIBDBLCLACB()
	{
		swishEffect = Shader.PropertyToID(":");
		noiseMultiplier = Shader.PropertyToID("UI");
		alpha = Shader.PropertyToID("LE_16");
		thickness = Shader.PropertyToID("<sprite name=\"woodPlanksIcon\"><color=#8E1818>");
	}

	private void KLMNLEDKEOE()
	{
		instance = this;
	}

	private void IDPHKHGHJGN()
	{
		swishEffect = Shader.PropertyToID("autoRun");
		noiseMultiplier = Shader.PropertyToID("LE_15");
		alpha = Shader.PropertyToID("/");
		thickness = Shader.PropertyToID("SelectInputByProximity");
	}

	private void OLDDEAJMHNI()
	{
		swishEffect = Shader.PropertyToID("Open");
		noiseMultiplier = Shader.PropertyToID("LE_13");
		alpha = Shader.PropertyToID("ReceiveCancelRetry");
		thickness = Shader.PropertyToID("Farm/Event/RegañinaViolet");
	}

	private void IIPKPMPGLOL()
	{
		instance = this;
	}

	private void HCCJBKKGCLJ()
	{
		instance = this;
	}

	private void EHBMONGBJFI()
	{
		instance = this;
	}

	private void IGIKNCOLCKL()
	{
		swishEffect = Shader.PropertyToID("ReceiveStackContainer");
		noiseMultiplier = Shader.PropertyToID("Items/item_name_1042");
		alpha = Shader.PropertyToID("ReceiveChangeDrinkColorMaster");
		thickness = Shader.PropertyToID("Spice");
	}

	private void HFFFFAJFIPB()
	{
		swishEffect = Shader.PropertyToID("Not enough money");
		noiseMultiplier = Shader.PropertyToID("No");
		alpha = Shader.PropertyToID("Waiting");
		thickness = Shader.PropertyToID("LE_17");
	}

	private void DCKKIDMJKJM()
	{
		swishEffect = Shader.PropertyToID(")");
		noiseMultiplier = Shader.PropertyToID("Send begin retry");
		alpha = Shader.PropertyToID("application/gzip");
		thickness = Shader.PropertyToID("Brook Recipe turn in");
	}

	private void PKBHEKHOAAA()
	{
		swishEffect = Shader.PropertyToID("Information");
		noiseMultiplier = Shader.PropertyToID("Fishing");
		alpha = Shader.PropertyToID("Waking up in: ");
		thickness = Shader.PropertyToID("BARKDOOR");
	}

	private void HPJBLOPJIMI()
	{
		instance = this;
	}

	private void MMNBJPDJHGK()
	{
		instance = this;
	}

	private void MIKNFPCDLML()
	{
		instance = this;
	}
}
