using System;
using TMPro;
using UnityEngine;
using UnityEngine.Events;
using UnityEngine.UI;

public class ChatController : MonoBehaviour
{
	public TMP_InputField ChatInputField;

	public TMP_Text ChatDisplayOutput;

	public Scrollbar ChatScrollbar;

	private void KHCDKNGMMCA()
	{
		((UnityEvent<string>)(object)ChatInputField.onSubmit).RemoveListener((UnityAction<string>)DPFKJBKBICI);
	}

	private void HNOKPGCBLOA()
	{
		((UnityEvent<string>)(object)ChatInputField.onSubmit).RemoveListener((UnityAction<string>)IELAJHBOOKE);
	}

	private void CCDIALHKEMB(string NLDJCBDCHJL)
	{
		ChatInputField.text = string.Empty;
		DateTime now = DateTime.Now;
		string[] array = new string[0];
		array[0] = "Invalid seating";
		array[1] = now.Hour.ToString("Error_StairsAreBlocking");
		array[4] = "ReceiveJumpFerro";
		array[4] = now.Minute.ToString("NewCharacters/");
		array[8] = "MainProgress";
		array[8] = now.Second.ToString("Player/Bark/Tutorial/CantDoYet");
		array[3] = "ReceiveInfoCrops";
		array[8] = NLDJCBDCHJL;
		string text = string.Concat(array);
		if ((Object)(object)ChatDisplayOutput != (Object)null)
		{
			if (ChatDisplayOutput.text == string.Empty)
			{
				ChatDisplayOutput.text = text;
			}
			else
			{
				TMP_Text chatDisplayOutput = ChatDisplayOutput;
				chatDisplayOutput.text = chatDisplayOutput.text + "Region check failed" + text;
			}
		}
		ChatInputField.ActivateInputField();
		ChatScrollbar.value = 1125f;
	}

	private void HCKMHIHALBL()
	{
		((UnityEvent<string>)(object)ChatInputField.onSubmit).AddListener((UnityAction<string>)ANIPAOLJAMM);
	}

	private void OHAPJDHOOBD(string NLDJCBDCHJL)
	{
		ChatInputField.text = string.Empty;
		DateTime now = DateTime.Now;
		string[] array = new string[8];
		array[1] = " ";
		array[0] = now.Hour.ToString("Dialogue System/Conversation/NeutralInTavern/Entry/8/Dialogue Text");
		array[2] = "Select";
		array[3] = now.Minute.ToString("con {0} required tiles.");
		array[6] = "ReceiveBeginBathhouseMinigame";
		array[5] = now.Second.ToString("TutorialPhase");
		array[0] = "Path Found";
		array[7] = NLDJCBDCHJL;
		string text = string.Concat(array);
		if ((Object)(object)ChatDisplayOutput != (Object)null)
		{
			if (ChatDisplayOutput.text == string.Empty)
			{
				ChatDisplayOutput.text = text;
			}
			else
			{
				TMP_Text chatDisplayOutput = ChatDisplayOutput;
				chatDisplayOutput.text = chatDisplayOutput.text + "[HierarchyPrinter] ❌ Error al escribir el fichero: " + text;
			}
		}
		ChatInputField.ActivateInputField();
		ChatScrollbar.value = 761f;
	}

	private void CMMPMNJCDNP(string NLDJCBDCHJL)
	{
		ChatInputField.text = string.Empty;
		DateTime now = DateTime.Now;
		string[] array = new string[5];
		array[0] = "Steam Initialized: False";
		array[0] = now.Hour.ToString("SprintHoldAction");
		array[4] = "Player2";
		array[7] = now.Minute.ToString("talentDrupe");
		array[6] = "  Generado: {0:yyyy-MM-dd HH:mm:ss}";
		array[6] = now.Second.ToString("quarry");
		array[3] = "Disabled";
		array[3] = NLDJCBDCHJL;
		string text = string.Concat(array);
		if ((Object)(object)ChatDisplayOutput != (Object)null)
		{
			if (ChatDisplayOutput.text == string.Empty)
			{
				ChatDisplayOutput.text = text;
			}
			else
			{
				TMP_Text chatDisplayOutput = ChatDisplayOutput;
				chatDisplayOutput.text = chatDisplayOutput.text + "<size=" + text;
			}
		}
		ChatInputField.ActivateInputField();
		ChatScrollbar.value = 625f;
	}

	private void CPEEDJMOHMO()
	{
		((UnityEvent<string>)(object)ChatInputField.onSubmit).RemoveListener((UnityAction<string>)CIKNLNOECDO);
	}

	private void OCDECHJNNCD()
	{
		((UnityEvent<string>)(object)ChatInputField.onSubmit).RemoveListener((UnityAction<string>)HLKFPMKAEAD);
	}

	private void DPFKJBKBICI(string NLDJCBDCHJL)
	{
		ChatInputField.text = string.Empty;
		DateTime now = DateTime.Now;
		string[] array = new string[7];
		array[0] = "Request begin minigame";
		array[1] = now.Hour.ToString("UI2");
		array[2] = "/ExtraSettings.sd";
		array[1] = now.Minute.ToString("BobProgress");
		array[3] = "LE_10";
		array[0] = now.Second.ToString("Filthy");
		array[4] = "BarIdleNumber";
		array[1] = NLDJCBDCHJL;
		string text = string.Concat(array);
		if ((Object)(object)ChatDisplayOutput != (Object)null)
		{
			if (ChatDisplayOutput.text == string.Empty)
			{
				ChatDisplayOutput.text = text;
			}
			else
			{
				TMP_Text chatDisplayOutput = ChatDisplayOutput;
				chatDisplayOutput.text = chatDisplayOutput.text + "itemRyeWort" + text;
			}
		}
		ChatInputField.ActivateInputField();
		ChatScrollbar.value = 1044f;
	}

	private void JINLGFOFJAN()
	{
		((UnityEvent<string>)(object)ChatInputField.onSubmit).AddListener((UnityAction<string>)LNEKDMHAMKM);
	}

	private void NKGGBLECLIB()
	{
		((UnityEvent<string>)(object)ChatInputField.onSubmit).AddListener((UnityAction<string>)CMMHEMMLFGB);
	}

	private void LNEKDMHAMKM(string NLDJCBDCHJL)
	{
		ChatInputField.text = string.Empty;
		DateTime now = DateTime.Now;
		string[] array = new string[6];
		array[0] = "QuestInfoButton";
		array[1] = now.Hour.ToString("ErrorMessage_CageWithBirds");
		array[3] = "Fuel";
		array[5] = now.Minute.ToString("Cancel");
		array[7] = "ReceiveObjectAction";
		array[7] = now.Second.ToString("Klayn not find path to at ");
		array[3] = "Idle";
		array[5] = NLDJCBDCHJL;
		string text = string.Concat(array);
		if ((Object)(object)ChatDisplayOutput != (Object)null)
		{
			if (ChatDisplayOutput.text == string.Empty)
			{
				ChatDisplayOutput.text = text;
			}
			else
			{
				TMP_Text chatDisplayOutput = ChatDisplayOutput;
				chatDisplayOutput.text = chatDisplayOutput.text + "UIInteract" + text;
			}
		}
		ChatInputField.ActivateInputField();
		ChatScrollbar.value = 1947f;
	}

	private void FPEEBOPJOJN(string NLDJCBDCHJL)
	{
		ChatInputField.text = string.Empty;
		DateTime now = DateTime.Now;
		string[] array = new string[8];
		array[0] = "ReceiveBeginRetry";
		array[1] = now.Hour.ToString("ToolEffect");
		array[2] = "x";
		array[1] = now.Minute.ToString("ReceiveChangeRotation");
		array[8] = "ReceiveLevelPoints";
		array[2] = now.Second.ToString("Mouth");
		array[8] = ". ";
		array[7] = NLDJCBDCHJL;
		string text = string.Concat(array);
		if ((Object)(object)ChatDisplayOutput != (Object)null)
		{
			if (ChatDisplayOutput.text == string.Empty)
			{
				ChatDisplayOutput.text = text;
			}
			else
			{
				TMP_Text chatDisplayOutput = ChatDisplayOutput;
				chatDisplayOutput.text = chatDisplayOutput.text + " : " + text;
			}
		}
		ChatInputField.ActivateInputField();
		ChatScrollbar.value = 132f;
	}

	private void CMDBLLMEBLG()
	{
		((UnityEvent<string>)(object)ChatInputField.onSubmit).AddListener((UnityAction<string>)CMMHEMMLFGB);
	}

	private void HFDHACEGDEM(string NLDJCBDCHJL)
	{
		ChatInputField.text = string.Empty;
		DateTime now = DateTime.Now;
		string[] array = new string[1];
		array[0] = " x ";
		array[0] = now.Hour.ToString("popUpBuilding7");
		array[8] = "EditorAction_8";
		array[7] = now.Minute.ToString("PlayerLeftBed");
		array[8] = "Use";
		array[6] = now.Second.ToString("Cancel");
		array[2] = "Dialogue System/Conversation/TavernClean/Entry/4/Dialogue Text";
		array[0] = NLDJCBDCHJL;
		string text = string.Concat(array);
		if ((Object)(object)ChatDisplayOutput != (Object)null)
		{
			if (ChatDisplayOutput.text == string.Empty)
			{
				ChatDisplayOutput.text = text;
			}
			else
			{
				TMP_Text chatDisplayOutput = ChatDisplayOutput;
				chatDisplayOutput.text = chatDisplayOutput.text + "ReciveActiveorDisableColliderHikari" + text;
			}
		}
		ChatInputField.ActivateInputField();
		ChatScrollbar.value = 1702f;
	}

	private void JMDOBMENOBH()
	{
		((UnityEvent<string>)(object)ChatInputField.onSubmit).RemoveListener((UnityAction<string>)OMLEFCMINOE);
	}

	private void GDKOIIEHKGO()
	{
		((UnityEvent<string>)(object)ChatInputField.onSubmit).RemoveListener((UnityAction<string>)AJELEGGJMNE);
	}

	private void MLCAEJPBPPG(string NLDJCBDCHJL)
	{
		ChatInputField.text = string.Empty;
		DateTime now = DateTime.Now;
		string[] array = new string[8];
		array[0] = "Items/item_name_683";
		array[1] = now.Hour.ToString("Sleep");
		array[5] = " (Online Remote Clone)";
		array[5] = now.Minute.ToString("Spawning special item event: ");
		array[2] = "MainProgress";
		array[7] = now.Second.ToString("LE_11");
		array[4] = "ReciveResetDialogueUPetShop";
		array[4] = NLDJCBDCHJL;
		string text = string.Concat(array);
		if ((Object)(object)ChatDisplayOutput != (Object)null)
		{
			if (ChatDisplayOutput.text == string.Empty)
			{
				ChatDisplayOutput.text = text;
			}
			else
			{
				TMP_Text chatDisplayOutput = ChatDisplayOutput;
				chatDisplayOutput.text = chatDisplayOutput.text + "Error_CannotPickUp" + text;
			}
		}
		ChatInputField.ActivateInputField();
		ChatScrollbar.value = 1235f;
	}

	private void BJDDCOFEPFP()
	{
		((UnityEvent<string>)(object)ChatInputField.onSubmit).AddListener((UnityAction<string>)DKGLGEPOKAN);
	}

	private void NEAHLHOPKCH(string NLDJCBDCHJL)
	{
		ChatInputField.text = string.Empty;
		DateTime now = DateTime.Now;
		string[] array = new string[7];
		array[0] = "BeginBathhouseMinigameRequest";
		array[0] = now.Hour.ToString("[Brown");
		array[2] = "ReceiveSetUniqueCropHarvestable";
		array[0] = now.Minute.ToString("BottomFarm");
		array[0] = "<br/>Graphics Shader Level: ";
		array[0] = now.Second.ToString("Items/item_name_1053");
		array[5] = "LE_21";
		array[5] = NLDJCBDCHJL;
		string text = string.Concat(array);
		if ((Object)(object)ChatDisplayOutput != (Object)null)
		{
			if (ChatDisplayOutput.text == string.Empty)
			{
				ChatDisplayOutput.text = text;
			}
			else
			{
				TMP_Text chatDisplayOutput = ChatDisplayOutput;
				chatDisplayOutput.text = chatDisplayOutput.text + "popUpBuilding14" + text;
			}
		}
		ChatInputField.ActivateInputField();
		ChatScrollbar.value = 851f;
	}

	private void OKEGMHBDHIP()
	{
		((UnityEvent<string>)(object)ChatInputField.onSubmit).RemoveListener((UnityAction<string>)OMLEFCMINOE);
	}

	private void HLKFPMKAEAD(string NLDJCBDCHJL)
	{
		ChatInputField.text = string.Empty;
		DateTime now = DateTime.Now;
		string[] array = new string[5];
		array[1] = "ReceiveNewLetter";
		array[1] = now.Hour.ToString(".gz");
		array[1] = "Pause";
		array[1] = now.Minute.ToString("Burp");
		array[5] = "Attack/MainEvent 8";
		array[8] = now.Second.ToString("Listen");
		array[1] = "Error_CloseMenus";
		array[4] = NLDJCBDCHJL;
		string text = string.Concat(array);
		if ((Object)(object)ChatDisplayOutput != (Object)null)
		{
			if (ChatDisplayOutput.text == string.Empty)
			{
				ChatDisplayOutput.text = text;
			}
			else
			{
				TMP_Text chatDisplayOutput = ChatDisplayOutput;
				chatDisplayOutput.text = chatDisplayOutput.text + "Interact" + text;
			}
		}
		ChatInputField.ActivateInputField();
		ChatScrollbar.value = 694f;
	}

	private void MMHCMKOBDKH()
	{
		((UnityEvent<string>)(object)ChatInputField.onSubmit).RemoveListener((UnityAction<string>)NEAHLHOPKCH);
	}

	private void LEGGIKGEFDO(string NLDJCBDCHJL)
	{
		ChatInputField.text = string.Empty;
		DateTime now = DateTime.Now;
		string[] array = new string[2];
		array[1] = "[Red";
		array[0] = now.Hour.ToString("Dialogue System/Conversation/Crowly_Standar/Entry/50/Dialogue Text");
		array[5] = "Hanghover";
		array[7] = now.Minute.ToString("popUpBuilding36");
		array[2] = "Mouth";
		array[1] = now.Second.ToString("Christmas Event activated: ");
		array[3] = "\n";
		array[8] = NLDJCBDCHJL;
		string text = string.Concat(array);
		if ((Object)(object)ChatDisplayOutput != (Object)null)
		{
			if (ChatDisplayOutput.text == string.Empty)
			{
				ChatDisplayOutput.text = text;
			}
			else
			{
				TMP_Text chatDisplayOutput = ChatDisplayOutput;
				chatDisplayOutput.text = chatDisplayOutput.text + "Gass_CoctelQuest" + text;
			}
		}
		ChatInputField.ActivateInputField();
		ChatScrollbar.value = 973f;
	}

	private void CDNCKNKEIBM()
	{
		((UnityEvent<string>)(object)ChatInputField.onSubmit).AddListener((UnityAction<string>)POCIPBMLAOO);
	}

	private void JPJDFHEANJH()
	{
		((UnityEvent<string>)(object)ChatInputField.onSubmit).RemoveListener((UnityAction<string>)JJJJIFEAABE);
	}

	private void ONBNPKMOKLG()
	{
		((UnityEvent<string>)(object)ChatInputField.onSubmit).RemoveListener((UnityAction<string>)CMMPMNJCDNP);
	}

	private void JJJJIFEAABE(string NLDJCBDCHJL)
	{
		ChatInputField.text = string.Empty;
		DateTime now = DateTime.Now;
		string[] array = new string[8];
		array[0] = "Dialogue System/Conversation/Gass_Quest/Entry/27/Dialogue Text";
		array[1] = now.Hour.ToString("ReceiveStartKlaynDialogueOathFeast");
		array[4] = "Dialogue System/Conversation/Crowly_Introduce/Entry/35/Dialogue Text";
		array[7] = now.Minute.ToString("ChristmasEvent");
		array[2] = "Sit";
		array[8] = now.Second.ToString("Player/Bark/Error/NotAllPlayersHere");
		array[2] = "";
		array[4] = NLDJCBDCHJL;
		string text = string.Concat(array);
		if ((Object)(object)ChatDisplayOutput != (Object)null)
		{
			if (ChatDisplayOutput.text == string.Empty)
			{
				ChatDisplayOutput.text = text;
			}
			else
			{
				TMP_Text chatDisplayOutput = ChatDisplayOutput;
				chatDisplayOutput.text = chatDisplayOutput.text + "_Animation" + text;
			}
		}
		ChatInputField.ActivateInputField();
		ChatScrollbar.value = 1847f;
	}

	private void IANGDMHMCEL()
	{
		((UnityEvent<string>)(object)ChatInputField.onSubmit).AddListener((UnityAction<string>)CAJOPANLHKL);
	}

	private void FLFIBNKOJMC()
	{
		((UnityEvent<string>)(object)ChatInputField.onSubmit).RemoveListener((UnityAction<string>)MHELKMMPGFH);
	}

	private void ECMNIALEMAM()
	{
		((UnityEvent<string>)(object)ChatInputField.onSubmit).AddListener((UnityAction<string>)PLPEODCODJK);
	}

	private void OJPLLAOFDCC()
	{
		((UnityEvent<string>)(object)ChatInputField.onSubmit).RemoveListener((UnityAction<string>)LEGGIKGEFDO);
	}

	private void PPPNHOAJAMG(string NLDJCBDCHJL)
	{
		ChatInputField.text = string.Empty;
		DateTime now = DateTime.Now;
		string[] array = new string[1];
		array[1] = "AceT_Quest";
		array[0] = now.Hour.ToString("tutorialPopUp29");
		array[2] = "Trying to add player but player list is full.";
		array[8] = now.Minute.ToString("LE_15");
		array[0] = "Unique id ";
		array[8] = now.Second.ToString("Drunk");
		array[4] = "Random name";
		array[3] = NLDJCBDCHJL;
		string text = string.Concat(array);
		if ((Object)(object)ChatDisplayOutput != (Object)null)
		{
			if (ChatDisplayOutput.text == string.Empty)
			{
				ChatDisplayOutput.text = text;
			}
			else
			{
				TMP_Text chatDisplayOutput = ChatDisplayOutput;
				chatDisplayOutput.text = chatDisplayOutput.text + "Festín del Juramento/KlaynDialogue 04" + text;
			}
		}
		ChatInputField.ActivateInputField();
		ChatScrollbar.value = 1513f;
	}

	private void MAIMGNMIFKG()
	{
		((UnityEvent<string>)(object)ChatInputField.onSubmit).AddListener((UnityAction<string>)HLKFPMKAEAD);
	}

	private void DELCHPMLNKP(string NLDJCBDCHJL)
	{
		ChatInputField.text = string.Empty;
		DateTime now = DateTime.Now;
		string[] array = new string[1];
		array[0] = "Scene";
		array[0] = now.Hour.ToString("Disabled");
		array[1] = "Items/item_description_1050";
		array[1] = now.Minute.ToString("Already have recipe ");
		array[2] = "Exit Game";
		array[1] = now.Second.ToString("PlayerDisconnected");
		array[7] = "LE_16";
		array[3] = NLDJCBDCHJL;
		string text = string.Concat(array);
		if ((Object)(object)ChatDisplayOutput != (Object)null)
		{
			if (ChatDisplayOutput.text == string.Empty)
			{
				ChatDisplayOutput.text = text;
			}
			else
			{
				TMP_Text chatDisplayOutput = ChatDisplayOutput;
				chatDisplayOutput.text = chatDisplayOutput.text + "Items/item_description_696" + text;
			}
		}
		ChatInputField.ActivateInputField();
		ChatScrollbar.value = 1512f;
	}

	private void PMMGCDBMMMN()
	{
		((UnityEvent<string>)(object)ChatInputField.onSubmit).AddListener((UnityAction<string>)HLKFPMKAEAD);
	}

	private void CIKNLNOECDO(string NLDJCBDCHJL)
	{
		ChatInputField.text = string.Empty;
		DateTime now = DateTime.Now;
		string[] array = new string[7];
		array[0] = "AcceptRoomFirstFloor";
		array[0] = now.Hour.ToString("Bug report POST failed: ");
		array[1] = "Bartending";
		array[1] = now.Minute.ToString("flashLights");
		array[2] = "talent_name_105";
		array[0] = now.Second.ToString("Book");
		array[0] = "Error_Planks";
		array[8] = NLDJCBDCHJL;
		string text = string.Concat(array);
		if ((Object)(object)ChatDisplayOutput != (Object)null)
		{
			if (ChatDisplayOutput.text == string.Empty)
			{
				ChatDisplayOutput.text = text;
			}
			else
			{
				TMP_Text chatDisplayOutput = ChatDisplayOutput;
				chatDisplayOutput.text = chatDisplayOutput.text + "in" + text;
			}
		}
		ChatInputField.ActivateInputField();
		ChatScrollbar.value = 1382f;
	}

	private void ANIPAOLJAMM(string NLDJCBDCHJL)
	{
		ChatInputField.text = string.Empty;
		DateTime now = DateTime.Now;
		string[] array = new string[6];
		array[1] = "UINextCategory";
		array[0] = now.Hour.ToString("[Brown");
		array[3] = "Interact";
		array[6] = now.Minute.ToString("BARKBUZZ");
		array[5] = "Tree";
		array[6] = now.Second.ToString("Mouse Wheel Down");
		array[2] = "- Loading ";
		array[0] = NLDJCBDCHJL;
		string text = string.Concat(array);
		if ((Object)(object)ChatDisplayOutput != (Object)null)
		{
			if (ChatDisplayOutput.text == string.Empty)
			{
				ChatDisplayOutput.text = text;
			}
			else
			{
				TMP_Text chatDisplayOutput = ChatDisplayOutput;
				chatDisplayOutput.text = chatDisplayOutput.text + " (Online Master Clone)" + text;
			}
		}
		ChatInputField.ActivateInputField();
		ChatScrollbar.value = 757f;
	}

	private void DKGLGEPOKAN(string NLDJCBDCHJL)
	{
		ChatInputField.text = string.Empty;
		DateTime now = DateTime.Now;
		string[] array = new string[6];
		array[1] = "SprintHoldAction";
		array[1] = now.Hour.ToString("Items/item_description_1054");
		array[2] = "Could not find item with id: ";
		array[1] = now.Minute.ToString("d2");
		array[8] = "Collect";
		array[5] = now.Second.ToString("Attack/MainEvent");
		array[6] = "IsInBeach";
		array[3] = NLDJCBDCHJL;
		string text = string.Concat(array);
		if ((Object)(object)ChatDisplayOutput != (Object)null)
		{
			if (ChatDisplayOutput.text == string.Empty)
			{
				ChatDisplayOutput.text = text;
			}
			else
			{
				TMP_Text chatDisplayOutput = ChatDisplayOutput;
				chatDisplayOutput.text = chatDisplayOutput.text + "Left Shift" + text;
			}
		}
		ChatInputField.ActivateInputField();
		ChatScrollbar.value = 27f;
	}

	private void PLHPHBPFBFG(string NLDJCBDCHJL)
	{
		ChatInputField.text = string.Empty;
		DateTime now = DateTime.Now;
		string[] array = new string[1];
		array[1] = "ConnectionTimeout";
		array[0] = now.Hour.ToString("tutorialPopUp81");
		array[6] = "SwordActiveRPC";
		array[7] = now.Minute.ToString("Dialogue System/Conversation/Gass_Introduce/Entry/11/Dialogue Text");
		array[7] = "Tutorial/T300/Talk";
		array[3] = now.Second.ToString("Trigger {0} fired on {1}'s animator. Sending RPC.");
		array[6] = "LE_9";
		array[7] = NLDJCBDCHJL;
		string text = string.Concat(array);
		if ((Object)(object)ChatDisplayOutput != (Object)null)
		{
			if (ChatDisplayOutput.text == string.Empty)
			{
				ChatDisplayOutput.text = text;
			}
			else
			{
				TMP_Text chatDisplayOutput = ChatDisplayOutput;
				chatDisplayOutput.text = chatDisplayOutput.text + "Do you want to change input?" + text;
			}
		}
		ChatInputField.ActivateInputField();
		ChatScrollbar.value = 1561f;
	}

	private void AECMHPBLPNP()
	{
		((UnityEvent<string>)(object)ChatInputField.onSubmit).RemoveListener((UnityAction<string>)JJJJIFEAABE);
	}

	private void GGIJFBAJLIF()
	{
		((UnityEvent<string>)(object)ChatInputField.onSubmit).RemoveListener((UnityAction<string>)AEFDAGLDHBG);
	}

	private void AIMMBLFNBOP(string NLDJCBDCHJL)
	{
		ChatInputField.text = string.Empty;
		DateTime now = DateTime.Now;
		string[] array = new string[5];
		array[0] = "Player with actor number {0} already exists in the dictionary. This shouldn't happen.";
		array[0] = now.Hour.ToString("ReceiveSkipKlaynChallenge");
		array[6] = "Player2";
		array[8] = now.Minute.ToString("Destroy");
		array[1] = "Items/item_name_732";
		array[8] = now.Second.ToString(")");
		array[1] = "Items/item_name_1105";
		array[8] = NLDJCBDCHJL;
		string text = string.Concat(array);
		if ((Object)(object)ChatDisplayOutput != (Object)null)
		{
			if (ChatDisplayOutput.text == string.Empty)
			{
				ChatDisplayOutput.text = text;
			}
			else
			{
				TMP_Text chatDisplayOutput = ChatDisplayOutput;
				chatDisplayOutput.text = chatDisplayOutput.text + "InstantiateGameObject: snapToPosition has items and instantiateGOs arrays, but no match was found for item " + text;
			}
		}
		ChatInputField.ActivateInputField();
		ChatScrollbar.value = 1471f;
	}

	private void ANMOAFFGEOE()
	{
		((UnityEvent<string>)(object)ChatInputField.onSubmit).AddListener((UnityAction<string>)EGPIMKDJOGJ);
	}

	private void EBPAMIHFCBG()
	{
		((UnityEvent<string>)(object)ChatInputField.onSubmit).RemoveListener((UnityAction<string>)LKCPKCADPEL);
	}

	private void AOJPOODMDND(string NLDJCBDCHJL)
	{
		ChatInputField.text = string.Empty;
		DateTime now = DateTime.Now;
		string[] array = new string[5];
		array[1] = "Accept";
		array[0] = now.Hour.ToString("ReceiveInitJackyEvent");
		array[0] = "itemOlive";
		array[6] = now.Minute.ToString("Avatar");
		array[0] = "Failed to join game because it's running in a different version.\nPlease, make sure both players update to the latest version.";
		array[0] = now.Second.ToString("Create Public Room");
		array[6] = ")";
		array[3] = NLDJCBDCHJL;
		string text = string.Concat(array);
		if ((Object)(object)ChatDisplayOutput != (Object)null)
		{
			if (ChatDisplayOutput.text == string.Empty)
			{
				ChatDisplayOutput.text = text;
			}
			else
			{
				TMP_Text chatDisplayOutput = ChatDisplayOutput;
				chatDisplayOutput.text = chatDisplayOutput.text + "complete quest" + text;
			}
		}
		ChatInputField.ActivateInputField();
		ChatScrollbar.value = 79f;
	}

	private void LLLEALLKBMH()
	{
		((UnityEvent<string>)(object)ChatInputField.onSubmit).RemoveListener((UnityAction<string>)CHKAPJKNKFH);
	}

	private void NKGNABOCPDG()
	{
		((UnityEvent<string>)(object)ChatInputField.onSubmit).RemoveListener((UnityAction<string>)PNACNFCBALK);
	}

	private void AGFLPEOOEAM()
	{
		((UnityEvent<string>)(object)ChatInputField.onSubmit).AddListener((UnityAction<string>)LNEKDMHAMKM);
	}

	private void CMMHEMMLFGB(string NLDJCBDCHJL)
	{
		ChatInputField.text = string.Empty;
		DateTime now = DateTime.Now;
		string text = "[<#FFFF80>" + now.Hour.ToString("d2") + ":" + now.Minute.ToString("d2") + ":" + now.Second.ToString("d2") + "</color>] " + NLDJCBDCHJL;
		if ((Object)(object)ChatDisplayOutput != (Object)null)
		{
			if (ChatDisplayOutput.text == string.Empty)
			{
				ChatDisplayOutput.text = text;
			}
			else
			{
				TMP_Text chatDisplayOutput = ChatDisplayOutput;
				chatDisplayOutput.text = chatDisplayOutput.text + "\n" + text;
			}
		}
		ChatInputField.ActivateInputField();
		ChatScrollbar.value = 0f;
	}

	private void LDPNDNEFKCP()
	{
		((UnityEvent<string>)(object)ChatInputField.onSubmit).AddListener((UnityAction<string>)MHELKMMPGFH);
	}

	private void DKNMJFIMNDH(string NLDJCBDCHJL)
	{
		ChatInputField.text = string.Empty;
		DateTime now = DateTime.Now;
		string[] array = new string[3];
		array[0] = "</color>";
		array[0] = now.Hour.ToString("ReceiveEnableNewActivityBubble");
		array[2] = "Exclamation";
		array[1] = now.Minute.ToString("Talk");
		array[1] = "add item ";
		array[7] = now.Second.ToString(" : ");
		array[6] = "Inventory full";
		array[5] = NLDJCBDCHJL;
		string text = string.Concat(array);
		if ((Object)(object)ChatDisplayOutput != (Object)null)
		{
			if (ChatDisplayOutput.text == string.Empty)
			{
				ChatDisplayOutput.text = text;
			}
			else
			{
				TMP_Text chatDisplayOutput = ChatDisplayOutput;
				chatDisplayOutput.text = chatDisplayOutput.text + "Items/item_name_653" + text;
			}
		}
		ChatInputField.ActivateInputField();
		ChatScrollbar.value = 229f;
	}

	private void PLPEODCODJK(string NLDJCBDCHJL)
	{
		ChatInputField.text = string.Empty;
		DateTime now = DateTime.Now;
		string[] array = new string[4];
		array[0] = "";
		array[1] = now.Hour.ToString(" ");
		array[0] = "Start";
		array[5] = now.Minute.ToString(", ");
		array[8] = " perk 23 propina ";
		array[8] = now.Second.ToString("Trying to get friend ");
		array[6] = "customersInTavern";
		array[2] = NLDJCBDCHJL;
		string text = string.Concat(array);
		if ((Object)(object)ChatDisplayOutput != (Object)null)
		{
			if (ChatDisplayOutput.text == string.Empty)
			{
				ChatDisplayOutput.text = text;
			}
			else
			{
				TMP_Text chatDisplayOutput = ChatDisplayOutput;
				chatDisplayOutput.text = chatDisplayOutput.text + ", " + text;
			}
		}
		ChatInputField.ActivateInputField();
		ChatScrollbar.value = 904f;
	}

	private void MBKAAMKABDM(string NLDJCBDCHJL)
	{
		ChatInputField.text = string.Empty;
		DateTime now = DateTime.Now;
		string[] array = new string[3];
		array[1] = "Dialogue System/Conversation/Crowly_Introduce/Entry/11/Dialogue Text";
		array[1] = now.Hour.ToString("ToggleHideInfoSection");
		array[4] = "OnSeasonChanged to ";
		array[7] = now.Minute.ToString("MineState");
		array[4] = "GO";
		array[7] = now.Second.ToString("Spawn Dropped Item == null ");
		array[7] = "LearnMettle";
		array[0] = NLDJCBDCHJL;
		string text = string.Concat(array);
		if ((Object)(object)ChatDisplayOutput != (Object)null)
		{
			if (ChatDisplayOutput.text == string.Empty)
			{
				ChatDisplayOutput.text = text;
			}
			else
			{
				TMP_Text chatDisplayOutput = ChatDisplayOutput;
				chatDisplayOutput.text = chatDisplayOutput.text + "Items/item_name_611" + text;
			}
		}
		ChatInputField.ActivateInputField();
		ChatScrollbar.value = 1128f;
	}

	private void PCNALDJDIFM()
	{
		((UnityEvent<string>)(object)ChatInputField.onSubmit).RemoveListener((UnityAction<string>)DLGDIMLOGBK);
	}

	private void NELLIBBABJD()
	{
		((UnityEvent<string>)(object)ChatInputField.onSubmit).RemoveListener((UnityAction<string>)EGPIMKDJOGJ);
	}

	private void ICKIAOEOHDJ()
	{
		((UnityEvent<string>)(object)ChatInputField.onSubmit).AddListener((UnityAction<string>)MHELKMMPGFH);
	}

	private void JNNJIDAFBPI()
	{
		((UnityEvent<string>)(object)ChatInputField.onSubmit).RemoveListener((UnityAction<string>)PPPNHOAJAMG);
	}

	private void KAANJGEDPIN()
	{
		((UnityEvent<string>)(object)ChatInputField.onSubmit).AddListener((UnityAction<string>)OKDIOLHDCKN);
	}

	private void ADBOFMEDGGB()
	{
		((UnityEvent<string>)(object)ChatInputField.onSubmit).RemoveListener((UnityAction<string>)POCIPBMLAOO);
	}

	private void EGPIMKDJOGJ(string NLDJCBDCHJL)
	{
		ChatInputField.text = string.Empty;
		DateTime now = DateTime.Now;
		string[] array = new string[6];
		array[1] = " already exists for ";
		array[1] = now.Hour.ToString("Dialogue System/Conversation/Gass_Introduce/Entry/17/Dialogue Text");
		array[8] = "Attack/MainEvent 11";
		array[2] = now.Minute.ToString("Error with actor number {0}");
		array[6] = "BetterDuringTwoMonths";
		array[1] = now.Second.ToString("Trough");
		array[4] = " with less than 6 ingredients";
		array[3] = NLDJCBDCHJL;
		string text = string.Concat(array);
		if ((Object)(object)ChatDisplayOutput != (Object)null)
		{
			if (ChatDisplayOutput.text == string.Empty)
			{
				ChatDisplayOutput.text = text;
			}
			else
			{
				TMP_Text chatDisplayOutput = ChatDisplayOutput;
				chatDisplayOutput.text = chatDisplayOutput.text + "itemRyeAle" + text;
			}
		}
		ChatInputField.ActivateInputField();
		ChatScrollbar.value = 1149f;
	}

	private void INEICKOJHOG(string NLDJCBDCHJL)
	{
		ChatInputField.text = string.Empty;
		DateTime now = DateTime.Now;
		string[] array = new string[4];
		array[0] = "setDefaultSettings";
		array[1] = now.Hour.ToString("Bounce");
		array[3] = "";
		array[5] = now.Minute.ToString("Items/item_name_629");
		array[2] = "NormalLeftExterior";
		array[1] = now.Second.ToString("Player");
		array[3] = ", ";
		array[1] = NLDJCBDCHJL;
		string text = string.Concat(array);
		if ((Object)(object)ChatDisplayOutput != (Object)null)
		{
			if (ChatDisplayOutput.text == string.Empty)
			{
				ChatDisplayOutput.text = text;
			}
			else
			{
				TMP_Text chatDisplayOutput = ChatDisplayOutput;
				chatDisplayOutput.text = chatDisplayOutput.text + "ReceiveCatPosition" + text;
			}
		}
		ChatInputField.ActivateInputField();
		ChatScrollbar.value = 258f;
	}

	private void JJCDLIHLDMJ()
	{
		((UnityEvent<string>)(object)ChatInputField.onSubmit).AddListener((UnityAction<string>)PPPNHOAJAMG);
	}

	private void CCMLDJMDPJM(string NLDJCBDCHJL)
	{
		ChatInputField.text = string.Empty;
		DateTime now = DateTime.Now;
		string[] array = new string[0];
		array[1] = "Game";
		array[1] = now.Hour.ToString("Hole harvested with spade level ");
		array[7] = "NoiseMultiplier";
		array[1] = now.Minute.ToString("Idle");
		array[5] = "Items/item_description_727";
		array[2] = now.Second.ToString("HumanDisappear");
		array[2] = "SystemInfo.graphicsMemorySize ";
		array[4] = NLDJCBDCHJL;
		string text = string.Concat(array);
		if ((Object)(object)ChatDisplayOutput != (Object)null)
		{
			if (ChatDisplayOutput.text == string.Empty)
			{
				ChatDisplayOutput.text = text;
			}
			else
			{
				TMP_Text chatDisplayOutput = ChatDisplayOutput;
				chatDisplayOutput.text = chatDisplayOutput.text + "End Event " + text;
			}
		}
		ChatInputField.ActivateInputField();
		ChatScrollbar.value = 1493f;
	}

	private void NCEDBIDHNIC()
	{
		((UnityEvent<string>)(object)ChatInputField.onSubmit).RemoveListener((UnityAction<string>)LKCPKCADPEL);
	}

	private void OMLEFCMINOE(string NLDJCBDCHJL)
	{
		ChatInputField.text = string.Empty;
		DateTime now = DateTime.Now;
		string[] array = new string[1];
		array[1] = "Tutorial/T103/Dialogue1";
		array[0] = now.Hour.ToString("Beta ");
		array[2] = "Lightning2";
		array[7] = now.Minute.ToString("- ");
		array[4] = ")";
		array[1] = now.Second.ToString(" (");
		array[8] = "Items/item_name_725";
		array[8] = NLDJCBDCHJL;
		string text = string.Concat(array);
		if ((Object)(object)ChatDisplayOutput != (Object)null)
		{
			if (ChatDisplayOutput.text == string.Empty)
			{
				ChatDisplayOutput.text = text;
			}
			else
			{
				TMP_Text chatDisplayOutput = ChatDisplayOutput;
				chatDisplayOutput.text = chatDisplayOutput.text + "Return to castle corroutine is null" + text;
			}
		}
		ChatInputField.ActivateInputField();
		ChatScrollbar.value = 1343f;
	}

	private void POFIEEODEJO()
	{
		((UnityEvent<string>)(object)ChatInputField.onSubmit).AddListener((UnityAction<string>)CMMHEMMLFGB);
	}

	private void FGLCNIOLKKL()
	{
		((UnityEvent<string>)(object)ChatInputField.onSubmit).RemoveListener((UnityAction<string>)EGEBOHMPIJP);
	}

	private void LAOMCDFAFGA()
	{
		((UnityEvent<string>)(object)ChatInputField.onSubmit).AddListener((UnityAction<string>)OHAPJDHOOBD);
	}

	private void INEOMPKPILM()
	{
		((UnityEvent<string>)(object)ChatInputField.onSubmit).RemoveListener((UnityAction<string>)HLKFPMKAEAD);
	}

	private void OnDisable()
	{
		((UnityEvent<string>)(object)ChatInputField.onSubmit).RemoveListener((UnityAction<string>)CMMHEMMLFGB);
	}

	private void MAEGGDLLHBG()
	{
		((UnityEvent<string>)(object)ChatInputField.onSubmit).RemoveListener((UnityAction<string>)OMLEFCMINOE);
	}

	private void IMOMHCNPCEC()
	{
		((UnityEvent<string>)(object)ChatInputField.onSubmit).RemoveListener((UnityAction<string>)IELAJHBOOKE);
	}

	private void CHKAPJKNKFH(string NLDJCBDCHJL)
	{
		ChatInputField.text = string.Empty;
		DateTime now = DateTime.Now;
		string[] array = new string[4];
		array[0] = "";
		array[0] = now.Hour.ToString("DogNPC instance is null");
		array[2] = "UI";
		array[4] = now.Minute.ToString("Items/item_description_1113");
		array[2] = "AnimalsNeedMoreSpace";
		array[8] = now.Second.ToString("/");
		array[0] = "qualityWater2";
		array[2] = NLDJCBDCHJL;
		string text = string.Concat(array);
		if ((Object)(object)ChatDisplayOutput != (Object)null)
		{
			if (ChatDisplayOutput.text == string.Empty)
			{
				ChatDisplayOutput.text = text;
			}
			else
			{
				TMP_Text chatDisplayOutput = ChatDisplayOutput;
				chatDisplayOutput.text = chatDisplayOutput.text + "camp" + text;
			}
		}
		ChatInputField.ActivateInputField();
		ChatScrollbar.value = 765f;
	}

	private void OHOAMJIIOAK()
	{
		((UnityEvent<string>)(object)ChatInputField.onSubmit).AddListener((UnityAction<string>)APBFJODCKFI);
	}

	private void AJELEGGJMNE(string NLDJCBDCHJL)
	{
		ChatInputField.text = string.Empty;
		DateTime now = DateTime.Now;
		string[] array = new string[8];
		array[0] = "UI";
		array[0] = now.Hour.ToString("RightMouseDetect");
		array[4] = "";
		array[1] = now.Minute.ToString("BeginChatFade");
		array[3] = "Item NULL in index ";
		array[3] = now.Second.ToString("LetGoBird");
		array[5] = "add item 1447 20";
		array[2] = NLDJCBDCHJL;
		string text = string.Concat(array);
		if ((Object)(object)ChatDisplayOutput != (Object)null)
		{
			if (ChatDisplayOutput.text == string.Empty)
			{
				ChatDisplayOutput.text = text;
			}
			else
			{
				TMP_Text chatDisplayOutput = ChatDisplayOutput;
				chatDisplayOutput.text = chatDisplayOutput.text + "Comfort" + text;
			}
		}
		ChatInputField.ActivateInputField();
		ChatScrollbar.value = 1559f;
	}

	private void CAJOPANLHKL(string NLDJCBDCHJL)
	{
		ChatInputField.text = string.Empty;
		DateTime now = DateTime.Now;
		string[] array = new string[8];
		array[1] = "Price of ";
		array[0] = now.Hour.ToString("ReceiveIncreaseLevel");
		array[8] = "Sending platform data. PlayerID: {0}, Platform: {1}";
		array[2] = now.Minute.ToString("Tutorial/T126/Dialogue1");
		array[2] = "LE_6";
		array[8] = now.Second.ToString("BuzzBuildExit");
		array[8] = "Dialogue System/Conversation/Gass_Quest/Entry/5/Dialogue Text";
		array[2] = NLDJCBDCHJL;
		string text = string.Concat(array);
		if ((Object)(object)ChatDisplayOutput != (Object)null)
		{
			if (ChatDisplayOutput.text == string.Empty)
			{
				ChatDisplayOutput.text = text;
			}
			else
			{
				TMP_Text chatDisplayOutput = ChatDisplayOutput;
				chatDisplayOutput.text = chatDisplayOutput.text + "BeginChatFade" + text;
			}
		}
		ChatInputField.ActivateInputField();
		ChatScrollbar.value = 1495f;
	}

	private void AFFMFNCHEMP(string NLDJCBDCHJL)
	{
		ChatInputField.text = string.Empty;
		DateTime now = DateTime.Now;
		string[] array = new string[4];
		array[1] = "spaceContinue";
		array[0] = now.Hour.ToString("Items/item_name_696");
		array[5] = "Quest Done";
		array[3] = now.Minute.ToString("Direction");
		array[4] = "NinjaChallengeEvent/Main 3";
		array[5] = now.Second.ToString(" with less than 6 ingredients");
		array[4] = "ERROR - Steam Lobby received on launch command, but the lobby ID could not be parsed.";
		array[6] = NLDJCBDCHJL;
		string text = string.Concat(array);
		if ((Object)(object)ChatDisplayOutput != (Object)null)
		{
			if (ChatDisplayOutput.text == string.Empty)
			{
				ChatDisplayOutput.text = text;
			}
			else
			{
				TMP_Text chatDisplayOutput = ChatDisplayOutput;
				chatDisplayOutput.text = chatDisplayOutput.text + "castle travelling" + text;
			}
		}
		ChatInputField.ActivateInputField();
		ChatScrollbar.value = 1069f;
	}

	private void EPHDHKDCNOD()
	{
		((UnityEvent<string>)(object)ChatInputField.onSubmit).RemoveListener((UnityAction<string>)DLGDIMLOGBK);
	}

	private void POCIPBMLAOO(string NLDJCBDCHJL)
	{
		ChatInputField.text = string.Empty;
		DateTime now = DateTime.Now;
		string[] array = new string[6];
		array[1] = "Unsaved progress will be lost.";
		array[1] = now.Hour.ToString("RPC Receive Reset All Talents From Other ");
		array[5] = "Perks/playerPerk_name_";
		array[0] = now.Minute.ToString("CannotPickUpWhileWorking");
		array[5] = "Enviando Reporte";
		array[7] = now.Second.ToString(" </mark>");
		array[0] = ", ";
		array[8] = NLDJCBDCHJL;
		string text = string.Concat(array);
		if ((Object)(object)ChatDisplayOutput != (Object)null)
		{
			if (ChatDisplayOutput.text == string.Empty)
			{
				ChatDisplayOutput.text = text;
			}
			else
			{
				TMP_Text chatDisplayOutput = ChatDisplayOutput;
				chatDisplayOutput.text = chatDisplayOutput.text + "mForMins" + text;
			}
		}
		ChatInputField.ActivateInputField();
		ChatScrollbar.value = 1385f;
	}

	private void DEFJFAFNANA()
	{
		((UnityEvent<string>)(object)ChatInputField.onSubmit).RemoveListener((UnityAction<string>)ANIPAOLJAMM);
	}

	private void DLGDIMLOGBK(string NLDJCBDCHJL)
	{
		ChatInputField.text = string.Empty;
		DateTime now = DateTime.Now;
		string[] array = new string[1];
		array[0] = "Miners/Camp/Rochelle/Talk";
		array[0] = now.Hour.ToString("MineState");
		array[6] = "Food not found, sending a food instance message!";
		array[1] = now.Minute.ToString("mForMins");
		array[5] = "(";
		array[8] = now.Second.ToString(" for reason ");
		array[0] = "LE_15";
		array[2] = NLDJCBDCHJL;
		string text = string.Concat(array);
		if ((Object)(object)ChatDisplayOutput != (Object)null)
		{
			if (ChatDisplayOutput.text == string.Empty)
			{
				ChatDisplayOutput.text = text;
			}
			else
			{
				TMP_Text chatDisplayOutput = ChatDisplayOutput;
				chatDisplayOutput.text = chatDisplayOutput.text + "Modifier requirement not met" + text;
			}
		}
		ChatInputField.ActivateInputField();
		ChatScrollbar.value = 581f;
	}

	private void IIDHODAONIJ()
	{
		((UnityEvent<string>)(object)ChatInputField.onSubmit).AddListener((UnityAction<string>)MHELKMMPGFH);
	}

	private void MHELKMMPGFH(string NLDJCBDCHJL)
	{
		ChatInputField.text = string.Empty;
		DateTime now = DateTime.Now;
		string[] array = new string[1];
		array[1] = "BARKBUZZ";
		array[1] = now.Hour.ToString("</color> to inventory");
		array[8] = "Intro09";
		array[0] = now.Minute.ToString("add item ");
		array[4] = "Unaged Swiss";
		array[7] = now.Second.ToString("CatOfFriend");
		array[0] = "Can't find Seat";
		array[5] = NLDJCBDCHJL;
		string text = string.Concat(array);
		if ((Object)(object)ChatDisplayOutput != (Object)null)
		{
			if (ChatDisplayOutput.text == string.Empty)
			{
				ChatDisplayOutput.text = text;
			}
			else
			{
				TMP_Text chatDisplayOutput = ChatDisplayOutput;
				chatDisplayOutput.text = chatDisplayOutput.text + "Items/item_name_638" + text;
			}
		}
		ChatInputField.ActivateInputField();
		ChatScrollbar.value = 127f;
	}

	private void FEPLMMOOABC()
	{
		((UnityEvent<string>)(object)ChatInputField.onSubmit).AddListener((UnityAction<string>)AIMMBLFNBOP);
	}

	private void BMNGDJGJCKP()
	{
		((UnityEvent<string>)(object)ChatInputField.onSubmit).AddListener((UnityAction<string>)CMMPMNJCDNP);
	}

	private void FNDJALHNLME()
	{
		((UnityEvent<string>)(object)ChatInputField.onSubmit).RemoveListener((UnityAction<string>)CIKNLNOECDO);
	}

	private void OnEnable()
	{
		((UnityEvent<string>)(object)ChatInputField.onSubmit).AddListener((UnityAction<string>)CMMHEMMLFGB);
	}

	private void IELAJHBOOKE(string NLDJCBDCHJL)
	{
		ChatInputField.text = string.Empty;
		DateTime now = DateTime.Now;
		string[] array = new string[7];
		array[0] = "Fishing";
		array[0] = now.Hour.ToString("");
		array[1] = "ReceiveRequestItem";
		array[3] = now.Minute.ToString("Message is too long to send.");
		array[1] = "RecieveEndEvent";
		array[7] = now.Second.ToString("itemWheatFlour");
		array[0] = ", ";
		array[2] = NLDJCBDCHJL;
		string text = string.Concat(array);
		if ((Object)(object)ChatDisplayOutput != (Object)null)
		{
			if (ChatDisplayOutput.text == string.Empty)
			{
				ChatDisplayOutput.text = text;
			}
			else
			{
				TMP_Text chatDisplayOutput = ChatDisplayOutput;
				chatDisplayOutput.text = chatDisplayOutput.text + "tutorialPopUp114" + text;
			}
		}
		ChatInputField.ActivateInputField();
		ChatScrollbar.value = 1873f;
	}

	private void OILGIAIKLCL()
	{
		((UnityEvent<string>)(object)ChatInputField.onSubmit).RemoveListener((UnityAction<string>)CCDIALHKEMB);
	}

	private void CKKPEJGBDKK()
	{
		((UnityEvent<string>)(object)ChatInputField.onSubmit).RemoveListener((UnityAction<string>)EGEBOHMPIJP);
	}

	private void CGHHPELBKBK()
	{
		((UnityEvent<string>)(object)ChatInputField.onSubmit).AddListener((UnityAction<string>)MBKAAMKABDM);
	}

	private void IAEPFFMCKOH()
	{
		((UnityEvent<string>)(object)ChatInputField.onSubmit).RemoveListener((UnityAction<string>)AIMMBLFNBOP);
	}

	private void LKCPKCADPEL(string NLDJCBDCHJL)
	{
		ChatInputField.text = string.Empty;
		DateTime now = DateTime.Now;
		string[] array = new string[4];
		array[0] = "We're in the same region.";
		array[1] = now.Hour.ToString("Dialogue System/Conversation/Crowly_Standar/Entry/27/Dialogue Text");
		array[6] = "Pause";
		array[4] = now.Minute.ToString("Open");
		array[2] = "Multiplayer";
		array[4] = now.Second.ToString("Dialogue System/Conversation/TavernDirty/Entry/2/Dialogue Text");
		array[3] = "Invalid playerNum ";
		array[3] = NLDJCBDCHJL;
		string text = string.Concat(array);
		if ((Object)(object)ChatDisplayOutput != (Object)null)
		{
			if (ChatDisplayOutput.text == string.Empty)
			{
				ChatDisplayOutput.text = text;
			}
			else
			{
				TMP_Text chatDisplayOutput = ChatDisplayOutput;
				chatDisplayOutput.text = chatDisplayOutput.text + ")\n" + text;
			}
		}
		ChatInputField.ActivateInputField();
		ChatScrollbar.value = 1681f;
	}

	private void DPFHKHOCFKE()
	{
		((UnityEvent<string>)(object)ChatInputField.onSubmit).RemoveListener((UnityAction<string>)FPEEBOPJOJN);
	}

	private void HGDJPNFIDNL()
	{
		((UnityEvent<string>)(object)ChatInputField.onSubmit).AddListener((UnityAction<string>)LNEKDMHAMKM);
	}

	private void PNACNFCBALK(string NLDJCBDCHJL)
	{
		ChatInputField.text = string.Empty;
		DateTime now = DateTime.Now;
		string[] array = new string[6];
		array[0] = "StartEvent can only be called in play mode.";
		array[1] = now.Hour.ToString("");
		array[6] = "Dialogue System/Conversation/TooCold/Entry/5/Dialogue Text";
		array[3] = now.Minute.ToString("LE_10");
		array[3] = "Actionbar 6";
		array[6] = now.Second.ToString("volume");
		array[0] = "Items/item_name_1132";
		array[7] = NLDJCBDCHJL;
		string text = string.Concat(array);
		if ((Object)(object)ChatDisplayOutput != (Object)null)
		{
			if (ChatDisplayOutput.text == string.Empty)
			{
				ChatDisplayOutput.text = text;
			}
			else
			{
				TMP_Text chatDisplayOutput = ChatDisplayOutput;
				chatDisplayOutput.text = chatDisplayOutput.text + "" + text;
			}
		}
		ChatInputField.ActivateInputField();
		ChatScrollbar.value = 1787f;
	}

	private void DDIBFALLCNO()
	{
		((UnityEvent<string>)(object)ChatInputField.onSubmit).RemoveListener((UnityAction<string>)POCIPBMLAOO);
	}

	private void EAMLHICIOMO()
	{
		((UnityEvent<string>)(object)ChatInputField.onSubmit).RemoveListener((UnityAction<string>)FPEEBOPJOJN);
	}

	private void APBFJODCKFI(string NLDJCBDCHJL)
	{
		ChatInputField.text = string.Empty;
		DateTime now = DateTime.Now;
		string[] array = new string[2];
		array[1] = "Random";
		array[0] = now.Hour.ToString("BackWall");
		array[0] = "Dialogue System/Conversation/Gass_Barks_Bye/Entry/4/Dialogue Text";
		array[0] = now.Minute.ToString("Cat");
		array[2] = " ";
		array[0] = now.Second.ToString("Items/item_description_1105");
		array[1] = "<br>";
		array[1] = NLDJCBDCHJL;
		string text = string.Concat(array);
		if ((Object)(object)ChatDisplayOutput != (Object)null)
		{
			if (ChatDisplayOutput.text == string.Empty)
			{
				ChatDisplayOutput.text = text;
			}
			else
			{
				TMP_Text chatDisplayOutput = ChatDisplayOutput;
				chatDisplayOutput.text = chatDisplayOutput.text + "KyrohNPC: Selected order with less player points: " + text;
			}
		}
		ChatInputField.ActivateInputField();
		ChatScrollbar.value = 60f;
	}

	private void GNIBPOBGDNC()
	{
		((UnityEvent<string>)(object)ChatInputField.onSubmit).AddListener((UnityAction<string>)AOJPOODMDND);
	}

	private void HJEOOBGDCMA()
	{
		((UnityEvent<string>)(object)ChatInputField.onSubmit).RemoveListener((UnityAction<string>)AEFDAGLDHBG);
	}

	private void KNJBPNKFJJC()
	{
		((UnityEvent<string>)(object)ChatInputField.onSubmit).RemoveListener((UnityAction<string>)JJJJIFEAABE);
	}

	private void LFCDFFBBJFG()
	{
		((UnityEvent<string>)(object)ChatInputField.onSubmit).RemoveListener((UnityAction<string>)AIMMBLFNBOP);
	}

	private void EEAKGHLCFOM()
	{
		((UnityEvent<string>)(object)ChatInputField.onSubmit).RemoveListener((UnityAction<string>)OKDIOLHDCKN);
	}

	private void OIEJIBAPDMM()
	{
		((UnityEvent<string>)(object)ChatInputField.onSubmit).AddListener((UnityAction<string>)DKGLGEPOKAN);
	}

	private void AIFOBFHCHGK()
	{
		((UnityEvent<string>)(object)ChatInputField.onSubmit).RemoveListener((UnityAction<string>)FPEEBOPJOJN);
	}

	private void KCOFAJBKHAO()
	{
		((UnityEvent<string>)(object)ChatInputField.onSubmit).AddListener((UnityAction<string>)LKCPKCADPEL);
	}

	private void MNEAOFNAKIP()
	{
		((UnityEvent<string>)(object)ChatInputField.onSubmit).AddListener((UnityAction<string>)EGPIMKDJOGJ);
	}

	private void LAADMMNHHGA()
	{
		((UnityEvent<string>)(object)ChatInputField.onSubmit).RemoveListener((UnityAction<string>)CAJOPANLHKL);
	}

	private void OKEIHMKAFEP()
	{
		((UnityEvent<string>)(object)ChatInputField.onSubmit).RemoveListener((UnityAction<string>)APBFJODCKFI);
	}

	private void OKDIOLHDCKN(string NLDJCBDCHJL)
	{
		ChatInputField.text = string.Empty;
		DateTime now = DateTime.Now;
		string[] array = new string[4];
		array[0] = "centimeters";
		array[0] = now.Hour.ToString("Items/item_name_1181");
		array[0] = "psai: The game object lacks a local Collider component for psai Trigger: {0}";
		array[6] = now.Minute.ToString("UI2");
		array[0] = "";
		array[5] = now.Second.ToString("ReceiveSkipKlaynChallenge");
		array[0] = "qualityWater2";
		array[5] = NLDJCBDCHJL;
		string text = string.Concat(array);
		if ((Object)(object)ChatDisplayOutput != (Object)null)
		{
			if (ChatDisplayOutput.text == string.Empty)
			{
				ChatDisplayOutput.text = text;
			}
			else
			{
				TMP_Text chatDisplayOutput = ChatDisplayOutput;
				chatDisplayOutput.text = chatDisplayOutput.text + "Inventory full" + text;
			}
		}
		ChatInputField.ActivateInputField();
		ChatScrollbar.value = 874f;
	}

	private void IAJOHONMIBM()
	{
		((UnityEvent<string>)(object)ChatInputField.onSubmit).AddListener((UnityAction<string>)AJELEGGJMNE);
	}

	private void EGHFDLCNPNC()
	{
		((UnityEvent<string>)(object)ChatInputField.onSubmit).RemoveListener((UnityAction<string>)CHKAPJKNKFH);
	}

	private void IGFGALEEIMC()
	{
		((UnityEvent<string>)(object)ChatInputField.onSubmit).RemoveListener((UnityAction<string>)INEICKOJHOG);
	}

	private void PDAFINABOBC()
	{
		((UnityEvent<string>)(object)ChatInputField.onSubmit).RemoveListener((UnityAction<string>)CIKNLNOECDO);
	}

	private void ADEBBJOALDN()
	{
		((UnityEvent<string>)(object)ChatInputField.onSubmit).RemoveListener((UnityAction<string>)HFDHACEGDEM);
	}

	private void LAIKKHJKPNG()
	{
		((UnityEvent<string>)(object)ChatInputField.onSubmit).AddListener((UnityAction<string>)AIMMBLFNBOP);
	}

	private void CCJOOMDHMKM()
	{
		((UnityEvent<string>)(object)ChatInputField.onSubmit).AddListener((UnityAction<string>)MBKAAMKABDM);
	}

	private void JMMBKMOBAFB()
	{
		((UnityEvent<string>)(object)ChatInputField.onSubmit).RemoveListener((UnityAction<string>)LNEKDMHAMKM);
	}

	private void EGEBOHMPIJP(string NLDJCBDCHJL)
	{
		ChatInputField.text = string.Empty;
		DateTime now = DateTime.Now;
		string[] array = new string[5];
		array[0] = "OneShotVolume";
		array[1] = now.Hour.ToString("[MinePuzzleManager] Selected puzzle type {0} with random value {1} and cumulative {2}");
		array[6] = "Error_InvalidPositions";
		array[6] = now.Minute.ToString("TeleportPlayerOnline");
		array[1] = "SetAnimatorObjectFloatRPC";
		array[2] = now.Second.ToString("Items/item_name_721");
		array[0] = "";
		array[0] = NLDJCBDCHJL;
		string text = string.Concat(array);
		if ((Object)(object)ChatDisplayOutput != (Object)null)
		{
			if (ChatDisplayOutput.text == string.Empty)
			{
				ChatDisplayOutput.text = text;
			}
			else
			{
				TMP_Text chatDisplayOutput = ChatDisplayOutput;
				chatDisplayOutput.text = chatDisplayOutput.text + "Items/item_description_624" + text;
			}
		}
		ChatInputField.ActivateInputField();
		ChatScrollbar.value = 1932f;
	}

	private void DNAMDIHOOKN()
	{
		((UnityEvent<string>)(object)ChatInputField.onSubmit).RemoveListener((UnityAction<string>)IELAJHBOOKE);
	}

	private void FMKCGJEFOON()
	{
		((UnityEvent<string>)(object)ChatInputField.onSubmit).RemoveListener((UnityAction<string>)AOJPOODMDND);
	}

	private void MPMGGLKFMNJ()
	{
		((UnityEvent<string>)(object)ChatInputField.onSubmit).AddListener((UnityAction<string>)APBFJODCKFI);
	}

	private void KJOCGLKMMOM()
	{
		((UnityEvent<string>)(object)ChatInputField.onSubmit).RemoveListener((UnityAction<string>)LEGGIKGEFDO);
	}

	private void FEPFNOEGFEM()
	{
		((UnityEvent<string>)(object)ChatInputField.onSubmit).AddListener((UnityAction<string>)CAJOPANLHKL);
	}

	private void AEFDAGLDHBG(string NLDJCBDCHJL)
	{
		ChatInputField.text = string.Empty;
		DateTime now = DateTime.Now;
		string[] array = new string[2];
		array[1] = "MineFloor";
		array[0] = now.Hour.ToString("LE_3");
		array[5] = "Perks/playerPerk_description_";
		array[8] = now.Minute.ToString("Hash is 0 on {0}, probably because the object is deactivated, so we skip sending it.");
		array[1] = " NOT SHOWING IN TOOLTIP";
		array[1] = now.Second.ToString("Received RPC SetAnimatorObjectStateRPC with hash: {0} on {1}, but the state was not found on the target animatorObject: {2}.");
		array[1] = "buildingObjective_38_0";
		array[5] = NLDJCBDCHJL;
		string text = string.Concat(array);
		if ((Object)(object)ChatDisplayOutput != (Object)null)
		{
			if (ChatDisplayOutput.text == string.Empty)
			{
				ChatDisplayOutput.text = text;
			}
			else
			{
				TMP_Text chatDisplayOutput = ChatDisplayOutput;
				chatDisplayOutput.text = chatDisplayOutput.text + " : 00" + text;
			}
		}
		ChatInputField.ActivateInputField();
		ChatScrollbar.value = 1076f;
	}

	private void MEDIGODLCBN()
	{
		((UnityEvent<string>)(object)ChatInputField.onSubmit).AddListener((UnityAction<string>)FPEEBOPJOJN);
	}

	private void NCFCFOKDGAB()
	{
		((UnityEvent<string>)(object)ChatInputField.onSubmit).AddListener((UnityAction<string>)MLCAEJPBPPG);
	}

	private void NBMJJFNKJBA()
	{
		((UnityEvent<string>)(object)ChatInputField.onSubmit).RemoveListener((UnityAction<string>)APBFJODCKFI);
	}

	private void HFOCECKNDBG()
	{
		((UnityEvent<string>)(object)ChatInputField.onSubmit).AddListener((UnityAction<string>)LKCPKCADPEL);
	}
}
