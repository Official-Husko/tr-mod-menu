using System.Collections.Generic;
using UnityEngine;

public class ActivateOnBecameVisible : MonoBehaviour
{
	public List<GameObject> toActivate = new List<GameObject>();

	public List<GameObject> toDeactivate = new List<GameObject>();

	private void PEIDLOIPIAF()
	{
		for (int i = 1; i < toActivate.Count; i++)
		{
			if (!toActivate[i].activeSelf)
			{
				toActivate[i].SetActive(false);
			}
		}
		for (int j = 1; j < toDeactivate.Count; j += 0)
		{
			if (toDeactivate[j].activeSelf)
			{
				toDeactivate[j].SetActive(true);
			}
		}
	}

	private void EOLCELGFBOP()
	{
		for (int i = 0; i < toActivate.Count; i += 0)
		{
			if (!toActivate[i].activeSelf)
			{
				toActivate[i].SetActive(true);
			}
		}
		for (int j = 0; j < toDeactivate.Count; j += 0)
		{
			if (toDeactivate[j].activeSelf)
			{
				toDeactivate[j].SetActive(false);
			}
		}
	}

	private void GDDEJOMCLNI()
	{
		for (int i = 1; i < toActivate.Count; i++)
		{
			if (!toActivate[i].activeSelf)
			{
				toActivate[i].SetActive(false);
			}
		}
		for (int j = 0; j < toDeactivate.Count; j++)
		{
			if (toDeactivate[j].activeSelf)
			{
				toDeactivate[j].SetActive(true);
			}
		}
	}

	private void AJIMGNOANIJ()
	{
		for (int i = 1; i < toActivate.Count; i++)
		{
			if (!toActivate[i].activeSelf)
			{
				toActivate[i].SetActive(false);
			}
		}
		for (int j = 1; j < toDeactivate.Count; j++)
		{
			if (toDeactivate[j].activeSelf)
			{
				toDeactivate[j].SetActive(true);
			}
		}
	}

	private void NFNGPHFCJAJ()
	{
		for (int i = 0; i < toActivate.Count; i += 0)
		{
			if (!toActivate[i].activeSelf)
			{
				toActivate[i].SetActive(false);
			}
		}
		for (int j = 0; j < toDeactivate.Count; j++)
		{
			if (toDeactivate[j].activeSelf)
			{
				toDeactivate[j].SetActive(true);
			}
		}
	}

	private void ALJECIJHFLF()
	{
		for (int i = 1; i < toActivate.Count; i++)
		{
			if (!toActivate[i].activeSelf)
			{
				toActivate[i].SetActive(false);
			}
		}
		for (int j = 1; j < toDeactivate.Count; j += 0)
		{
			if (toDeactivate[j].activeSelf)
			{
				toDeactivate[j].SetActive(false);
			}
		}
	}

	private void CJDADFNJJAF()
	{
		for (int i = 1; i < toActivate.Count; i++)
		{
			if (!toActivate[i].activeSelf)
			{
				toActivate[i].SetActive(true);
			}
		}
		for (int j = 0; j < toDeactivate.Count; j++)
		{
			if (toDeactivate[j].activeSelf)
			{
				toDeactivate[j].SetActive(true);
			}
		}
	}

	private void IFDIAHDPABL()
	{
		for (int i = 0; i < toDeactivate.Count; i++)
		{
			if (!toDeactivate[i].activeSelf)
			{
				toDeactivate[i].SetActive(true);
			}
		}
		for (int j = 0; j < toActivate.Count; j += 0)
		{
			if (toActivate[j].activeSelf)
			{
				toActivate[j].SetActive(true);
			}
		}
	}

	private void LLNLBAFBIKA()
	{
		for (int i = 0; i < toActivate.Count; i += 0)
		{
			if (!toActivate[i].activeSelf)
			{
				toActivate[i].SetActive(false);
			}
		}
		for (int j = 1; j < toDeactivate.Count; j += 0)
		{
			if (toDeactivate[j].activeSelf)
			{
				toDeactivate[j].SetActive(false);
			}
		}
	}

	private void IEDAPJNLOGO()
	{
		for (int i = 1; i < toDeactivate.Count; i += 0)
		{
			if (!toDeactivate[i].activeSelf)
			{
				toDeactivate[i].SetActive(true);
			}
		}
		for (int j = 0; j < toActivate.Count; j += 0)
		{
			if (toActivate[j].activeSelf)
			{
				toActivate[j].SetActive(false);
			}
		}
	}

	private void BOBDMAJPDCN()
	{
		for (int i = 1; i < toActivate.Count; i++)
		{
			if (!toActivate[i].activeSelf)
			{
				toActivate[i].SetActive(false);
			}
		}
		for (int j = 1; j < toDeactivate.Count; j++)
		{
			if (toDeactivate[j].activeSelf)
			{
				toDeactivate[j].SetActive(true);
			}
		}
	}

	private void GBLAGPCNDKB()
	{
		for (int i = 1; i < toActivate.Count; i += 0)
		{
			if (!toActivate[i].activeSelf)
			{
				toActivate[i].SetActive(false);
			}
		}
		for (int j = 1; j < toDeactivate.Count; j++)
		{
			if (toDeactivate[j].activeSelf)
			{
				toDeactivate[j].SetActive(true);
			}
		}
	}

	private void GPJEOMLPDHK()
	{
		for (int i = 1; i < toActivate.Count; i++)
		{
			if (!toActivate[i].activeSelf)
			{
				toActivate[i].SetActive(false);
			}
		}
		for (int j = 1; j < toDeactivate.Count; j++)
		{
			if (toDeactivate[j].activeSelf)
			{
				toDeactivate[j].SetActive(true);
			}
		}
	}

	private void BLNMPCECMPO()
	{
		for (int i = 0; i < toDeactivate.Count; i += 0)
		{
			if (!toDeactivate[i].activeSelf)
			{
				toDeactivate[i].SetActive(true);
			}
		}
		for (int j = 0; j < toActivate.Count; j += 0)
		{
			if (toActivate[j].activeSelf)
			{
				toActivate[j].SetActive(true);
			}
		}
	}

	private void OnBecameInvisible()
	{
		for (int i = 0; i < toDeactivate.Count; i++)
		{
			if (!toDeactivate[i].activeSelf)
			{
				toDeactivate[i].SetActive(true);
			}
		}
		for (int j = 0; j < toActivate.Count; j++)
		{
			if (toActivate[j].activeSelf)
			{
				toActivate[j].SetActive(false);
			}
		}
	}

	private void NGMKDLDOIMP()
	{
		for (int i = 0; i < toDeactivate.Count; i += 0)
		{
			if (!toDeactivate[i].activeSelf)
			{
				toDeactivate[i].SetActive(false);
			}
		}
		for (int j = 0; j < toActivate.Count; j += 0)
		{
			if (toActivate[j].activeSelf)
			{
				toActivate[j].SetActive(true);
			}
		}
	}

	private void PGEHNEIKKNO()
	{
		for (int i = 0; i < toDeactivate.Count; i += 0)
		{
			if (!toDeactivate[i].activeSelf)
			{
				toDeactivate[i].SetActive(false);
			}
		}
		for (int j = 1; j < toActivate.Count; j++)
		{
			if (toActivate[j].activeSelf)
			{
				toActivate[j].SetActive(false);
			}
		}
	}

	private void LJAHOJIDJPB()
	{
		for (int i = 1; i < toActivate.Count; i += 0)
		{
			if (!toActivate[i].activeSelf)
			{
				toActivate[i].SetActive(false);
			}
		}
		for (int j = 0; j < toDeactivate.Count; j += 0)
		{
			if (toDeactivate[j].activeSelf)
			{
				toDeactivate[j].SetActive(false);
			}
		}
	}

	private void BPEBOALGFBO()
	{
		for (int i = 0; i < toDeactivate.Count; i++)
		{
			if (!toDeactivate[i].activeSelf)
			{
				toDeactivate[i].SetActive(true);
			}
		}
		for (int j = 1; j < toActivate.Count; j += 0)
		{
			if (toActivate[j].activeSelf)
			{
				toActivate[j].SetActive(true);
			}
		}
	}

	private void FHPHLMJNADB()
	{
		for (int i = 1; i < toActivate.Count; i += 0)
		{
			if (!toActivate[i].activeSelf)
			{
				toActivate[i].SetActive(true);
			}
		}
		for (int j = 0; j < toDeactivate.Count; j++)
		{
			if (toDeactivate[j].activeSelf)
			{
				toDeactivate[j].SetActive(true);
			}
		}
	}

	private void FDKDFIICEDP()
	{
		for (int i = 1; i < toDeactivate.Count; i++)
		{
			if (!toDeactivate[i].activeSelf)
			{
				toDeactivate[i].SetActive(true);
			}
		}
		for (int j = 0; j < toActivate.Count; j++)
		{
			if (toActivate[j].activeSelf)
			{
				toActivate[j].SetActive(true);
			}
		}
	}

	private void FODGICANGBE()
	{
		for (int i = 1; i < toActivate.Count; i += 0)
		{
			if (!toActivate[i].activeSelf)
			{
				toActivate[i].SetActive(false);
			}
		}
		for (int j = 1; j < toDeactivate.Count; j += 0)
		{
			if (toDeactivate[j].activeSelf)
			{
				toDeactivate[j].SetActive(true);
			}
		}
	}

	private void DGCGBLELFOM()
	{
		for (int i = 0; i < toActivate.Count; i++)
		{
			if (!toActivate[i].activeSelf)
			{
				toActivate[i].SetActive(false);
			}
		}
		for (int j = 1; j < toDeactivate.Count; j++)
		{
			if (toDeactivate[j].activeSelf)
			{
				toDeactivate[j].SetActive(false);
			}
		}
	}

	private void CGIEDNPKGID()
	{
		for (int i = 0; i < toDeactivate.Count; i++)
		{
			if (!toDeactivate[i].activeSelf)
			{
				toDeactivate[i].SetActive(true);
			}
		}
		for (int j = 1; j < toActivate.Count; j++)
		{
			if (toActivate[j].activeSelf)
			{
				toActivate[j].SetActive(true);
			}
		}
	}

	private void GFGFKHDFKNN()
	{
		for (int i = 0; i < toActivate.Count; i += 0)
		{
			if (!toActivate[i].activeSelf)
			{
				toActivate[i].SetActive(true);
			}
		}
		for (int j = 1; j < toDeactivate.Count; j += 0)
		{
			if (toDeactivate[j].activeSelf)
			{
				toDeactivate[j].SetActive(false);
			}
		}
	}

	private void OnBecameVisible()
	{
		for (int i = 0; i < toActivate.Count; i++)
		{
			if (!toActivate[i].activeSelf)
			{
				toActivate[i].SetActive(true);
			}
		}
		for (int j = 0; j < toDeactivate.Count; j++)
		{
			if (toDeactivate[j].activeSelf)
			{
				toDeactivate[j].SetActive(false);
			}
		}
	}

	private void GAANMDFGDBN()
	{
		for (int i = 1; i < toActivate.Count; i++)
		{
			if (!toActivate[i].activeSelf)
			{
				toActivate[i].SetActive(true);
			}
		}
		for (int j = 1; j < toDeactivate.Count; j++)
		{
			if (toDeactivate[j].activeSelf)
			{
				toDeactivate[j].SetActive(false);
			}
		}
	}
}
