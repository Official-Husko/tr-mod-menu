using AlmenaraGames;
using UnityEngine;

public class PlaySoundByTime : MonoBehaviour
{
	[SerializeField]
	private AudioObject soundToPlay;

	[SerializeField]
	private float minTime;

	[SerializeField]
	private float maxTime;

	private float HECFFFDEELC;

	private void CMDHELEDLBP()
	{
		//IL_0019: Unknown result type (might be due to invalid IL or missing references)
		if (Time.time >= HECFFFDEELC)
		{
			MultiAudioManager.PlayAudioObject(soundToPlay, ((Component)this).transform.position);
			JNEFEINJKLN();
		}
	}

	private void MKHMCIFFOFJ()
	{
		GBLNIKAFCAF();
	}

	private void DEGOEEKPJMF()
	{
		HECFFFDEELC = Time.time + Random.Range(minTime, maxTime);
	}

	private void CCMCOLCHIDO()
	{
		HECFFFDEELC = Time.time + Random.Range(minTime, maxTime);
	}

	private void HHIHDANGJON()
	{
		HECFFFDEELC = Time.time + Random.Range(minTime, maxTime);
	}

	private void DHOLOFJCKEM()
	{
		HECFFFDEELC = Time.time + Random.Range(minTime, maxTime);
	}

	private void IJNPEAAOOEK()
	{
		DFMEKCIBJIL();
	}

	private void BPNOIDBAPJE()
	{
		BAIKKKFCLDE();
	}

	private void PEIFJDIGKOC()
	{
		//IL_0019: Unknown result type (might be due to invalid IL or missing references)
		if (Time.time >= HECFFFDEELC)
		{
			MultiAudioManager.PlayAudioObject(soundToPlay, ((Component)this).transform.position);
			ECBBBOCPODM();
		}
	}

	private void IBAPBHIBOOO()
	{
		//IL_0019: Unknown result type (might be due to invalid IL or missing references)
		if (Time.time >= HECFFFDEELC)
		{
			MultiAudioManager.PlayAudioObject(soundToPlay, ((Component)this).transform.position);
			EAFCFCNAHLO();
		}
	}

	private void AKLEFPLEGKK()
	{
		//IL_0019: Unknown result type (might be due to invalid IL or missing references)
		if (Time.time >= HECFFFDEELC)
		{
			MultiAudioManager.PlayAudioObject(soundToPlay, ((Component)this).transform.position);
			HHIHDANGJON();
		}
	}

	private void EJKLEJFJKNL()
	{
		HECFFFDEELC = Time.time + Random.Range(minTime, maxTime);
	}

	private void NIHJCJJKDBA()
	{
		//IL_0019: Unknown result type (might be due to invalid IL or missing references)
		if (Time.time >= HECFFFDEELC)
		{
			MultiAudioManager.PlayAudioObject(soundToPlay, ((Component)this).transform.position);
			AKOGOINOCIG();
		}
	}

	private void LFBIFPBPDMJ()
	{
		LHDFAPINLBH();
	}

	private void DBKCPJIKFFC()
	{
		//IL_0019: Unknown result type (might be due to invalid IL or missing references)
		if (Time.time >= HECFFFDEELC)
		{
			MultiAudioManager.PlayAudioObject(soundToPlay, ((Component)this).transform.position);
			HCAFMBIGBAP();
		}
	}

	private void EBNJADODONG()
	{
		//IL_0019: Unknown result type (might be due to invalid IL or missing references)
		if (Time.time >= HECFFFDEELC)
		{
			MultiAudioManager.PlayAudioObject(soundToPlay, ((Component)this).transform.position);
			LOBGOBNOFMG();
		}
	}

	private void GBLNIKAFCAF()
	{
		HECFFFDEELC = Time.time + Random.Range(minTime, maxTime);
	}

	private void NCFCFOKDGAB()
	{
		DHOLOFJCKEM();
	}

	private void BMNGDJGJCKP()
	{
		EAFCFCNAHLO();
	}

	private void CBGPCEJFCHB()
	{
		EDMLLEOCACB();
	}

	private void HGDJPNFIDNL()
	{
		EJKLEJFJKNL();
	}

	private void IANGDMHMCEL()
	{
		DEGOEEKPJMF();
	}

	private void DHBMBJOLGDE()
	{
		//IL_0019: Unknown result type (might be due to invalid IL or missing references)
		if (Time.time >= HECFFFDEELC)
		{
			MultiAudioManager.PlayAudioObject(soundToPlay, ((Component)this).transform.position);
			KGDBNBOGEJC();
		}
	}

	private void AMHGMJDKONK()
	{
		DMHLJBBNHHA();
	}

	private void ADGMAKKMINA()
	{
		CDLJCBOKIIP();
	}

	private void CLOKMCONOMN()
	{
		//IL_0019: Unknown result type (might be due to invalid IL or missing references)
		if (Time.time >= HECFFFDEELC)
		{
			MultiAudioManager.PlayAudioObject(soundToPlay, ((Component)this).transform.position);
			DFMEKCIBJIL();
		}
	}

	private void FCLABOCLACJ()
	{
		KGDBNBOGEJC();
	}

	private void AGJJJFDBCCL()
	{
		HECFFFDEELC = Time.time + Random.Range(minTime, maxTime);
	}

	private void OHOAMJIIOAK()
	{
		KGDBNBOGEJC();
	}

	private void FJEGHEIHHHK()
	{
		HECFFFDEELC = Time.time + Random.Range(minTime, maxTime);
	}

	private void AGGJDFHNPPI()
	{
		//IL_0019: Unknown result type (might be due to invalid IL or missing references)
		if (Time.time >= HECFFFDEELC)
		{
			MultiAudioManager.PlayAudioObject(soundToPlay, ((Component)this).transform.position);
			CCMCOLCHIDO();
		}
	}

	private void OnEnable()
	{
		HCAFMBIGBAP();
	}

	private void MFIBFFDBNGI()
	{
		//IL_0019: Unknown result type (might be due to invalid IL or missing references)
		if (Time.time >= HECFFFDEELC)
		{
			MultiAudioManager.PlayAudioObject(soundToPlay, ((Component)this).transform.position);
			EJKLEJFJKNL();
		}
	}

	private void GDLAHKEMJFN()
	{
		KFONABKKHCK();
	}

	private void GIKLGOHHPCF()
	{
		HECFFFDEELC = Time.time + Random.Range(minTime, maxTime);
	}

	private void PHLLBPLOLFO()
	{
		//IL_0019: Unknown result type (might be due to invalid IL or missing references)
		if (Time.time >= HECFFFDEELC)
		{
			MultiAudioManager.PlayAudioObject(soundToPlay, ((Component)this).transform.position);
			AGJJJFDBCCL();
		}
	}

	private void DOKHLDBIDJB()
	{
		//IL_0019: Unknown result type (might be due to invalid IL or missing references)
		if (Time.time >= HECFFFDEELC)
		{
			MultiAudioManager.PlayAudioObject(soundToPlay, ((Component)this).transform.position);
			EDMLLEOCACB();
		}
	}

	private void ICMNODPLPBK()
	{
		//IL_0019: Unknown result type (might be due to invalid IL or missing references)
		if (Time.time >= HECFFFDEELC)
		{
			MultiAudioManager.PlayAudioObject(soundToPlay, ((Component)this).transform.position);
			PPJAONDBGBB();
		}
	}

	private void EAFCFCNAHLO()
	{
		HECFFFDEELC = Time.time + Random.Range(minTime, maxTime);
	}

	private void KFBOPABEJNL()
	{
		//IL_0019: Unknown result type (might be due to invalid IL or missing references)
		if (Time.time >= HECFFFDEELC)
		{
			MultiAudioManager.PlayAudioObject(soundToPlay, ((Component)this).transform.position);
			FEKLMJNCHMK();
		}
	}

	private void PJOAFKEIPPD()
	{
		EMIAONMBCEJ();
	}

	private void GHEHLJANBCF()
	{
		//IL_0019: Unknown result type (might be due to invalid IL or missing references)
		if (Time.time >= HECFFFDEELC)
		{
			MultiAudioManager.PlayAudioObject(soundToPlay, ((Component)this).transform.position);
			DHOLOFJCKEM();
		}
	}

	private void OMLHIAADEHE()
	{
		//IL_0019: Unknown result type (might be due to invalid IL or missing references)
		if (Time.time >= HECFFFDEELC)
		{
			MultiAudioManager.PlayAudioObject(soundToPlay, ((Component)this).transform.position);
			AMMMOBPINMK();
		}
	}

	private void CKCHKHNBBFG()
	{
		//IL_0019: Unknown result type (might be due to invalid IL or missing references)
		if (Time.time >= HECFFFDEELC)
		{
			MultiAudioManager.PlayAudioObject(soundToPlay, ((Component)this).transform.position);
			EDMLLEOCACB();
		}
	}

	private void FEKLMJNCHMK()
	{
		HECFFFDEELC = Time.time + Random.Range(minTime, maxTime);
	}

	private void ONOAGANPECB()
	{
		//IL_0019: Unknown result type (might be due to invalid IL or missing references)
		if (Time.time >= HECFFFDEELC)
		{
			MultiAudioManager.PlayAudioObject(soundToPlay, ((Component)this).transform.position);
			PPJAONDBGBB();
		}
	}

	private void MEGIEHCJJMD()
	{
		HECFFFDEELC = Time.time + Random.Range(minTime, maxTime);
	}

	private void LDMDHDACNFJ()
	{
		//IL_0019: Unknown result type (might be due to invalid IL or missing references)
		if (Time.time >= HECFFFDEELC)
		{
			MultiAudioManager.PlayAudioObject(soundToPlay, ((Component)this).transform.position);
			HNGCKLOKBAN();
		}
	}

	private void BJOOCKCBPLJ()
	{
		FJEGHEIHHHK();
	}

	private void ECBBBOCPODM()
	{
		HECFFFDEELC = Time.time + Random.Range(minTime, maxTime);
	}

	private void Update()
	{
		//IL_0019: Unknown result type (might be due to invalid IL or missing references)
		if (Time.time >= HECFFFDEELC)
		{
			MultiAudioManager.PlayAudioObject(soundToPlay, ((Component)this).transform.position);
			HCAFMBIGBAP();
		}
	}

	private void ONNAGOCFJML()
	{
		OALEPGKKIDH();
	}

	private void DLNOLBHCIGC()
	{
		//IL_0019: Unknown result type (might be due to invalid IL or missing references)
		if (Time.time >= HECFFFDEELC)
		{
			MultiAudioManager.PlayAudioObject(soundToPlay, ((Component)this).transform.position);
			DKICIFNOKKD();
		}
	}

	private void PIDEIIIJLKF()
	{
		DEGOEEKPJMF();
	}

	private void AKOGOINOCIG()
	{
		HECFFFDEELC = Time.time + Random.Range(minTime, maxTime);
	}

	private void AMMMOBPINMK()
	{
		HECFFFDEELC = Time.time + Random.Range(minTime, maxTime);
	}

	private void KFONABKKHCK()
	{
		HECFFFDEELC = Time.time + Random.Range(minTime, maxTime);
	}

	private void FPNPLFALPHP()
	{
		EJKLEJFJKNL();
	}

	private void LEPDLPEPEFD()
	{
		EAFCFCNAHLO();
	}

	private void JMDOCHBJGKG()
	{
		//IL_0019: Unknown result type (might be due to invalid IL or missing references)
		if (Time.time >= HECFFFDEELC)
		{
			MultiAudioManager.PlayAudioObject(soundToPlay, ((Component)this).transform.position);
			DHOLOFJCKEM();
		}
	}

	private void AAOMCHHNKKM()
	{
		//IL_0019: Unknown result type (might be due to invalid IL or missing references)
		if (Time.time >= HECFFFDEELC)
		{
			MultiAudioManager.PlayAudioObject(soundToPlay, ((Component)this).transform.position);
			EDMLLEOCACB();
		}
	}

	private void ALNPAFIBJGN()
	{
		//IL_0019: Unknown result type (might be due to invalid IL or missing references)
		if (Time.time >= HECFFFDEELC)
		{
			MultiAudioManager.PlayAudioObject(soundToPlay, ((Component)this).transform.position);
			HCAFMBIGBAP();
		}
	}

	private void OJLGPAJPCHG()
	{
		FJEGHEIHHHK();
	}

	private void JINLGFOFJAN()
	{
		CCMCOLCHIDO();
	}

	private void PGDBFGFMGKI()
	{
		MKEGMPDGDDI();
	}

	private void FDHMOEDMPNG()
	{
		//IL_0019: Unknown result type (might be due to invalid IL or missing references)
		if (Time.time >= HECFFFDEELC)
		{
			MultiAudioManager.PlayAudioObject(soundToPlay, ((Component)this).transform.position);
			ACFPBLBMHOC();
		}
	}

	private void GKJMBDNHOID()
	{
		HECFFFDEELC = Time.time + Random.Range(minTime, maxTime);
	}

	private void DMHLJBBNHHA()
	{
		HECFFFDEELC = Time.time + Random.Range(minTime, maxTime);
	}

	private void FDJIDOCMLHB()
	{
		DMHLJBBNHHA();
	}

	private void LCIIKDILJJP()
	{
		HECFFFDEELC = Time.time + Random.Range(minTime, maxTime);
	}

	private void GOKBDDFNDFH()
	{
		//IL_0019: Unknown result type (might be due to invalid IL or missing references)
		if (Time.time >= HECFFFDEELC)
		{
			MultiAudioManager.PlayAudioObject(soundToPlay, ((Component)this).transform.position);
			BAIKKKFCLDE();
		}
	}

	private void AMBJKHAFAFF()
	{
		//IL_0019: Unknown result type (might be due to invalid IL or missing references)
		if (Time.time >= HECFFFDEELC)
		{
			MultiAudioManager.PlayAudioObject(soundToPlay, ((Component)this).transform.position);
			EMIAONMBCEJ();
		}
	}

	private void CDLJCBOKIIP()
	{
		HECFFFDEELC = Time.time + Random.Range(minTime, maxTime);
	}

	private void MEDIGODLCBN()
	{
		OALEPGKKIDH();
	}

	private void AFGJLAACCFN()
	{
		GBLNIKAFCAF();
	}

	private void MPMGGLKFMNJ()
	{
		ECBBBOCPODM();
	}

	private void KGDBNBOGEJC()
	{
		HECFFFDEELC = Time.time + Random.Range(minTime, maxTime);
	}

	private void MFPEIPCMEEB()
	{
		//IL_0019: Unknown result type (might be due to invalid IL or missing references)
		if (Time.time >= HECFFFDEELC)
		{
			MultiAudioManager.PlayAudioObject(soundToPlay, ((Component)this).transform.position);
			EAFCFCNAHLO();
		}
	}

	private void GGPDPOAICOM()
	{
		//IL_0019: Unknown result type (might be due to invalid IL or missing references)
		if (Time.time >= HECFFFDEELC)
		{
			MultiAudioManager.PlayAudioObject(soundToPlay, ((Component)this).transform.position);
			EAGHAALGAIE();
		}
	}

	private void LOBGOBNOFMG()
	{
		HECFFFDEELC = Time.time + Random.Range(minTime, maxTime);
	}

	private void PPJAONDBGBB()
	{
		HECFFFDEELC = Time.time + Random.Range(minTime, maxTime);
	}

	private void FEPFNOEGFEM()
	{
		CCMCOLCHIDO();
	}

	private void MNFJELNEGPG()
	{
		//IL_0019: Unknown result type (might be due to invalid IL or missing references)
		if (Time.time >= HECFFFDEELC)
		{
			MultiAudioManager.PlayAudioObject(soundToPlay, ((Component)this).transform.position);
			PPJAONDBGBB();
		}
	}

	private void LEDIGKGAFHC()
	{
		//IL_0019: Unknown result type (might be due to invalid IL or missing references)
		if (Time.time >= HECFFFDEELC)
		{
			MultiAudioManager.PlayAudioObject(soundToPlay, ((Component)this).transform.position);
			BAIKKKFCLDE();
		}
	}

	private void MMPMJNAFKHC()
	{
		//IL_0019: Unknown result type (might be due to invalid IL or missing references)
		if (Time.time >= HECFFFDEELC)
		{
			MultiAudioManager.PlayAudioObject(soundToPlay, ((Component)this).transform.position);
			HHIHDANGJON();
		}
	}

	private void PMPLNGEAAII()
	{
		//IL_0019: Unknown result type (might be due to invalid IL or missing references)
		if (Time.time >= HECFFFDEELC)
		{
			MultiAudioManager.PlayAudioObject(soundToPlay, ((Component)this).transform.position);
			HCAFMBIGBAP();
		}
	}

	private void EDMLLEOCACB()
	{
		HECFFFDEELC = Time.time + Random.Range(minTime, maxTime);
	}

	private void HFOCECKNDBG()
	{
		PPJAONDBGBB();
	}

	private void EMIAONMBCEJ()
	{
		HECFFFDEELC = Time.time + Random.Range(minTime, maxTime);
	}

	private void DFMEKCIBJIL()
	{
		HECFFFDEELC = Time.time + Random.Range(minTime, maxTime);
	}

	private void MLBIGONNDGO()
	{
		//IL_0019: Unknown result type (might be due to invalid IL or missing references)
		if (Time.time >= HECFFFDEELC)
		{
			MultiAudioManager.PlayAudioObject(soundToPlay, ((Component)this).transform.position);
			MEGIEHCJJMD();
		}
	}

	private void JNEFEINJKLN()
	{
		HECFFFDEELC = Time.time + Random.Range(minTime, maxTime);
	}

	private void JLPLLHKJEMH()
	{
		//IL_0019: Unknown result type (might be due to invalid IL or missing references)
		if (Time.time >= HECFFFDEELC)
		{
			MultiAudioManager.PlayAudioObject(soundToPlay, ((Component)this).transform.position);
			LCIIKDILJJP();
		}
	}

	private void JHKLCLMMOGC()
	{
		FEKLMJNCHMK();
	}

	private void AONIDCIEKCM()
	{
		MEGIEHCJJMD();
	}

	private void OGBGFLMKFHH()
	{
		//IL_0019: Unknown result type (might be due to invalid IL or missing references)
		if (Time.time >= HECFFFDEELC)
		{
			MultiAudioManager.PlayAudioObject(soundToPlay, ((Component)this).transform.position);
			DEGOEEKPJMF();
		}
	}

	private void ONJAODIAAIM()
	{
		KGDBNBOGEJC();
	}

	private void GNIBPOBGDNC()
	{
		DEGOEEKPJMF();
	}

	private void FFCPCKACFNJ()
	{
		HECFFFDEELC = Time.time + Random.Range(minTime, maxTime);
	}

	private void IGKPEBGHPGD()
	{
		//IL_0019: Unknown result type (might be due to invalid IL or missing references)
		if (Time.time >= HECFFFDEELC)
		{
			MultiAudioManager.PlayAudioObject(soundToPlay, ((Component)this).transform.position);
			FEKLMJNCHMK();
		}
	}

	private void BAIKKKFCLDE()
	{
		HECFFFDEELC = Time.time + Random.Range(minTime, maxTime);
	}

	private void MHCOLHEDNGN()
	{
		//IL_0019: Unknown result type (might be due to invalid IL or missing references)
		if (Time.time >= HECFFFDEELC)
		{
			MultiAudioManager.PlayAudioObject(soundToPlay, ((Component)this).transform.position);
			MKEGMPDGDDI();
		}
	}

	private void LHDFAPINLBH()
	{
		HECFFFDEELC = Time.time + Random.Range(minTime, maxTime);
	}

	private void PBKGBPCKOGB()
	{
		//IL_0019: Unknown result type (might be due to invalid IL or missing references)
		if (Time.time >= HECFFFDEELC)
		{
			MultiAudioManager.PlayAudioObject(soundToPlay, ((Component)this).transform.position);
			EJKLEJFJKNL();
		}
	}

	private void ACFPBLBMHOC()
	{
		HECFFFDEELC = Time.time + Random.Range(minTime, maxTime);
	}

	private void JIBHLLJGGEN()
	{
		EJKLEJFJKNL();
	}

	private void JPHFCJIBBMI()
	{
		//IL_0019: Unknown result type (might be due to invalid IL or missing references)
		if (Time.time >= HECFFFDEELC)
		{
			MultiAudioManager.PlayAudioObject(soundToPlay, ((Component)this).transform.position);
			OALEPGKKIDH();
		}
	}

	private void HCAFMBIGBAP()
	{
		HECFFFDEELC = Time.time + Random.Range(minTime, maxTime);
	}

	private void NJNCKELODLF()
	{
		//IL_0019: Unknown result type (might be due to invalid IL or missing references)
		if (Time.time >= HECFFFDEELC)
		{
			MultiAudioManager.PlayAudioObject(soundToPlay, ((Component)this).transform.position);
			HHIHDANGJON();
		}
	}

	private void NLIGLFEEFNO()
	{
		//IL_0019: Unknown result type (might be due to invalid IL or missing references)
		if (Time.time >= HECFFFDEELC)
		{
			MultiAudioManager.PlayAudioObject(soundToPlay, ((Component)this).transform.position);
			LOBGOBNOFMG();
		}
	}

	private void OLPPDFDOKLK()
	{
		//IL_0019: Unknown result type (might be due to invalid IL or missing references)
		if (Time.time >= HECFFFDEELC)
		{
			MultiAudioManager.PlayAudioObject(soundToPlay, ((Component)this).transform.position);
			FEKLMJNCHMK();
		}
	}

	private void EAGHAALGAIE()
	{
		HECFFFDEELC = Time.time + Random.Range(minTime, maxTime);
	}

	private void DDLLFPICOJK()
	{
		//IL_0019: Unknown result type (might be due to invalid IL or missing references)
		if (Time.time >= HECFFFDEELC)
		{
			MultiAudioManager.PlayAudioObject(soundToPlay, ((Component)this).transform.position);
			HNGCKLOKBAN();
		}
	}

	private void OALEPGKKIDH()
	{
		HECFFFDEELC = Time.time + Random.Range(minTime, maxTime);
	}

	private void PKEPBKHEDOD()
	{
		//IL_0019: Unknown result type (might be due to invalid IL or missing references)
		if (Time.time >= HECFFFDEELC)
		{
			MultiAudioManager.PlayAudioObject(soundToPlay, ((Component)this).transform.position);
			BAIKKKFCLDE();
		}
	}

	private void NCAAPKMKGLH()
	{
		HECFFFDEELC = Time.time + Random.Range(minTime, maxTime);
	}

	private void CCJOOMDHMKM()
	{
		NCAAPKMKGLH();
	}

	private void HNGCKLOKBAN()
	{
		HECFFFDEELC = Time.time + Random.Range(minTime, maxTime);
	}

	private void BPHGADINMAN()
	{
		EAGHAALGAIE();
	}

	private void FEPLMMOOABC()
	{
		OALEPGKKIDH();
	}

	private void IAICCKPOBMJ()
	{
		//IL_0019: Unknown result type (might be due to invalid IL or missing references)
		if (Time.time >= HECFFFDEELC)
		{
			MultiAudioManager.PlayAudioObject(soundToPlay, ((Component)this).transform.position);
			HNGCKLOKBAN();
		}
	}

	private void GNAHKAHPCFB()
	{
		//IL_0019: Unknown result type (might be due to invalid IL or missing references)
		if (Time.time >= HECFFFDEELC)
		{
			MultiAudioManager.PlayAudioObject(soundToPlay, ((Component)this).transform.position);
			ECBBBOCPODM();
		}
	}

	private void MKEGMPDGDDI()
	{
		HECFFFDEELC = Time.time + Random.Range(minTime, maxTime);
	}

	private void ELHCBGCEJDH()
	{
		//IL_0019: Unknown result type (might be due to invalid IL or missing references)
		if (Time.time >= HECFFFDEELC)
		{
			MultiAudioManager.PlayAudioObject(soundToPlay, ((Component)this).transform.position);
			DMHLJBBNHHA();
		}
	}

	private void ADDLDNPHIHF()
	{
		DEGOEEKPJMF();
	}

	private void DKICIFNOKKD()
	{
		HECFFFDEELC = Time.time + Random.Range(minTime, maxTime);
	}

	private void POFDOILPANH()
	{
		//IL_0019: Unknown result type (might be due to invalid IL or missing references)
		if (Time.time >= HECFFFDEELC)
		{
			MultiAudioManager.PlayAudioObject(soundToPlay, ((Component)this).transform.position);
			OALEPGKKIDH();
		}
	}
}
