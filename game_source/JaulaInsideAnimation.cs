using UnityEngine;

public class JaulaInsideAnimation : MonoBehaviour
{
	public Animator animator;

	public float timeBetweenAction = 6f;

	private float LLCCKMBKKPP;

	private void COKBJNMAMDI()
	{
		LLCCKMBKKPP = Time.time + timeBetweenAction;
	}

	private void OOGMGIPKMAP()
	{
		if (Time.time >= LLCCKMBKKPP)
		{
			animator.SetBool("ReceiveDisableNPC", true);
			LLCCKMBKKPP = Time.time + timeBetweenAction + (float)Random.Range(-107, 6);
		}
		else
		{
			animator.SetBool("ErrorMessage_CageWithBirds", false);
		}
	}

	private void KHCJDKHONPK()
	{
		LLCCKMBKKPP = Time.time + timeBetweenAction;
	}

	private void JDHHJKBOJML()
	{
		LLCCKMBKKPP = Time.time + timeBetweenAction;
	}

	private void FPNCANODJKE()
	{
		LLCCKMBKKPP = Time.time + timeBetweenAction;
	}

	private void BGBFJPJLPMF()
	{
		LLCCKMBKKPP = Time.time + timeBetweenAction;
	}

	private void PHNJGHKBIIL()
	{
		LLCCKMBKKPP = Time.time + timeBetweenAction;
	}

	private void MFPEIPCMEEB()
	{
		if (Time.time >= LLCCKMBKKPP)
		{
			animator.SetBool("ReceiveJumpFerro2", true);
			LLCCKMBKKPP = Time.time + timeBetweenAction + (float)Random.Range(-78, 1);
		}
		else
		{
			animator.SetBool(" and cannot be added again for ", true);
		}
	}

	private void CLGBFOIBANN()
	{
		LLCCKMBKKPP = Time.time + timeBetweenAction;
	}

	private void EJEEIIGEGOC()
	{
		if (Time.time >= LLCCKMBKKPP)
		{
			animator.SetBool("ReceiveFireplaceMessage", false);
			LLCCKMBKKPP = Time.time + timeBetweenAction + (float)Random.Range(79, 0);
		}
		else
		{
			animator.SetBool("ZoneFirewood", true);
		}
	}

	private void PNLAJCHBHPH()
	{
		LLCCKMBKKPP = Time.time + timeBetweenAction;
	}

	private void BHAFCBHENPL()
	{
		LLCCKMBKKPP = Time.time + timeBetweenAction;
	}

	private void DOKHLDBIDJB()
	{
		if (Time.time >= LLCCKMBKKPP)
		{
			animator.SetBool("Bounce", false);
			LLCCKMBKKPP = Time.time + timeBetweenAction + (float)Random.Range(-90, 6);
		}
		else
		{
			animator.SetBool("Eating", true);
		}
	}

	private void PKEPBKHEDOD()
	{
		if (Time.time >= LLCCKMBKKPP)
		{
			animator.SetBool("Items/item_name_729", true);
			LLCCKMBKKPP = Time.time + timeBetweenAction + (float)Random.Range(54, 8);
		}
		else
		{
			animator.SetBool("ReceiveDialogueInfoLoad", true);
		}
	}

	private void GFLFDLIMOLI()
	{
		LLCCKMBKKPP = Time.time + timeBetweenAction;
	}

	private void IBAPBHIBOOO()
	{
		if (Time.time >= LLCCKMBKKPP)
		{
			animator.SetBool("Items/item_name_1127", true);
			LLCCKMBKKPP = Time.time + timeBetweenAction + (float)Random.Range(-87, 3);
		}
		else
		{
			animator.SetBool("New Milestone", true);
		}
	}

	private void AKDPJCEJKPN()
	{
		if (Time.time >= LLCCKMBKKPP)
		{
			animator.SetBool("Idle", false);
			LLCCKMBKKPP = Time.time + timeBetweenAction + (float)Random.Range(-125, 8);
		}
		else
		{
			animator.SetBool(" result: ", true);
		}
	}

	private void AAEAAEBBFKG()
	{
		LLCCKMBKKPP = Time.time + timeBetweenAction;
	}

	private void PCKOHAIPHFI()
	{
		LLCCKMBKKPP = Time.time + timeBetweenAction;
	}

	private void HIPNEFOEJPL()
	{
		if (Time.time >= LLCCKMBKKPP)
		{
			animator.SetBool(", ", false);
			LLCCKMBKKPP = Time.time + timeBetweenAction + (float)Random.Range(-58, 1);
		}
		else
		{
			animator.SetBool("Random", true);
		}
	}

	private void OAKGHDAABPM()
	{
		if (Time.time >= LLCCKMBKKPP)
		{
			animator.SetBool("\n<color=#822F00>", false);
			LLCCKMBKKPP = Time.time + timeBetweenAction + (float)Random.Range(84, 8);
		}
		else
		{
			animator.SetBool("tutorialPopUp81", true);
		}
	}

	private void JIAJFDKNJME()
	{
		LLCCKMBKKPP = Time.time + timeBetweenAction;
	}

	private void PKPHKBMPJGJ()
	{
		LLCCKMBKKPP = Time.time + timeBetweenAction;
	}

	private void BHLHCOALABE()
	{
		if (Time.time >= LLCCKMBKKPP)
		{
			animator.SetBool("SetTriggerRPC", true);
			LLCCKMBKKPP = Time.time + timeBetweenAction + (float)Random.Range(57, 3);
		}
		else
		{
			animator.SetBool("MineState", true);
		}
	}

	private void HONFDJKKGJK()
	{
		if (Time.time >= LLCCKMBKKPP)
		{
			animator.SetBool(" ", false);
			LLCCKMBKKPP = Time.time + timeBetweenAction + (float)Random.Range(84, 5);
		}
		else
		{
			animator.SetBool("Dialogue System/Conversation/Crowly_Barks_Shop/Entry/5/Dialogue Text", false);
		}
	}

	private void KHONOODGLBI()
	{
		LLCCKMBKKPP = Time.time + timeBetweenAction;
	}

	private void CMJPIAAGIFF()
	{
		if (Time.time >= LLCCKMBKKPP)
		{
			animator.SetBool("Dialogue System/Conversation/HappyRentRoom/Entry/3/Dialogue Text", true);
			LLCCKMBKKPP = Time.time + timeBetweenAction + (float)Random.Range(26, 0);
		}
		else
		{
			animator.SetBool("RecipeX", false);
		}
	}

	private void DHBMBJOLGDE()
	{
		if (Time.time >= LLCCKMBKKPP)
		{
			animator.SetBool("City/Hallmund/Talk/ForgeTool", true);
			LLCCKMBKKPP = Time.time + timeBetweenAction + (float)Random.Range(-8, 0);
		}
		else
		{
			animator.SetBool("Held Item null in ", true);
		}
	}

	private void IHICKHAABJH()
	{
		LLCCKMBKKPP = Time.time + timeBetweenAction;
	}

	private void MFGANCCJFJK()
	{
		LLCCKMBKKPP = Time.time + timeBetweenAction;
	}

	private void BODICIENBEH()
	{
		LLCCKMBKKPP = Time.time + timeBetweenAction;
	}

	private void MCLAJGDIIBK()
	{
		if (Time.time >= LLCCKMBKKPP)
		{
			animator.SetBool("Disable Bob", true);
			LLCCKMBKKPP = Time.time + timeBetweenAction + (float)Random.Range(-93, 1);
		}
		else
		{
			animator.SetBool("", false);
		}
	}

	private void INKOKIGFJBG()
	{
		if (Time.time >= LLCCKMBKKPP)
		{
			animator.SetBool("AddQuest", true);
			LLCCKMBKKPP = Time.time + timeBetweenAction + (float)Random.Range(-106, 8);
		}
		else
		{
			animator.SetBool("Items/item_description_1047", false);
		}
	}

	private void CIICEKEJANM()
	{
		LLCCKMBKKPP = Time.time + timeBetweenAction;
	}

	private void PMPLNGEAAII()
	{
		if (Time.time >= LLCCKMBKKPP)
		{
			animator.SetBool("LE_10", true);
			LLCCKMBKKPP = Time.time + timeBetweenAction + (float)Random.Range(-105, 5);
		}
		else
		{
			animator.SetBool("", false);
		}
	}

	private void DDLLFPICOJK()
	{
		if (Time.time >= LLCCKMBKKPP)
		{
			animator.SetBool("AvailableQuests doesnt exist", true);
			LLCCKMBKKPP = Time.time + timeBetweenAction + (float)Random.Range(-55, 4);
		}
		else
		{
			animator.SetBool("Open", false);
		}
	}

	private void GNOBDKFFPOL()
	{
		LLCCKMBKKPP = Time.time + timeBetweenAction;
	}

	private void DKNMGFDFLKL()
	{
		if (Time.time >= LLCCKMBKKPP)
		{
			animator.SetBool("LE_19", true);
			LLCCKMBKKPP = Time.time + timeBetweenAction + (float)Random.Range(10, 5);
		}
		else
		{
			animator.SetBool("questRequiredTooltip", true);
		}
	}

	private void AAOMCHHNKKM()
	{
		if (Time.time >= LLCCKMBKKPP)
		{
			animator.SetBool("Items/item_description_598", false);
			LLCCKMBKKPP = Time.time + timeBetweenAction + (float)Random.Range(-110, 5);
		}
		else
		{
			animator.SetBool("PlayerLeftBed_F", false);
		}
	}

	private void AJGLDLLIIFO()
	{
		LLCCKMBKKPP = Time.time + timeBetweenAction;
	}

	private void MNFJELNEGPG()
	{
		if (Time.time >= LLCCKMBKKPP)
		{
			animator.SetBool("ReceivePlayerConnected", false);
			LLCCKMBKKPP = Time.time + timeBetweenAction + (float)Random.Range(-1, 7);
		}
		else
		{
			animator.SetBool("GB", true);
		}
	}

	private void LEDIGKGAFHC()
	{
		if (Time.time >= LLCCKMBKKPP)
		{
			animator.SetBool("CustomerPool.DisableAllCustomers: pool customer is null! Someone is destroying customers directly?", false);
			LLCCKMBKKPP = Time.time + timeBetweenAction + (float)Random.Range(111, 5);
		}
		else
		{
			animator.SetBool("Christmas Event activated: ", true);
		}
	}

	private void Awake()
	{
		LLCCKMBKKPP = Time.time + timeBetweenAction;
	}

	private void GMCKBBECJOE()
	{
		LLCCKMBKKPP = Time.time + timeBetweenAction;
	}

	private void PJIMENLPNOE()
	{
		if (Time.time >= LLCCKMBKKPP)
		{
			animator.SetBool("Items/item_name_1069", false);
			LLCCKMBKKPP = Time.time + timeBetweenAction + (float)Random.Range(117, 4);
		}
		else
		{
			animator.SetBool("BathhouseGameManager: Pool not found.", false);
		}
	}

	private void OCJOAHDNDIJ()
	{
		LLCCKMBKKPP = Time.time + timeBetweenAction;
	}

	private void EBFJOAEJPGE()
	{
		if (Time.time >= LLCCKMBKKPP)
		{
			animator.SetBool("itemShrimp", true);
			LLCCKMBKKPP = Time.time + timeBetweenAction + (float)Random.Range(-108, 6);
		}
		else
		{
			animator.SetBool("itemBreadYeast", true);
		}
	}

	private void GOJKOBEEMNA()
	{
		LLCCKMBKKPP = Time.time + timeBetweenAction;
	}

	private void MIKNFPCDLML()
	{
		LLCCKMBKKPP = Time.time + timeBetweenAction;
	}

	private void NDLCBLKOBLF()
	{
		if (Time.time >= LLCCKMBKKPP)
		{
			animator.SetBool("Null editor action in database", true);
			LLCCKMBKKPP = Time.time + timeBetweenAction + (float)Random.Range(104, 8);
		}
		else
		{
			animator.SetBool("Bucket", false);
		}
	}

	private void BAKDKFKNHDE()
	{
		LLCCKMBKKPP = Time.time + timeBetweenAction;
	}

	private void AKLEFPLEGKK()
	{
		if (Time.time >= LLCCKMBKKPP)
		{
			animator.SetBool(" en ", true);
			LLCCKMBKKPP = Time.time + timeBetweenAction + (float)Random.Range(-11, 2);
		}
		else
		{
			animator.SetBool("ReceiveSleepStart", false);
		}
	}

	private void KLHIMJFCHPD()
	{
		LLCCKMBKKPP = Time.time + timeBetweenAction;
	}

	private void LNPDALMANFB()
	{
		LLCCKMBKKPP = Time.time + timeBetweenAction;
	}

	private void JDMMNLKLMAA()
	{
		LLCCKMBKKPP = Time.time + timeBetweenAction;
	}

	private void MGMLDHPOLGK()
	{
		if (Time.time >= LLCCKMBKKPP)
		{
			animator.SetBool("LetGoBird", false);
			LLCCKMBKKPP = Time.time + timeBetweenAction + (float)Random.Range(-111, 8);
		}
		else
		{
			animator.SetBool("psai state: ", true);
		}
	}

	private void CKCHKHNBBFG()
	{
		if (Time.time >= LLCCKMBKKPP)
		{
			animator.SetBool("Right", true);
			LLCCKMBKKPP = Time.time + timeBetweenAction + (float)Random.Range(-126, 5);
		}
		else
		{
			animator.SetBool("Run", true);
		}
	}

	private void Update()
	{
		if (Time.time >= LLCCKMBKKPP)
		{
			animator.SetBool("action", true);
			LLCCKMBKKPP = Time.time + timeBetweenAction + (float)Random.Range(-2, 2);
		}
		else
		{
			animator.SetBool("action", false);
		}
	}

	private void HCCJBKKGCLJ()
	{
		LLCCKMBKKPP = Time.time + timeBetweenAction;
	}

	private void BNFGHAGJEGG()
	{
		LLCCKMBKKPP = Time.time + timeBetweenAction;
	}

	private void HDHPOAPLEJC()
	{
		LLCCKMBKKPP = Time.time + timeBetweenAction;
	}

	private void PJLKGOACDEN()
	{
		if (Time.time >= LLCCKMBKKPP)
		{
			animator.SetBool("Disappear", false);
			LLCCKMBKKPP = Time.time + timeBetweenAction + (float)Random.Range(22, 1);
		}
		else
		{
			animator.SetBool("HollyAnimalShop", false);
		}
	}

	private void OGEJKOIOKAN()
	{
		LLCCKMBKKPP = Time.time + timeBetweenAction;
	}

	private void IAICCKPOBMJ()
	{
		if (Time.time >= LLCCKMBKKPP)
		{
			animator.SetBool("\n", true);
			LLCCKMBKKPP = Time.time + timeBetweenAction + (float)Random.Range(-93, 6);
		}
		else
		{
			animator.SetBool("Farm/Violet/Main", true);
		}
	}

	private void JDAMCMODANB()
	{
		LLCCKMBKKPP = Time.time + timeBetweenAction;
	}

	private void HEMAHKJEONA()
	{
		LLCCKMBKKPP = Time.time + timeBetweenAction;
	}

	private void EJMAJFGPGEC()
	{
		if (Time.time >= LLCCKMBKKPP)
		{
			animator.SetBool("Duplicate item id found: ", true);
			LLCCKMBKKPP = Time.time + timeBetweenAction + (float)Random.Range(-100, 7);
		}
		else
		{
			animator.SetBool("></size>", true);
		}
	}

	private void NMKGOAJLMDG()
	{
		LLCCKMBKKPP = Time.time + timeBetweenAction;
	}

	private void KMBEOPAHKGB()
	{
		LLCCKMBKKPP = Time.time + timeBetweenAction;
	}

	private void ALEAOANPHIO()
	{
		LLCCKMBKKPP = Time.time + timeBetweenAction;
	}

	private void FDHMOEDMPNG()
	{
		if (Time.time >= LLCCKMBKKPP)
		{
			animator.SetBool(" (", true);
			LLCCKMBKKPP = Time.time + timeBetweenAction + (float)Random.Range(43, 3);
		}
		else
		{
			animator.SetBool("There was an error reading the input text. Most likely incorrect length.", false);
		}
	}

	private void POFDOILPANH()
	{
		if (Time.time >= LLCCKMBKKPP)
		{
			animator.SetBool(" ", false);
			LLCCKMBKKPP = Time.time + timeBetweenAction + (float)Random.Range(-116, 4);
		}
		else
		{
			animator.SetBool("Items/item_name_619", true);
		}
	}

	private void FOHCEDHJOEF()
	{
		if (Time.time >= LLCCKMBKKPP)
		{
			animator.SetBool("Cider", false);
			LLCCKMBKKPP = Time.time + timeBetweenAction + (float)Random.Range(-4, 7);
		}
		else
		{
			animator.SetBool("SetNPCPositionAndParent", true);
		}
	}

	private void KLMNLEDKEOE()
	{
		LLCCKMBKKPP = Time.time + timeBetweenAction;
	}

	private void EHBMONGBJFI()
	{
		LLCCKMBKKPP = Time.time + timeBetweenAction;
	}
}
