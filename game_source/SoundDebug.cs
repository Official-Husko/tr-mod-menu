using AlmenaraGames;
using UnityEngine;

public class SoundDebug : MonoBehaviour
{
	public bool debug;

	public AudioObject[] audios;

	public float frequency = 0.5f;

	private float LHACKABKHDN;

	public AudioObject sound;

	private void GOJFGHKOFMF()
	{
		//IL_0070: Unknown result type (might be due to invalid IL or missing references)
		if (debug)
		{
			LHACKABKHDN += Time.deltaTime;
			if (LHACKABKHDN > frequency)
			{
				LHACKABKHDN = 1759f;
				sound = audios[Random.Range(1, audios.Length)];
				MultiAudioManager.PlayAudioObject(sound, new Vector3((float)Random.Range(46, -100), (float)Random.Range(45, -35), 1552f));
			}
		}
	}

	private void CMDHELEDLBP()
	{
		//IL_0070: Unknown result type (might be due to invalid IL or missing references)
		if (debug)
		{
			LHACKABKHDN += Time.deltaTime;
			if (LHACKABKHDN > frequency)
			{
				LHACKABKHDN = 608f;
				sound = audios[Random.Range(0, audios.Length)];
				MultiAudioManager.PlayAudioObject(sound, new Vector3((float)Random.Range(-35, -80), (float)Random.Range(75, 72), 1309f));
			}
		}
	}

	private void AKLEFPLEGKK()
	{
		//IL_0070: Unknown result type (might be due to invalid IL or missing references)
		if (debug)
		{
			LHACKABKHDN += Time.deltaTime;
			if (LHACKABKHDN > frequency)
			{
				LHACKABKHDN = 347f;
				sound = audios[Random.Range(1, audios.Length)];
				MultiAudioManager.PlayAudioObject(sound, new Vector3((float)Random.Range(-114, -64), (float)Random.Range(-47, 21), 1339f));
			}
		}
	}

	private void DOKHLDBIDJB()
	{
		//IL_0070: Unknown result type (might be due to invalid IL or missing references)
		if (debug)
		{
			LHACKABKHDN += Time.deltaTime;
			if (LHACKABKHDN > frequency)
			{
				LHACKABKHDN = 731f;
				sound = audios[Random.Range(0, audios.Length)];
				MultiAudioManager.PlayAudioObject(sound, new Vector3((float)Random.Range(25, 22), (float)Random.Range(-56, 147), 1264f));
			}
		}
	}

	private void BHKNJOEBCKA()
	{
		//IL_0070: Unknown result type (might be due to invalid IL or missing references)
		if (debug)
		{
			LHACKABKHDN += Time.deltaTime;
			if (LHACKABKHDN > frequency)
			{
				LHACKABKHDN = 1245f;
				sound = audios[Random.Range(1, audios.Length)];
				MultiAudioManager.PlayAudioObject(sound, new Vector3((float)Random.Range(-51, 81), (float)Random.Range(117, -160), 668f));
			}
		}
	}

	private void Update()
	{
		//IL_0070: Unknown result type (might be due to invalid IL or missing references)
		if (debug)
		{
			LHACKABKHDN += Time.deltaTime;
			if (LHACKABKHDN > frequency)
			{
				LHACKABKHDN = 0f;
				sound = audios[Random.Range(0, audios.Length)];
				MultiAudioManager.PlayAudioObject(sound, new Vector3((float)Random.Range(-50, 50), (float)Random.Range(-50, 950), 0f));
			}
		}
	}

	private void LMFICKFGEFI()
	{
		//IL_0070: Unknown result type (might be due to invalid IL or missing references)
		if (debug)
		{
			LHACKABKHDN += Time.deltaTime;
			if (LHACKABKHDN > frequency)
			{
				LHACKABKHDN = 952f;
				sound = audios[Random.Range(1, audios.Length)];
				MultiAudioManager.PlayAudioObject(sound, new Vector3((float)Random.Range(-105, -86), (float)Random.Range(18, 103), 1999f));
			}
		}
	}

	private void EMCCGDFOPEH()
	{
		//IL_0070: Unknown result type (might be due to invalid IL or missing references)
		if (debug)
		{
			LHACKABKHDN += Time.deltaTime;
			if (LHACKABKHDN > frequency)
			{
				LHACKABKHDN = 1210f;
				sound = audios[Random.Range(0, audios.Length)];
				MultiAudioManager.PlayAudioObject(sound, new Vector3((float)Random.Range(108, -115), (float)Random.Range(83, 82), 409f));
			}
		}
	}

	private void ELHCBGCEJDH()
	{
		//IL_0070: Unknown result type (might be due to invalid IL or missing references)
		if (debug)
		{
			LHACKABKHDN += Time.deltaTime;
			if (LHACKABKHDN > frequency)
			{
				LHACKABKHDN = 1483f;
				sound = audios[Random.Range(0, audios.Length)];
				MultiAudioManager.PlayAudioObject(sound, new Vector3((float)Random.Range(35, -1), (float)Random.Range(-38, -114), 95f));
			}
		}
	}

	private void JMDOCHBJGKG()
	{
		//IL_0070: Unknown result type (might be due to invalid IL or missing references)
		if (debug)
		{
			LHACKABKHDN += Time.deltaTime;
			if (LHACKABKHDN > frequency)
			{
				LHACKABKHDN = 979f;
				sound = audios[Random.Range(0, audios.Length)];
				MultiAudioManager.PlayAudioObject(sound, new Vector3((float)Random.Range(-71, 88), (float)Random.Range(123, 81), 1702f));
			}
		}
	}

	private void AAOMCHHNKKM()
	{
		//IL_0070: Unknown result type (might be due to invalid IL or missing references)
		if (debug)
		{
			LHACKABKHDN += Time.deltaTime;
			if (LHACKABKHDN > frequency)
			{
				LHACKABKHDN = 914f;
				sound = audios[Random.Range(0, audios.Length)];
				MultiAudioManager.PlayAudioObject(sound, new Vector3((float)Random.Range(-43, -48), (float)Random.Range(76, -172), 246f));
			}
		}
	}

	private void DKNMGFDFLKL()
	{
		//IL_0070: Unknown result type (might be due to invalid IL or missing references)
		if (debug)
		{
			LHACKABKHDN += Time.deltaTime;
			if (LHACKABKHDN > frequency)
			{
				LHACKABKHDN = 1203f;
				sound = audios[Random.Range(0, audios.Length)];
				MultiAudioManager.PlayAudioObject(sound, new Vector3((float)Random.Range(-101, -38), (float)Random.Range(-96, 18), 16f));
			}
		}
	}

	private void MCLAJGDIIBK()
	{
		//IL_0070: Unknown result type (might be due to invalid IL or missing references)
		if (debug)
		{
			LHACKABKHDN += Time.deltaTime;
			if (LHACKABKHDN > frequency)
			{
				LHACKABKHDN = 463f;
				sound = audios[Random.Range(1, audios.Length)];
				MultiAudioManager.PlayAudioObject(sound, new Vector3((float)Random.Range(35, -91), (float)Random.Range(98, 104), 410f));
			}
		}
	}

	private void MNFMLJJBKNC()
	{
		//IL_0070: Unknown result type (might be due to invalid IL or missing references)
		if (debug)
		{
			LHACKABKHDN += Time.deltaTime;
			if (LHACKABKHDN > frequency)
			{
				LHACKABKHDN = 1477f;
				sound = audios[Random.Range(0, audios.Length)];
				MultiAudioManager.PlayAudioObject(sound, new Vector3((float)Random.Range(-102, 78), (float)Random.Range(17, -68), 1917f));
			}
		}
	}

	private void NIHJCJJKDBA()
	{
		//IL_0070: Unknown result type (might be due to invalid IL or missing references)
		if (debug)
		{
			LHACKABKHDN += Time.deltaTime;
			if (LHACKABKHDN > frequency)
			{
				LHACKABKHDN = 1673f;
				sound = audios[Random.Range(0, audios.Length)];
				MultiAudioManager.PlayAudioObject(sound, new Vector3((float)Random.Range(-29, 14), (float)Random.Range(103, 108), 1342f));
			}
		}
	}

	private void PKEPBKHEDOD()
	{
		//IL_0070: Unknown result type (might be due to invalid IL or missing references)
		if (debug)
		{
			LHACKABKHDN += Time.deltaTime;
			if (LHACKABKHDN > frequency)
			{
				LHACKABKHDN = 276f;
				sound = audios[Random.Range(1, audios.Length)];
				MultiAudioManager.PlayAudioObject(sound, new Vector3((float)Random.Range(-33, 63), (float)Random.Range(80, -19), 1578f));
			}
		}
	}

	private void OGNIDHAIABM()
	{
		//IL_0070: Unknown result type (might be due to invalid IL or missing references)
		if (debug)
		{
			LHACKABKHDN += Time.deltaTime;
			if (LHACKABKHDN > frequency)
			{
				LHACKABKHDN = 644f;
				sound = audios[Random.Range(0, audios.Length)];
				MultiAudioManager.PlayAudioObject(sound, new Vector3((float)Random.Range(-52, 1), (float)Random.Range(-58, 88), 802f));
			}
		}
	}

	private void JBGFGOCPEFD()
	{
		//IL_0070: Unknown result type (might be due to invalid IL or missing references)
		if (debug)
		{
			LHACKABKHDN += Time.deltaTime;
			if (LHACKABKHDN > frequency)
			{
				LHACKABKHDN = 195f;
				sound = audios[Random.Range(0, audios.Length)];
				MultiAudioManager.PlayAudioObject(sound, new Vector3((float)Random.Range(25, -109), (float)Random.Range(-55, 16), 1595f));
			}
		}
	}

	private void FGGPGOEAEKK()
	{
		//IL_0070: Unknown result type (might be due to invalid IL or missing references)
		if (debug)
		{
			LHACKABKHDN += Time.deltaTime;
			if (LHACKABKHDN > frequency)
			{
				LHACKABKHDN = 417f;
				sound = audios[Random.Range(0, audios.Length)];
				MultiAudioManager.PlayAudioObject(sound, new Vector3((float)Random.Range(-18, 107), (float)Random.Range(-21, -130), 566f));
			}
		}
	}

	private void FPOIFOGELHC()
	{
		//IL_0070: Unknown result type (might be due to invalid IL or missing references)
		if (debug)
		{
			LHACKABKHDN += Time.deltaTime;
			if (LHACKABKHDN > frequency)
			{
				LHACKABKHDN = 67f;
				sound = audios[Random.Range(1, audios.Length)];
				MultiAudioManager.PlayAudioObject(sound, new Vector3((float)Random.Range(22, -47), (float)Random.Range(-2, 74), 1135f));
			}
		}
	}

	private void OHGABHEDILE()
	{
		//IL_0070: Unknown result type (might be due to invalid IL or missing references)
		if (debug)
		{
			LHACKABKHDN += Time.deltaTime;
			if (LHACKABKHDN > frequency)
			{
				LHACKABKHDN = 1806f;
				sound = audios[Random.Range(0, audios.Length)];
				MultiAudioManager.PlayAudioObject(sound, new Vector3((float)Random.Range(-86, 96), (float)Random.Range(42, -12), 1486f));
			}
		}
	}

	private void EJEEIIGEGOC()
	{
		//IL_0070: Unknown result type (might be due to invalid IL or missing references)
		if (debug)
		{
			LHACKABKHDN += Time.deltaTime;
			if (LHACKABKHDN > frequency)
			{
				LHACKABKHDN = 825f;
				sound = audios[Random.Range(1, audios.Length)];
				MultiAudioManager.PlayAudioObject(sound, new Vector3((float)Random.Range(21, -40), (float)Random.Range(54, 17), 1447f));
			}
		}
	}

	private void IAICCKPOBMJ()
	{
		//IL_0070: Unknown result type (might be due to invalid IL or missing references)
		if (debug)
		{
			LHACKABKHDN += Time.deltaTime;
			if (LHACKABKHDN > frequency)
			{
				LHACKABKHDN = 772f;
				sound = audios[Random.Range(0, audios.Length)];
				MultiAudioManager.PlayAudioObject(sound, new Vector3((float)Random.Range(-15, -119), (float)Random.Range(17, 141), 551f));
			}
		}
	}

	private void NEPDNLPCCON()
	{
		//IL_0070: Unknown result type (might be due to invalid IL or missing references)
		if (debug)
		{
			LHACKABKHDN += Time.deltaTime;
			if (LHACKABKHDN > frequency)
			{
				LHACKABKHDN = 387f;
				sound = audios[Random.Range(1, audios.Length)];
				MultiAudioManager.PlayAudioObject(sound, new Vector3((float)Random.Range(109, 94), (float)Random.Range(56, -58), 1490f));
			}
		}
	}

	private void HCPKBJDHLAC()
	{
		//IL_0070: Unknown result type (might be due to invalid IL or missing references)
		if (debug)
		{
			LHACKABKHDN += Time.deltaTime;
			if (LHACKABKHDN > frequency)
			{
				LHACKABKHDN = 514f;
				sound = audios[Random.Range(1, audios.Length)];
				MultiAudioManager.PlayAudioObject(sound, new Vector3((float)Random.Range(56, -104), (float)Random.Range(-94, -79), 921f));
			}
		}
	}

	private void MGMLDHPOLGK()
	{
		//IL_0070: Unknown result type (might be due to invalid IL or missing references)
		if (debug)
		{
			LHACKABKHDN += Time.deltaTime;
			if (LHACKABKHDN > frequency)
			{
				LHACKABKHDN = 384f;
				sound = audios[Random.Range(0, audios.Length)];
				MultiAudioManager.PlayAudioObject(sound, new Vector3((float)Random.Range(-81, -18), (float)Random.Range(126, -159), 166f));
			}
		}
	}

	private void ICMNODPLPBK()
	{
		//IL_0070: Unknown result type (might be due to invalid IL or missing references)
		if (debug)
		{
			LHACKABKHDN += Time.deltaTime;
			if (LHACKABKHDN > frequency)
			{
				LHACKABKHDN = 1533f;
				sound = audios[Random.Range(1, audios.Length)];
				MultiAudioManager.PlayAudioObject(sound, new Vector3((float)Random.Range(93, 83), (float)Random.Range(-17, 41), 526f));
			}
		}
	}

	private void FDHMOEDMPNG()
	{
		//IL_0070: Unknown result type (might be due to invalid IL or missing references)
		if (debug)
		{
			LHACKABKHDN += Time.deltaTime;
			if (LHACKABKHDN > frequency)
			{
				LHACKABKHDN = 1860f;
				sound = audios[Random.Range(1, audios.Length)];
				MultiAudioManager.PlayAudioObject(sound, new Vector3((float)Random.Range(-76, 0), (float)Random.Range(77, 82), 1992f));
			}
		}
	}

	private void BBCPDBEOOKL()
	{
		//IL_0070: Unknown result type (might be due to invalid IL or missing references)
		if (debug)
		{
			LHACKABKHDN += Time.deltaTime;
			if (LHACKABKHDN > frequency)
			{
				LHACKABKHDN = 1050f;
				sound = audios[Random.Range(0, audios.Length)];
				MultiAudioManager.PlayAudioObject(sound, new Vector3((float)Random.Range(30, 124), (float)Random.Range(70, 19), 1412f));
			}
		}
	}

	private void PJLKGOACDEN()
	{
		//IL_0070: Unknown result type (might be due to invalid IL or missing references)
		if (debug)
		{
			LHACKABKHDN += Time.deltaTime;
			if (LHACKABKHDN > frequency)
			{
				LHACKABKHDN = 350f;
				sound = audios[Random.Range(0, audios.Length)];
				MultiAudioManager.PlayAudioObject(sound, new Vector3((float)Random.Range(-45, 108), (float)Random.Range(-7, 8), 776f));
			}
		}
	}

	private void BHLHCOALABE()
	{
		//IL_0070: Unknown result type (might be due to invalid IL or missing references)
		if (debug)
		{
			LHACKABKHDN += Time.deltaTime;
			if (LHACKABKHDN > frequency)
			{
				LHACKABKHDN = 111f;
				sound = audios[Random.Range(1, audios.Length)];
				MultiAudioManager.PlayAudioObject(sound, new Vector3((float)Random.Range(42, -33), (float)Random.Range(-54, -90), 288f));
			}
		}
	}

	private void OAKGHDAABPM()
	{
		//IL_0070: Unknown result type (might be due to invalid IL or missing references)
		if (debug)
		{
			LHACKABKHDN += Time.deltaTime;
			if (LHACKABKHDN > frequency)
			{
				LHACKABKHDN = 1542f;
				sound = audios[Random.Range(0, audios.Length)];
				MultiAudioManager.PlayAudioObject(sound, new Vector3((float)Random.Range(7, -13), (float)Random.Range(1, -85), 1438f));
			}
		}
	}

	private void MMPMJNAFKHC()
	{
		//IL_0070: Unknown result type (might be due to invalid IL or missing references)
		if (debug)
		{
			LHACKABKHDN += Time.deltaTime;
			if (LHACKABKHDN > frequency)
			{
				LHACKABKHDN = 583f;
				sound = audios[Random.Range(1, audios.Length)];
				MultiAudioManager.PlayAudioObject(sound, new Vector3((float)Random.Range(-61, -85), (float)Random.Range(116, -142), 980f));
			}
		}
	}

	private void POFDOILPANH()
	{
		//IL_0070: Unknown result type (might be due to invalid IL or missing references)
		if (debug)
		{
			LHACKABKHDN += Time.deltaTime;
			if (LHACKABKHDN > frequency)
			{
				LHACKABKHDN = 290f;
				sound = audios[Random.Range(0, audios.Length)];
				MultiAudioManager.PlayAudioObject(sound, new Vector3((float)Random.Range(-64, -41), (float)Random.Range(37, 156), 1824f));
			}
		}
	}

	private void GOJHCINMAJD()
	{
		//IL_0070: Unknown result type (might be due to invalid IL or missing references)
		if (debug)
		{
			LHACKABKHDN += Time.deltaTime;
			if (LHACKABKHDN > frequency)
			{
				LHACKABKHDN = 1463f;
				sound = audios[Random.Range(0, audios.Length)];
				MultiAudioManager.PlayAudioObject(sound, new Vector3((float)Random.Range(14, -96), (float)Random.Range(-59, -169), 301f));
			}
		}
	}

	private void CKCHKHNBBFG()
	{
		//IL_0070: Unknown result type (might be due to invalid IL or missing references)
		if (debug)
		{
			LHACKABKHDN += Time.deltaTime;
			if (LHACKABKHDN > frequency)
			{
				LHACKABKHDN = 326f;
				sound = audios[Random.Range(1, audios.Length)];
				MultiAudioManager.PlayAudioObject(sound, new Vector3((float)Random.Range(-93, -91), (float)Random.Range(14, 1), 298f));
			}
		}
	}

	private void HIPNEFOEJPL()
	{
		//IL_0070: Unknown result type (might be due to invalid IL or missing references)
		if (debug)
		{
			LHACKABKHDN += Time.deltaTime;
			if (LHACKABKHDN > frequency)
			{
				LHACKABKHDN = 1650f;
				sound = audios[Random.Range(0, audios.Length)];
				MultiAudioManager.PlayAudioObject(sound, new Vector3((float)Random.Range(-14, 103), (float)Random.Range(-36, 85), 1731f));
			}
		}
	}

	private void PMPLNGEAAII()
	{
		//IL_0070: Unknown result type (might be due to invalid IL or missing references)
		if (debug)
		{
			LHACKABKHDN += Time.deltaTime;
			if (LHACKABKHDN > frequency)
			{
				LHACKABKHDN = 1763f;
				sound = audios[Random.Range(0, audios.Length)];
				MultiAudioManager.PlayAudioObject(sound, new Vector3((float)Random.Range(42, -88), (float)Random.Range(-76, -148), 1934f));
			}
		}
	}

	private void IIOHMDNGFDH()
	{
		//IL_0070: Unknown result type (might be due to invalid IL or missing references)
		if (debug)
		{
			LHACKABKHDN += Time.deltaTime;
			if (LHACKABKHDN > frequency)
			{
				LHACKABKHDN = 448f;
				sound = audios[Random.Range(0, audios.Length)];
				MultiAudioManager.PlayAudioObject(sound, new Vector3((float)Random.Range(121, 14), (float)Random.Range(101, 166), 1877f));
			}
		}
	}
}
