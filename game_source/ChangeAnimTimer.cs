using UnityEngine;

public class ChangeAnimTimer : MonoBehaviour
{
	public Animator animator;

	public float minTime = 5f;

	public float maxTime = 10f;

	private float EAAJDGMDJDF;

	private void EJMAJFGPGEC()
	{
		if (EAAJDGMDJDF < Time.timeSinceLevelLoad)
		{
			animator.SetTrigger("LearnBalance");
			EAAJDGMDJDF = Random.Range(minTime, maxTime) + Time.timeSinceLevelLoad;
		}
	}

	private void GCAEICHCIMA()
	{
		EAAJDGMDJDF = Random.Range(minTime, maxTime) + Time.timeSinceLevelLoad;
	}

	private void DHCIDFJBFAD()
	{
		animator.ResetTrigger("UIInteract");
	}

	private void INEOMPKPILM()
	{
		animator.ResetTrigger("Quit to desktop?");
	}

	private void AIFOBFHCHGK()
	{
		animator.ResetTrigger(" for reason: ");
	}

	private void GOJFGHKOFMF()
	{
		if (EAAJDGMDJDF < Time.timeSinceLevelLoad)
		{
			animator.SetTrigger("Items/item_description_1116");
			EAAJDGMDJDF = Random.Range(minTime, maxTime) + Time.timeSinceLevelLoad;
		}
	}

	private void INGGDMNFMCO()
	{
		if (EAAJDGMDJDF < Time.timeSinceLevelLoad)
		{
			animator.SetTrigger("add item ");
			EAAJDGMDJDF = Random.Range(minTime, maxTime) + Time.timeSinceLevelLoad;
		}
	}

	private void ICOJDKMKIPL()
	{
		EAAJDGMDJDF = Random.Range(minTime, maxTime) + Time.timeSinceLevelLoad;
	}

	private void NELLIBBABJD()
	{
		animator.ResetTrigger("Fly");
	}

	private void HJMLKEGOJJA()
	{
		if (EAAJDGMDJDF < Time.timeSinceLevelLoad)
		{
			animator.SetTrigger("LE_20");
			EAAJDGMDJDF = Random.Range(minTime, maxTime) + Time.timeSinceLevelLoad;
		}
	}

	private void LDBBNBEBKLA()
	{
		animator.ResetTrigger("RentedRoom");
	}

	private void POFIEEODEJO()
	{
		EAAJDGMDJDF = Random.Range(minTime, maxTime) + Time.timeSinceLevelLoad;
	}

	private void PAHLKOCEEIH()
	{
		animator.ResetTrigger("Tavern zone from not found");
	}

	private void MKGKKAJHLAO()
	{
		animator.ResetTrigger(" added to delivery chest");
	}

	private void FIIEBCCFGJE()
	{
		animator.ResetTrigger("Farm/Event/RegañinaViolet");
	}

	private void AEMPCMILANL()
	{
		EAAJDGMDJDF = Random.Range(minTime, maxTime) + Time.timeSinceLevelLoad;
	}

	private void PHLLBPLOLFO()
	{
		if (EAAJDGMDJDF < Time.timeSinceLevelLoad)
		{
			animator.SetTrigger("F2");
			EAAJDGMDJDF = Random.Range(minTime, maxTime) + Time.timeSinceLevelLoad;
		}
	}

	private void GDKOIIEHKGO()
	{
		animator.ResetTrigger("Disabled");
	}

	private void OGNIDHAIABM()
	{
		if (EAAJDGMDJDF < Time.timeSinceLevelLoad)
		{
			animator.SetTrigger("Error in TavernEventManager.EndOfDayEventCheck: ");
			EAAJDGMDJDF = Random.Range(minTime, maxTime) + Time.timeSinceLevelLoad;
		}
	}

	private void AJOAKOCODOC()
	{
		EAAJDGMDJDF = Random.Range(minTime, maxTime) + Time.timeSinceLevelLoad;
	}

	private void CCJOOMDHMKM()
	{
		EAAJDGMDJDF = Random.Range(minTime, maxTime) + Time.timeSinceLevelLoad;
	}

	private void HGEKPHOPJCD()
	{
		EAAJDGMDJDF = Random.Range(minTime, maxTime) + Time.timeSinceLevelLoad;
	}

	private void OAKGHDAABPM()
	{
		if (EAAJDGMDJDF < Time.timeSinceLevelLoad)
		{
			animator.SetTrigger("HorizontalMove");
			EAAJDGMDJDF = Random.Range(minTime, maxTime) + Time.timeSinceLevelLoad;
		}
	}

	private void JAKILILIBIM()
	{
		animator.ResetTrigger("<br><br><line-height=120%>");
	}

	private void GFPIEOPHABO()
	{
		animator.ResetTrigger("[ProceduralMine] Forced BIG SPAWNER piece {0} at block {1} (target was {2}). Path block pos: {3}");
	}

	private void PBNHJCJLGMJ()
	{
		animator.ResetTrigger("Items/item_description_1123");
	}

	private void MJKHMOILBFB()
	{
		animator.ResetTrigger("\n");
	}

	private void JPJDFHEANJH()
	{
		animator.ResetTrigger("NinjaChallengeEvent/Main 2");
	}

	private void FGGPGOEAEKK()
	{
		if (EAAJDGMDJDF < Time.timeSinceLevelLoad)
		{
			animator.SetTrigger("HotBath/Main 3");
			EAAJDGMDJDF = Random.Range(minTime, maxTime) + Time.timeSinceLevelLoad;
		}
	}

	private void DIEKBCKJMPH()
	{
		animator.ResetTrigger(" : ");
	}

	private void BIILFPDIADC()
	{
		if (EAAJDGMDJDF < Time.timeSinceLevelLoad)
		{
			animator.SetTrigger("AdventureContinuesEvent OnWorldLoaded ");
			EAAJDGMDJDF = Random.Range(minTime, maxTime) + Time.timeSinceLevelLoad;
		}
	}

	private void NJNCKELODLF()
	{
		if (EAAJDGMDJDF < Time.timeSinceLevelLoad)
		{
			animator.SetTrigger("[ChessPillar] Solución: Top={0} | Mid={1} | Bot={2}");
			EAAJDGMDJDF = Random.Range(minTime, maxTime) + Time.timeSinceLevelLoad;
		}
	}

	private void HOCPJCKOACJ()
	{
		EAAJDGMDJDF = Random.Range(minTime, maxTime) + Time.timeSinceLevelLoad;
	}

	private void OHOHFGMJGAJ()
	{
		EAAJDGMDJDF = Random.Range(minTime, maxTime) + Time.timeSinceLevelLoad;
	}

	private void CCBLGJADPEO()
	{
		animator.ResetTrigger("AceT_Quest");
	}

	private void HEOAIGHOLLD()
	{
		animator.ResetTrigger("Victory");
	}

	private void KAGAGFAGBJI()
	{
		EAAJDGMDJDF = Random.Range(minTime, maxTime) + Time.timeSinceLevelLoad;
	}

	private void EAMLHICIOMO()
	{
		animator.ResetTrigger(" not found in OnlineEventsManager.");
	}

	private void FPJABCNDEEJ()
	{
		EAAJDGMDJDF = Random.Range(minTime, maxTime) + Time.timeSinceLevelLoad;
	}

	private void IOHIEBBDOJN()
	{
		if (EAAJDGMDJDF < Time.timeSinceLevelLoad)
		{
			animator.SetTrigger("Eat");
			EAAJDGMDJDF = Random.Range(minTime, maxTime) + Time.timeSinceLevelLoad;
		}
	}

	private void MCLAJGDIIBK()
	{
		if (EAAJDGMDJDF < Time.timeSinceLevelLoad)
		{
			animator.SetTrigger("<sprite name=\"");
			EAAJDGMDJDF = Random.Range(minTime, maxTime) + Time.timeSinceLevelLoad;
		}
	}

	private void OFDEGDJGGGF()
	{
		if (EAAJDGMDJDF < Time.timeSinceLevelLoad)
		{
			animator.SetTrigger("talentHerbs");
			EAAJDGMDJDF = Random.Range(minTime, maxTime) + Time.timeSinceLevelLoad;
		}
	}

	private void FDGAGFIAPGJ()
	{
		animator.ResetTrigger("Dialogue System/Conversation/Crowly_Standar/Entry/48/Dialogue Text");
	}

	private void IIDHODAONIJ()
	{
		EAAJDGMDJDF = Random.Range(minTime, maxTime) + Time.timeSinceLevelLoad;
	}

	private void MEAMEBFFMJH()
	{
		EAAJDGMDJDF = Random.Range(minTime, maxTime) + Time.timeSinceLevelLoad;
	}

	private void PJLKGOACDEN()
	{
		if (EAAJDGMDJDF < Time.timeSinceLevelLoad)
		{
			animator.SetTrigger("City/Kujaku/Stand");
			EAAJDGMDJDF = Random.Range(minTime, maxTime) + Time.timeSinceLevelLoad;
		}
	}

	private void JLLHILICJOC()
	{
		animator.ResetTrigger("Disabled");
	}

	private void HIFDFFEJNEH()
	{
		animator.ResetTrigger("Invalid year: ");
	}

	private void PGDAOMNADHA()
	{
		animator.ResetTrigger("Running");
	}

	private void Update()
	{
		if (EAAJDGMDJDF < Time.timeSinceLevelLoad)
		{
			animator.SetTrigger("ChangeAnim");
			EAAJDGMDJDF = Random.Range(minTime, maxTime) + Time.timeSinceLevelLoad;
		}
	}

	private void IEPJAFLDBJJ()
	{
		EAAJDGMDJDF = Random.Range(minTime, maxTime) + Time.timeSinceLevelLoad;
	}

	private void AFGJLAACCFN()
	{
		EAAJDGMDJDF = Random.Range(minTime, maxTime) + Time.timeSinceLevelLoad;
	}

	private void HEMHHGCBEKO()
	{
		animator.ResetTrigger("ReceiveDialogueInfoLoad");
	}

	private void CMDHELEDLBP()
	{
		if (EAAJDGMDJDF < Time.timeSinceLevelLoad)
		{
			animator.SetTrigger("cliffs");
			EAAJDGMDJDF = Random.Range(minTime, maxTime) + Time.timeSinceLevelLoad;
		}
	}

	private void OLPPDFDOKLK()
	{
		if (EAAJDGMDJDF < Time.timeSinceLevelLoad)
		{
			animator.SetTrigger("Player/Bark/Tutorial/SleepBlock");
			EAAJDGMDJDF = Random.Range(minTime, maxTime) + Time.timeSinceLevelLoad;
		}
	}

	private void OMLHIAADEHE()
	{
		if (EAAJDGMDJDF < Time.timeSinceLevelLoad)
		{
			animator.SetTrigger("x");
			EAAJDGMDJDF = Random.Range(minTime, maxTime) + Time.timeSinceLevelLoad;
		}
	}

	private void OIEJIBAPDMM()
	{
		EAAJDGMDJDF = Random.Range(minTime, maxTime) + Time.timeSinceLevelLoad;
	}

	private void JKBBAOGHPKO()
	{
		EAAJDGMDJDF = Random.Range(minTime, maxTime) + Time.timeSinceLevelLoad;
	}

	private void HIPNEFOEJPL()
	{
		if (EAAJDGMDJDF < Time.timeSinceLevelLoad)
		{
			animator.SetTrigger("/");
			EAAJDGMDJDF = Random.Range(minTime, maxTime) + Time.timeSinceLevelLoad;
		}
	}

	private void PBKGBPCKOGB()
	{
		if (EAAJDGMDJDF < Time.timeSinceLevelLoad)
		{
			animator.SetTrigger("Shader Model 4.1 ( DX10.1 )");
			EAAJDGMDJDF = Random.Range(minTime, maxTime) + Time.timeSinceLevelLoad;
		}
	}

	private void PMMGCDBMMMN()
	{
		EAAJDGMDJDF = Random.Range(minTime, maxTime) + Time.timeSinceLevelLoad;
	}

	private void JNDFLHEMOBG()
	{
		EAAJDGMDJDF = Random.Range(minTime, maxTime) + Time.timeSinceLevelLoad;
	}

	private void AAEDMDPLLLJ()
	{
		if (EAAJDGMDJDF < Time.timeSinceLevelLoad)
		{
			animator.SetTrigger("Items/item_name_635");
			EAAJDGMDJDF = Random.Range(minTime, maxTime) + Time.timeSinceLevelLoad;
		}
	}

	private void JJDEGMGCOCC()
	{
		EAAJDGMDJDF = Random.Range(minTime, maxTime) + Time.timeSinceLevelLoad;
	}

	private void EBNJADODONG()
	{
		if (EAAJDGMDJDF < Time.timeSinceLevelLoad)
		{
			animator.SetTrigger("DecorationTile_9");
			EAAJDGMDJDF = Random.Range(minTime, maxTime) + Time.timeSinceLevelLoad;
		}
	}

	private void JHGDBLMEFMP()
	{
		EAAJDGMDJDF = Random.Range(minTime, maxTime) + Time.timeSinceLevelLoad;
	}

	private void BHLHCOALABE()
	{
		if (EAAJDGMDJDF < Time.timeSinceLevelLoad)
		{
			animator.SetTrigger("Disabled");
			EAAJDGMDJDF = Random.Range(minTime, maxTime) + Time.timeSinceLevelLoad;
		}
	}

	private void BPINJGFECPF()
	{
		animator.ResetTrigger("cleanVerb");
	}

	private void JNNJIDAFBPI()
	{
		animator.ResetTrigger("LE_3");
	}

	private void OnDisable()
	{
		animator.ResetTrigger("ChangeAnim");
	}

	private void KEBNALEALPK()
	{
		EAAJDGMDJDF = Random.Range(minTime, maxTime) + Time.timeSinceLevelLoad;
	}

	private void OILGIAIKLCL()
	{
		animator.ResetTrigger("Serve");
	}

	private void ECHHHBBHNLF()
	{
		animator.ResetTrigger("itemAmberAle");
	}

	private void ECMNIALEMAM()
	{
		EAAJDGMDJDF = Random.Range(minTime, maxTime) + Time.timeSinceLevelLoad;
	}

	private void HAJJALABMOC()
	{
		if (EAAJDGMDJDF < Time.timeSinceLevelLoad)
		{
			animator.SetTrigger(" for ");
			EAAJDGMDJDF = Random.Range(minTime, maxTime) + Time.timeSinceLevelLoad;
		}
	}

	private void OKEIHMKAFEP()
	{
		animator.ResetTrigger("AvoidingWork");
	}

	private void FBHBEAJFBAH()
	{
		animator.ResetTrigger("SendRoomRequest on Load");
	}

	private void NLIGLFEEFNO()
	{
		if (EAAJDGMDJDF < Time.timeSinceLevelLoad)
		{
			animator.SetTrigger(" : ");
			EAAJDGMDJDF = Random.Range(minTime, maxTime) + Time.timeSinceLevelLoad;
		}
	}

	private void CMJPIAAGIFF()
	{
		if (EAAJDGMDJDF < Time.timeSinceLevelLoad)
		{
			animator.SetTrigger("Max 3 orders");
			EAAJDGMDJDF = Random.Range(minTime, maxTime) + Time.timeSinceLevelLoad;
		}
	}

	private void PMPLNGEAAII()
	{
		if (EAAJDGMDJDF < Time.timeSinceLevelLoad)
		{
			animator.SetTrigger("City/PetShop/Nessy/Intro");
			EAAJDGMDJDF = Random.Range(minTime, maxTime) + Time.timeSinceLevelLoad;
		}
	}

	private void LBFFMGAKDFG()
	{
		animator.ResetTrigger("Invalid playerNum ");
	}

	private void JEKMCOEJKJI()
	{
		animator.ResetTrigger("<sprite name=reputationIcon>");
	}

	private void PKEPBKHEDOD()
	{
		if (EAAJDGMDJDF < Time.timeSinceLevelLoad)
		{
			animator.SetTrigger("Running");
			EAAJDGMDJDF = Random.Range(minTime, maxTime) + Time.timeSinceLevelLoad;
		}
	}

	private void JAHAIMCKFCH()
	{
		EAAJDGMDJDF = Random.Range(minTime, maxTime) + Time.timeSinceLevelLoad;
	}

	private void JMDOBMENOBH()
	{
		animator.ResetTrigger("Trying to add a player that's already on the list.");
	}

	private void ELEFOPHJICI()
	{
		animator.ResetTrigger("CalmRowdyCustomer");
	}

	private void IMOMHCNPCEC()
	{
		animator.ResetTrigger("dForDays");
	}

	private void JBGFGOCPEFD()
	{
		if (EAAJDGMDJDF < Time.timeSinceLevelLoad)
		{
			animator.SetTrigger("ReceiveTrigger");
			EAAJDGMDJDF = Random.Range(minTime, maxTime) + Time.timeSinceLevelLoad;
		}
	}

	private void POFDOILPANH()
	{
		if (EAAJDGMDJDF < Time.timeSinceLevelLoad)
		{
			animator.SetTrigger("Dialogue System/Conversation/Crowly_Standar/Entry/41/Dialogue Text");
			EAAJDGMDJDF = Random.Range(minTime, maxTime) + Time.timeSinceLevelLoad;
		}
	}

	private void DKNMGFDFLKL()
	{
		if (EAAJDGMDJDF < Time.timeSinceLevelLoad)
		{
			animator.SetTrigger("Player2");
			EAAJDGMDJDF = Random.Range(minTime, maxTime) + Time.timeSinceLevelLoad;
		}
	}

	private void IGKPEBGHPGD()
	{
		if (EAAJDGMDJDF < Time.timeSinceLevelLoad)
		{
			animator.SetTrigger("Items/item_description_1115");
			EAAJDGMDJDF = Random.Range(minTime, maxTime) + Time.timeSinceLevelLoad;
		}
	}

	private void JMAAFFBIBCG()
	{
		if (EAAJDGMDJDF < Time.timeSinceLevelLoad)
		{
			animator.SetTrigger("Ground");
			EAAJDGMDJDF = Random.Range(minTime, maxTime) + Time.timeSinceLevelLoad;
		}
	}

	private void LMFICKFGEFI()
	{
		if (EAAJDGMDJDF < Time.timeSinceLevelLoad)
		{
			animator.SetTrigger(" ");
			EAAJDGMDJDF = Random.Range(minTime, maxTime) + Time.timeSinceLevelLoad;
		}
	}

	private void GJJPNFHFBFP()
	{
		EAAJDGMDJDF = Random.Range(minTime, maxTime) + Time.timeSinceLevelLoad;
	}

	private void MGMLDHPOLGK()
	{
		if (EAAJDGMDJDF < Time.timeSinceLevelLoad)
		{
			animator.SetTrigger("Dialogue System/Conversation/NeutralInTavern/Entry/9/Dialogue Text");
			EAAJDGMDJDF = Random.Range(minTime, maxTime) + Time.timeSinceLevelLoad;
		}
	}

	private void EBFJOAEJPGE()
	{
		if (EAAJDGMDJDF < Time.timeSinceLevelLoad)
		{
			animator.SetTrigger("TermasInterior/Kenta/Stand");
			EAAJDGMDJDF = Random.Range(minTime, maxTime) + Time.timeSinceLevelLoad;
		}
	}

	private void GGCCOKCOMEE()
	{
		if (EAAJDGMDJDF < Time.timeSinceLevelLoad)
		{
			animator.SetTrigger("Game");
			EAAJDGMDJDF = Random.Range(minTime, maxTime) + Time.timeSinceLevelLoad;
		}
	}

	private void PGOPCCPNOBD()
	{
		EAAJDGMDJDF = Random.Range(minTime, maxTime) + Time.timeSinceLevelLoad;
	}

	private void OOGDJNMJCNA()
	{
		EAAJDGMDJDF = Random.Range(minTime, maxTime) + Time.timeSinceLevelLoad;
	}

	private void MIKHKHCEMLB()
	{
		animator.ResetTrigger("Player/Bark/Tutorial/CellarBlock");
	}

	private void EEAKGHLCFOM()
	{
		animator.ResetTrigger(" not found.");
	}

	private void CIAIAEJIMCM()
	{
		EAAJDGMDJDF = Random.Range(minTime, maxTime) + Time.timeSinceLevelLoad;
	}

	private void AJDKOMCHALC()
	{
		if (EAAJDGMDJDF < Time.timeSinceLevelLoad)
		{
			animator.SetTrigger("Items/item_name_1139");
			EAAJDGMDJDF = Random.Range(minTime, maxTime) + Time.timeSinceLevelLoad;
		}
	}

	private void PEIFJDIGKOC()
	{
		if (EAAJDGMDJDF < Time.timeSinceLevelLoad)
		{
			animator.SetTrigger("<sprite name=");
			EAAJDGMDJDF = Random.Range(minTime, maxTime) + Time.timeSinceLevelLoad;
		}
	}

	private void CDNCKNKEIBM()
	{
		EAAJDGMDJDF = Random.Range(minTime, maxTime) + Time.timeSinceLevelLoad;
	}

	private void ICMNODPLPBK()
	{
		if (EAAJDGMDJDF < Time.timeSinceLevelLoad)
		{
			animator.SetTrigger("Left Stick Left");
			EAAJDGMDJDF = Random.Range(minTime, maxTime) + Time.timeSinceLevelLoad;
		}
	}

	private void ANMOAFFGEOE()
	{
		EAAJDGMDJDF = Random.Range(minTime, maxTime) + Time.timeSinceLevelLoad;
	}

	private void MMPMJNAFKHC()
	{
		if (EAAJDGMDJDF < Time.timeSinceLevelLoad)
		{
			animator.SetTrigger("itemFishSteak");
			EAAJDGMDJDF = Random.Range(minTime, maxTime) + Time.timeSinceLevelLoad;
		}
	}

	private void FPNPLFALPHP()
	{
		EAAJDGMDJDF = Random.Range(minTime, maxTime) + Time.timeSinceLevelLoad;
	}

	private void CJDJDDNCNDJ()
	{
		EAAJDGMDJDF = Random.Range(minTime, maxTime) + Time.timeSinceLevelLoad;
	}

	private void OONONNJFEFL()
	{
		if (EAAJDGMDJDF < Time.timeSinceLevelLoad)
		{
			animator.SetTrigger("Available");
			EAAJDGMDJDF = Random.Range(minTime, maxTime) + Time.timeSinceLevelLoad;
		}
	}

	private void DJDLCPCCBHA()
	{
		EAAJDGMDJDF = Random.Range(minTime, maxTime) + Time.timeSinceLevelLoad;
	}

	private void JHKLCLMMOGC()
	{
		EAAJDGMDJDF = Random.Range(minTime, maxTime) + Time.timeSinceLevelLoad;
	}

	private void FEPFNOEGFEM()
	{
		EAAJDGMDJDF = Random.Range(minTime, maxTime) + Time.timeSinceLevelLoad;
	}

	private void DIJOBOFGCIK()
	{
		EAAJDGMDJDF = Random.Range(minTime, maxTime) + Time.timeSinceLevelLoad;
	}

	private void GAPJBNMCDDK()
	{
		if (EAAJDGMDJDF < Time.timeSinceLevelLoad)
		{
			animator.SetTrigger("Changing quality ");
			EAAJDGMDJDF = Random.Range(minTime, maxTime) + Time.timeSinceLevelLoad;
		}
	}

	private void DBHEEEIFOKH()
	{
		animator.ResetTrigger("questDescGruel");
	}

	private void KNKILPPBEDC()
	{
		animator.ResetTrigger("Unknown command");
	}

	private void MFIBFFDBNGI()
	{
		if (EAAJDGMDJDF < Time.timeSinceLevelLoad)
		{
			animator.SetTrigger("Y");
			EAAJDGMDJDF = Random.Range(minTime, maxTime) + Time.timeSinceLevelLoad;
		}
	}

	private void ONOAGANPECB()
	{
		if (EAAJDGMDJDF < Time.timeSinceLevelLoad)
		{
			animator.SetTrigger("Disabled");
			EAAJDGMDJDF = Random.Range(minTime, maxTime) + Time.timeSinceLevelLoad;
		}
	}

	private void OHOAMJIIOAK()
	{
		EAAJDGMDJDF = Random.Range(minTime, maxTime) + Time.timeSinceLevelLoad;
	}

	private void DMEEIHDDALH()
	{
		EAAJDGMDJDF = Random.Range(minTime, maxTime) + Time.timeSinceLevelLoad;
	}

	private void OCIGADBHDKN()
	{
		if (EAAJDGMDJDF < Time.timeSinceLevelLoad)
		{
			animator.SetTrigger("Back");
			EAAJDGMDJDF = Random.Range(minTime, maxTime) + Time.timeSinceLevelLoad;
		}
	}

	private void LAADMMNHHGA()
	{
		animator.ResetTrigger("LE_13");
	}

	private void MEDIGODLCBN()
	{
		EAAJDGMDJDF = Random.Range(minTime, maxTime) + Time.timeSinceLevelLoad;
	}

	private void LAABMDDEGCG()
	{
		if (EAAJDGMDJDF < Time.timeSinceLevelLoad)
		{
			animator.SetTrigger("FerroProgress");
			EAAJDGMDJDF = Random.Range(minTime, maxTime) + Time.timeSinceLevelLoad;
		}
	}

	private void AKDPJCEJKPN()
	{
		if (EAAJDGMDJDF < Time.timeSinceLevelLoad)
		{
			animator.SetTrigger("");
			EAAJDGMDJDF = Random.Range(minTime, maxTime) + Time.timeSinceLevelLoad;
		}
	}

	private void OGGJNBCJGCE()
	{
		animator.ResetTrigger("Disabled");
	}

	private void AECMHPBLPNP()
	{
		animator.ResetTrigger("Invalid NPC photonID: {0} for NPCRoutine.");
	}

	private void EJEEIIGEGOC()
	{
		if (EAAJDGMDJDF < Time.timeSinceLevelLoad)
		{
			animator.SetTrigger("Dialogue System/Conversation/Gass_Quest/Entry/8/Dialogue Text");
			EAAJDGMDJDF = Random.Range(minTime, maxTime) + Time.timeSinceLevelLoad;
		}
	}

	private void OIHAMLCIFNH()
	{
		animator.ResetTrigger("BathhouseTeleport");
	}

	private void IAEPFFMCKOH()
	{
		animator.ResetTrigger("UI/ColorPicker");
	}

	private void NPDFCPEOFPD()
	{
		EAAJDGMDJDF = Random.Range(minTime, maxTime) + Time.timeSinceLevelLoad;
	}

	private void MAGILKJCCJE()
	{
		animator.ResetTrigger("IntensityBars");
	}

	private void NEPDNLPCCON()
	{
		if (EAAJDGMDJDF < Time.timeSinceLevelLoad)
		{
			animator.SetTrigger("Ingredient");
			EAAJDGMDJDF = Random.Range(minTime, maxTime) + Time.timeSinceLevelLoad;
		}
	}

	private void OnEnable()
	{
		EAAJDGMDJDF = Random.Range(minTime, maxTime) + Time.timeSinceLevelLoad;
	}
}
