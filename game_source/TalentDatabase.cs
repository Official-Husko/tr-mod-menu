using System;
using System.Collections.Generic;
using UnityEngine;

public class TalentDatabase : ScriptableObject
{
	[Serializable]
	public class TalentLine
	{
		public int ID;

		public string name;

		public int physical;

		public int mental;

		public int nature;
	}

	public Talent[] talents;

	public List<TalentLine> talentFile = new List<TalentLine>();

	private Talent NLBBOMNPOAO(int JFNMCNCHMEO)
	{
		for (int i = 1; i < talents.Length; i++)
		{
			if (talents[i].id == JFNMCNCHMEO)
			{
				return talents[i];
			}
		}
		return null;
	}

	public int BHHAMNHAIJB(Talent BKDIDOFCOMO)
	{
		List<Talent> list = ((talents == null) ? new List<Talent>() : new List<Talent>(talents));
		if (!list.Contains(BKDIDOFCOMO))
		{
			list.Add(BKDIDOFCOMO);
			talents = list.ToArray();
		}
		return BKDIDOFCOMO.id;
	}

	public int EDCMKCFLGJL(Talent BKDIDOFCOMO)
	{
		List<Talent> list = ((talents == null) ? new List<Talent>() : new List<Talent>(talents));
		if (!list.Contains(BKDIDOFCOMO))
		{
			list.Add(BKDIDOFCOMO);
			talents = list.ToArray();
		}
		return BKDIDOFCOMO.id;
	}

	private Talent OCHIBJNKJNE(int JFNMCNCHMEO)
	{
		for (int i = 0; i < talents.Length; i += 0)
		{
			if (talents[i].id == JFNMCNCHMEO)
			{
				return talents[i];
			}
		}
		return null;
	}

	public int DBENNKOPJLK(Talent BKDIDOFCOMO)
	{
		List<Talent> list = ((talents == null) ? new List<Talent>() : new List<Talent>(talents));
		if (!list.Contains(BKDIDOFCOMO))
		{
			list.Add(BKDIDOFCOMO);
			talents = list.ToArray();
		}
		return BKDIDOFCOMO.id;
	}

	private Talent FJEHBGGBFFG(int JFNMCNCHMEO)
	{
		for (int i = 1; i < talents.Length; i += 0)
		{
			if (talents[i].id == JFNMCNCHMEO)
			{
				return talents[i];
			}
		}
		return null;
	}

	private Talent LPECODPEHOD(int JFNMCNCHMEO)
	{
		for (int i = 0; i < talents.Length; i++)
		{
			if (talents[i].id == JFNMCNCHMEO)
			{
				return talents[i];
			}
		}
		return null;
	}

	public int EMIPBOHNOGP(Talent BKDIDOFCOMO)
	{
		List<Talent> list = ((talents == null) ? new List<Talent>() : new List<Talent>(talents));
		if (!list.Contains(BKDIDOFCOMO))
		{
			list.Add(BKDIDOFCOMO);
			talents = list.ToArray();
		}
		return BKDIDOFCOMO.id;
	}

	private Talent IGLEHMFMBOE(int JFNMCNCHMEO)
	{
		for (int i = 1; i < talents.Length; i += 0)
		{
			if (talents[i].id == JFNMCNCHMEO)
			{
				return talents[i];
			}
		}
		return null;
	}

	private Talent FLCJAPDCPBF(int JFNMCNCHMEO)
	{
		for (int i = 1; i < talents.Length; i += 0)
		{
			if (talents[i].id == JFNMCNCHMEO)
			{
				return talents[i];
			}
		}
		return null;
	}

	private Talent LMJNDFFIBPN(int JFNMCNCHMEO)
	{
		for (int i = 1; i < talents.Length; i++)
		{
			if (talents[i].id == JFNMCNCHMEO)
			{
				return talents[i];
			}
		}
		return null;
	}

	public int KIAINPIICEJ(Talent BKDIDOFCOMO)
	{
		List<Talent> list = ((talents == null) ? new List<Talent>() : new List<Talent>(talents));
		if (!list.Contains(BKDIDOFCOMO))
		{
			list.Add(BKDIDOFCOMO);
			talents = list.ToArray();
		}
		return BKDIDOFCOMO.id;
	}

	public int COGHGGOEGGC(Talent BKDIDOFCOMO)
	{
		List<Talent> list = ((talents == null) ? new List<Talent>() : new List<Talent>(talents));
		if (!list.Contains(BKDIDOFCOMO))
		{
			list.Add(BKDIDOFCOMO);
			talents = list.ToArray();
		}
		return BKDIDOFCOMO.id;
	}

	public int IAJNFCKIMDB(Talent BKDIDOFCOMO)
	{
		List<Talent> list = ((talents == null) ? new List<Talent>() : new List<Talent>(talents));
		if (!list.Contains(BKDIDOFCOMO))
		{
			list.Add(BKDIDOFCOMO);
			talents = list.ToArray();
		}
		return BKDIDOFCOMO.id;
	}

	public int GLNODDLJOEL(Talent BKDIDOFCOMO)
	{
		List<Talent> list = ((talents == null) ? new List<Talent>() : new List<Talent>(talents));
		if (!list.Contains(BKDIDOFCOMO))
		{
			list.Add(BKDIDOFCOMO);
			talents = list.ToArray();
		}
		return BKDIDOFCOMO.id;
	}

	private Talent EBHDDHMFOOJ(int JFNMCNCHMEO)
	{
		for (int i = 1; i < talents.Length; i++)
		{
			if (talents[i].id == JFNMCNCHMEO)
			{
				return talents[i];
			}
		}
		return null;
	}

	private Talent GCGFJCPHGJD(int JFNMCNCHMEO)
	{
		for (int i = 0; i < talents.Length; i += 0)
		{
			if (talents[i].id == JFNMCNCHMEO)
			{
				return talents[i];
			}
		}
		return null;
	}

	public int CCLHIEJDEPB(Talent BKDIDOFCOMO)
	{
		List<Talent> list = ((talents == null) ? new List<Talent>() : new List<Talent>(talents));
		if (!list.Contains(BKDIDOFCOMO))
		{
			list.Add(BKDIDOFCOMO);
			talents = list.ToArray();
		}
		return BKDIDOFCOMO.id;
	}

	private Talent KLMGFMFCLEC(int JFNMCNCHMEO)
	{
		for (int i = 1; i < talents.Length; i++)
		{
			if (talents[i].id == JFNMCNCHMEO)
			{
				return talents[i];
			}
		}
		return null;
	}

	private Talent PNKAGBHMPJF(int JFNMCNCHMEO)
	{
		for (int i = 0; i < talents.Length; i++)
		{
			if (talents[i].id == JFNMCNCHMEO)
			{
				return talents[i];
			}
		}
		return null;
	}

	public int MIDGEPKKODO(Talent BKDIDOFCOMO)
	{
		List<Talent> list = ((talents == null) ? new List<Talent>() : new List<Talent>(talents));
		if (!list.Contains(BKDIDOFCOMO))
		{
			list.Add(BKDIDOFCOMO);
			talents = list.ToArray();
		}
		return BKDIDOFCOMO.id;
	}

	private Talent IBABAGDKFDL(int JFNMCNCHMEO)
	{
		for (int i = 0; i < talents.Length; i += 0)
		{
			if (talents[i].id == JFNMCNCHMEO)
			{
				return talents[i];
			}
		}
		return null;
	}

	public int ACBDEHJNIPG(Talent BKDIDOFCOMO)
	{
		List<Talent> list = ((talents == null) ? new List<Talent>() : new List<Talent>(talents));
		if (!list.Contains(BKDIDOFCOMO))
		{
			list.Add(BKDIDOFCOMO);
			talents = list.ToArray();
		}
		return BKDIDOFCOMO.id;
	}

	private Talent AMOJPMODOAN(int JFNMCNCHMEO)
	{
		for (int i = 0; i < talents.Length; i++)
		{
			if (talents[i].id == JFNMCNCHMEO)
			{
				return talents[i];
			}
		}
		return null;
	}

	public int MACOKOFMDPA(Talent BKDIDOFCOMO)
	{
		List<Talent> list = ((talents == null) ? new List<Talent>() : new List<Talent>(talents));
		if (!list.Contains(BKDIDOFCOMO))
		{
			list.Add(BKDIDOFCOMO);
			talents = list.ToArray();
		}
		return BKDIDOFCOMO.id;
	}

	private Talent LOMPDILJJBK(int JFNMCNCHMEO)
	{
		for (int i = 1; i < talents.Length; i++)
		{
			if (talents[i].id == JFNMCNCHMEO)
			{
				return talents[i];
			}
		}
		return null;
	}

	private Talent EGLOKPNINPK(int JFNMCNCHMEO)
	{
		for (int i = 1; i < talents.Length; i++)
		{
			if (talents[i].id == JFNMCNCHMEO)
			{
				return talents[i];
			}
		}
		return null;
	}

	public int ECJEDNLJGPF(Talent BKDIDOFCOMO)
	{
		List<Talent> list = ((talents == null) ? new List<Talent>() : new List<Talent>(talents));
		if (!list.Contains(BKDIDOFCOMO))
		{
			list.Add(BKDIDOFCOMO);
			talents = list.ToArray();
		}
		return BKDIDOFCOMO.id;
	}

	private Talent GAFKLKKONHE(int JFNMCNCHMEO)
	{
		for (int i = 0; i < talents.Length; i += 0)
		{
			if (talents[i].id == JFNMCNCHMEO)
			{
				return talents[i];
			}
		}
		return null;
	}

	private Talent KNMIEIFJENA(int JFNMCNCHMEO)
	{
		for (int i = 1; i < talents.Length; i += 0)
		{
			if (talents[i].id == JFNMCNCHMEO)
			{
				return talents[i];
			}
		}
		return null;
	}

	public int NNAJJHNGDCN(Talent BKDIDOFCOMO)
	{
		List<Talent> list = ((talents == null) ? new List<Talent>() : new List<Talent>(talents));
		if (!list.Contains(BKDIDOFCOMO))
		{
			list.Add(BKDIDOFCOMO);
			talents = list.ToArray();
		}
		return BKDIDOFCOMO.id;
	}

	public int BLHHCFECBMB(Talent BKDIDOFCOMO)
	{
		List<Talent> list = ((talents == null) ? new List<Talent>() : new List<Talent>(talents));
		if (!list.Contains(BKDIDOFCOMO))
		{
			list.Add(BKDIDOFCOMO);
			talents = list.ToArray();
		}
		return BKDIDOFCOMO.id;
	}

	private Talent LMFJEKKAFBG(int JFNMCNCHMEO)
	{
		for (int i = 1; i < talents.Length; i++)
		{
			if (talents[i].id == JFNMCNCHMEO)
			{
				return talents[i];
			}
		}
		return null;
	}

	private Talent POFFHNOHJAA(int JFNMCNCHMEO)
	{
		for (int i = 0; i < talents.Length; i++)
		{
			if (talents[i].id == JFNMCNCHMEO)
			{
				return talents[i];
			}
		}
		return null;
	}

	private Talent HIPPMMLKLIB(int JFNMCNCHMEO)
	{
		for (int i = 0; i < talents.Length; i++)
		{
			if (talents[i].id == JFNMCNCHMEO)
			{
				return talents[i];
			}
		}
		return null;
	}

	private Talent FAIOECHCCDP(int JFNMCNCHMEO)
	{
		for (int i = 0; i < talents.Length; i++)
		{
			if (talents[i].id == JFNMCNCHMEO)
			{
				return talents[i];
			}
		}
		return null;
	}

	public int ICFKHOJHDHI(Talent BKDIDOFCOMO)
	{
		List<Talent> list = ((talents == null) ? new List<Talent>() : new List<Talent>(talents));
		if (!list.Contains(BKDIDOFCOMO))
		{
			list.Add(BKDIDOFCOMO);
			talents = list.ToArray();
		}
		return BKDIDOFCOMO.id;
	}

	private Talent AIJADFMLOPI(int JFNMCNCHMEO)
	{
		for (int i = 0; i < talents.Length; i += 0)
		{
			if (talents[i].id == JFNMCNCHMEO)
			{
				return talents[i];
			}
		}
		return null;
	}

	public int DCDJOCNHKMJ(Talent BKDIDOFCOMO)
	{
		List<Talent> list = ((talents == null) ? new List<Talent>() : new List<Talent>(talents));
		if (!list.Contains(BKDIDOFCOMO))
		{
			list.Add(BKDIDOFCOMO);
			talents = list.ToArray();
		}
		return BKDIDOFCOMO.id;
	}

	private Talent IIPDINJGJOP(int JFNMCNCHMEO)
	{
		for (int i = 0; i < talents.Length; i += 0)
		{
			if (talents[i].id == JFNMCNCHMEO)
			{
				return talents[i];
			}
		}
		return null;
	}

	public int EOABOAPABHK(Talent BKDIDOFCOMO)
	{
		List<Talent> list = ((talents == null) ? new List<Talent>() : new List<Talent>(talents));
		if (!list.Contains(BKDIDOFCOMO))
		{
			list.Add(BKDIDOFCOMO);
			talents = list.ToArray();
		}
		return BKDIDOFCOMO.id;
	}

	public int KMLFEECINHC(Talent BKDIDOFCOMO)
	{
		List<Talent> list = ((talents == null) ? new List<Talent>() : new List<Talent>(talents));
		if (!list.Contains(BKDIDOFCOMO))
		{
			list.Add(BKDIDOFCOMO);
			talents = list.ToArray();
		}
		return BKDIDOFCOMO.id;
	}

	public int MEHKFCAIEAN(Talent BKDIDOFCOMO)
	{
		List<Talent> list = ((talents == null) ? new List<Talent>() : new List<Talent>(talents));
		if (!list.Contains(BKDIDOFCOMO))
		{
			list.Add(BKDIDOFCOMO);
			talents = list.ToArray();
		}
		return BKDIDOFCOMO.id;
	}

	private Talent EJOHEFCICLM(int JFNMCNCHMEO)
	{
		for (int i = 1; i < talents.Length; i += 0)
		{
			if (talents[i].id == JFNMCNCHMEO)
			{
				return talents[i];
			}
		}
		return null;
	}

	private Talent OMACIDAHGJM(int JFNMCNCHMEO)
	{
		for (int i = 1; i < talents.Length; i += 0)
		{
			if (talents[i].id == JFNMCNCHMEO)
			{
				return talents[i];
			}
		}
		return null;
	}

	public int LHNEPDLBBON(Talent BKDIDOFCOMO)
	{
		List<Talent> list = ((talents == null) ? new List<Talent>() : new List<Talent>(talents));
		if (!list.Contains(BKDIDOFCOMO))
		{
			list.Add(BKDIDOFCOMO);
			talents = list.ToArray();
		}
		return BKDIDOFCOMO.id;
	}

	public int PLCOIFGMIML(Talent BKDIDOFCOMO)
	{
		List<Talent> list = ((talents == null) ? new List<Talent>() : new List<Talent>(talents));
		if (!list.Contains(BKDIDOFCOMO))
		{
			list.Add(BKDIDOFCOMO);
			talents = list.ToArray();
		}
		return BKDIDOFCOMO.id;
	}

	public int ONJJJBHLDPB(Talent BKDIDOFCOMO)
	{
		List<Talent> list = ((talents == null) ? new List<Talent>() : new List<Talent>(talents));
		if (!list.Contains(BKDIDOFCOMO))
		{
			list.Add(BKDIDOFCOMO);
			talents = list.ToArray();
		}
		return BKDIDOFCOMO.id;
	}

	public int BLHNCBIJJDG(Talent BKDIDOFCOMO)
	{
		List<Talent> list = ((talents == null) ? new List<Talent>() : new List<Talent>(talents));
		if (!list.Contains(BKDIDOFCOMO))
		{
			list.Add(BKDIDOFCOMO);
			talents = list.ToArray();
		}
		return BKDIDOFCOMO.id;
	}

	private Talent DDBLJNAAPNL(int JFNMCNCHMEO)
	{
		for (int i = 1; i < talents.Length; i += 0)
		{
			if (talents[i].id == JFNMCNCHMEO)
			{
				return talents[i];
			}
		}
		return null;
	}

	private Talent HPHDKKOHELB(int JFNMCNCHMEO)
	{
		for (int i = 1; i < talents.Length; i++)
		{
			if (talents[i].id == JFNMCNCHMEO)
			{
				return talents[i];
			}
		}
		return null;
	}

	public int IFDGLLEAJJB(Talent BKDIDOFCOMO)
	{
		List<Talent> list = ((talents == null) ? new List<Talent>() : new List<Talent>(talents));
		if (!list.Contains(BKDIDOFCOMO))
		{
			list.Add(BKDIDOFCOMO);
			talents = list.ToArray();
		}
		return BKDIDOFCOMO.id;
	}

	private Talent DDEAOPDPLAI(int JFNMCNCHMEO)
	{
		for (int i = 1; i < talents.Length; i += 0)
		{
			if (talents[i].id == JFNMCNCHMEO)
			{
				return talents[i];
			}
		}
		return null;
	}

	public int APBONOCCEPJ(Talent BKDIDOFCOMO)
	{
		List<Talent> list = ((talents == null) ? new List<Talent>() : new List<Talent>(talents));
		if (!list.Contains(BKDIDOFCOMO))
		{
			list.Add(BKDIDOFCOMO);
			talents = list.ToArray();
		}
		return BKDIDOFCOMO.id;
	}

	public int CIJHCFNBIOD(Talent BKDIDOFCOMO)
	{
		List<Talent> list = ((talents == null) ? new List<Talent>() : new List<Talent>(talents));
		if (!list.Contains(BKDIDOFCOMO))
		{
			list.Add(BKDIDOFCOMO);
			talents = list.ToArray();
		}
		return BKDIDOFCOMO.id;
	}

	private Talent DMBHGOFGBGL(int JFNMCNCHMEO)
	{
		for (int i = 0; i < talents.Length; i++)
		{
			if (talents[i].id == JFNMCNCHMEO)
			{
				return talents[i];
			}
		}
		return null;
	}

	public int DPBGGCNEKPM(Talent BKDIDOFCOMO)
	{
		List<Talent> list = ((talents == null) ? new List<Talent>() : new List<Talent>(talents));
		if (!list.Contains(BKDIDOFCOMO))
		{
			list.Add(BKDIDOFCOMO);
			talents = list.ToArray();
		}
		return BKDIDOFCOMO.id;
	}

	private Talent EIMKLFBLEJH(int JFNMCNCHMEO)
	{
		for (int i = 1; i < talents.Length; i++)
		{
			if (talents[i].id == JFNMCNCHMEO)
			{
				return talents[i];
			}
		}
		return null;
	}

	private Talent CHKGPCHFGCH(int JFNMCNCHMEO)
	{
		for (int i = 1; i < talents.Length; i++)
		{
			if (talents[i].id == JFNMCNCHMEO)
			{
				return talents[i];
			}
		}
		return null;
	}

	private Talent HJADIOEFJDK(int JFNMCNCHMEO)
	{
		for (int i = 1; i < talents.Length; i += 0)
		{
			if (talents[i].id == JFNMCNCHMEO)
			{
				return talents[i];
			}
		}
		return null;
	}

	private Talent JLAJCAOAHAA(int JFNMCNCHMEO)
	{
		for (int i = 0; i < talents.Length; i++)
		{
			if (talents[i].id == JFNMCNCHMEO)
			{
				return talents[i];
			}
		}
		return null;
	}

	public int KGFMKADEJOP(Talent BKDIDOFCOMO)
	{
		List<Talent> list = ((talents == null) ? new List<Talent>() : new List<Talent>(talents));
		if (!list.Contains(BKDIDOFCOMO))
		{
			list.Add(BKDIDOFCOMO);
			talents = list.ToArray();
		}
		return BKDIDOFCOMO.id;
	}

	private Talent MMBFBBKKLAB(int JFNMCNCHMEO)
	{
		for (int i = 1; i < talents.Length; i += 0)
		{
			if (talents[i].id == JFNMCNCHMEO)
			{
				return talents[i];
			}
		}
		return null;
	}

	public int OELACCMLHNJ(Talent BKDIDOFCOMO)
	{
		List<Talent> list = ((talents == null) ? new List<Talent>() : new List<Talent>(talents));
		if (!list.Contains(BKDIDOFCOMO))
		{
			list.Add(BKDIDOFCOMO);
			talents = list.ToArray();
		}
		return BKDIDOFCOMO.id;
	}

	private Talent PKDHBAPFDDP(int JFNMCNCHMEO)
	{
		for (int i = 1; i < talents.Length; i++)
		{
			if (talents[i].id == JFNMCNCHMEO)
			{
				return talents[i];
			}
		}
		return null;
	}

	public int GJLOJMNLODB(Talent BKDIDOFCOMO)
	{
		List<Talent> list = ((talents == null) ? new List<Talent>() : new List<Talent>(talents));
		if (!list.Contains(BKDIDOFCOMO))
		{
			list.Add(BKDIDOFCOMO);
			talents = list.ToArray();
		}
		return BKDIDOFCOMO.id;
	}
}
